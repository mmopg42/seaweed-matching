# 리팩토링 요구사항 문서: monitoring_app 모듈 분리

## 1. 개요 (Overview)

본 문서는 현재 약 3,000줄의 단일 스크립트로 구현된 `monitoring_app.py`를 "책임 단위"로 분리하여, 유지보수성을 향상하고 향후 새로운 GUI 구현을 위한 기반을 마련하는 리팩토링 작업의 요구사항을 정의합니다.

**현재 문제 상황:**
- `monitoring_app.py`는 92개의 메서드를 가진 `MainWindow` 클래스와 `DragSelectWidget` 클래스를 포함하며, UI 초기화, 이벤트 처리, 파일 작업 실행, 이미지 관리, 통계 계산, 설정 관리 등 여러 책임을 모두 담당하고 있습니다.
- 현재 프로젝트에는 이미 28개의 전문화된 모듈이 존재하지만, `monitoring_app.py`는 이들 모듈을 단순히 호출하는 것을 넘어 많은 비즈니스 로직을 직접 포함하고 있어, 코드의 중복과 책임 분산의 어려움이 발생하고 있습니다.
- 단일 파일의 비대화로 인해 코드 가독성이 낮고, 신규 기능 추가 시 영향 범위 파악이 어려우며, 테스트 작성이 어렵습니다.

**최종 목표:**
단일 책임 원칙에 따라 `monitoring_app.py`를 여러 모듈로 분리하고, 기존 28개 모듈과의 중복을 제거하여, 새로운 GUI 프레임워크 도입 시에도 비즈니스 로직을 재사용할 수 있는 구조를 구축합니다.

---

## 2. 리팩토링 목표 (Refactoring Goals)

1. **단일 책임 원칙 준수**: `MainWindow` 클래스가 담당하는 다양한 책임을 식별하고, 각 책임을 담당하는 별도 모듈/클래스로 분리합니다.

2. **기존 기능 동일성 유지**: 리팩토링 후에도 사용자가 경험하는 모든 기능이 동일하게 작동해야 합니다. UI 동작, 파일 처리 로직, 통계 계산 등 모든 기능의 동작이 정확히 보존되어야 합니다.

3. **기존 모듈 중복 제거**: 현재 `monitoring_app.py`에서 직접 구현된 로직 중, 이미 기존 28개 모듈에서 제공하는 기능은 해당 모듈을 활용하도록 리팩토링합니다.

4. **테스트 가능성 향상**: 분리된 모듈들은 독립적으로 테스트 가능해야 하며, 단위 테스트 작성이 용이한 구조를 갖추어야 합니다.

5. **GUI 독립성 확보**: 비즈니스 로직을 GUI 코드와 분리하여, 향후 다른 GUI 프레임워크(예: React, Vue, Flutter 등)로 전환하더라도 비즈니스 로직을 재사용할 수 있도록 합니다.

6. **명확한 의존성 구조**: 모듈 간 의존성을 명확히 하고, 순환 의존성을 제거하여 코드의 이해와 유지보수를 용이하게 합니다.

7. **코드 가독성 향상**: 각 모듈의 역할이 명확히 드러나도록 이름을 지정하고, 파일 구조를 체계화하여 신규 개발자도 쉽게 이해할 수 있는 코드베이스를 만듭니다.

8. **확장성 확보**: 새로운 기능 추가 시, 어디에 코드를 작성해야 하는지 명확하며, 기존 코드에 미치는 영향을 최소화할 수 있는 구조를 확립합니다.

9. **성능 유지**: 리팩토링으로 인한 성능 저하가 없어야 하며, 가능하다면 코드 최적화를 통해 성능을 개선합니다.

10. **문서화 기준 수립**: 각 분리된 모듈은 `docs/modules/` 디렉토리에 문서를 포함해야 하며, 모듈 간 상호작용을 설명하는 아키텍처 문서를 작성합니다.

---

## 3. 범위 (Scope)

### 3.1 이번 리팩토링에서 다룰 범위 (In Scope)

1. **`monitoring_app.py` 책임 분석 및 분류**
   - `MainWindow` 클래스의 92개 메서드를 기능별로 분류
   - 각 기능이 담당하는 책임을 명확히 정의

2. **기존 28개 모듈 활용 계획 수립**
   - 현재 `monitoring_app.py`에서 수행 중인 작업 중, 기존 모듈로 이관 가능한 로직 식별
   - 중복 제거 대상 리스트 작성

3. **새로운 모듈 후보 정의**
   - 기존 모듈로 커버되지 않는 책임에 대해 새로운 모듈 필요성 검토
   - 새 모듈 생성 시, 과도한 책임 집중 방지를 위한 설계 검토

4. **리팩토링 우선순위 설정**
   - 의존성이 적고 독립적으로 분리 가능한 모듈부터 작업 시작
   - 점진적 리팩토링 전략 수립

5. **검증 전략 수립**
   - 리팩토링 각 단계에서 기능 동일성을 검증하는 방법 정의
   - 자동 테스트 및 수동 테스ト 계획 수립

6. **`DragSelectWidget` 처리 방침 결정**
   - UI 컴포넌트인 `DragSelectWidget`을 `ui_components.py`로 이동할지 여부 결정
   - 사용자 확인 필요

### 3.2 이번에는 다루지 않을 범위 (Out of Scope)

1. **새 GUI 자체 구현**
   - 이번 리팩토링의 목적은 새 GUI를 만드는 것이 아니라, 새 GUI를 만들 수 있는 **기반**을 마련하는 것입니다.
   - 실제 새 GUI 개발(React, Vue, Flutter 등)은 리팩토링 완료 후 별도 프로젝트로 진행됩니다.

2. **기존 기능의 변경 또는 추가**
   - 리팩토링 중에는 기존 기능을 변경하거나 새로운 기능을 추가하지 않습니다.
   - 기능 변경은 리팩토링 완료 후 별도로 진행합니다.

3. **전체 아키텍처 재설계**
   - 현재 PySide6 기반 GUI, watchdog 기반 파일 감시, QThread 기반 비동기 처리 등의 핵심 아키텍처는 유지합니다.
   - 전체 아키텍처 변경은 이번 범위를 벗어납니다.

4. **성능 최적화 작업**
   - 리팩토링 중 명백한 비효율이 발견되면 개선하되, 대규모 성능 최적화는 별도 작업으로 진행합니다.

5. **기타 모듈 개선**
   - 28개 기존 모듈의 내부 리팩토링은 이번 범위에 포함되지 않습니다.
   - `monitoring_app.py`와의 상호작용 부분만 다룹니다.

---

## 4. 기존 모듈 요약 (Existing Modules Summary)

프로젝트에는 현재 28개의 모듈이 존재하며, `docs/MODULE_DOCS_SUMMARY.md`에 전체 요약이 문서화되어 있습니다.

### 4.1 카테고리별 모듈 분류

**코어 모듈 (2개)**
- `monitoring_app.py`: 메인 윈도우 (약 3000줄) - **리팩토링 대상**
- `main.py`: 애플리케이션 진입점

**UI 관련 (5개)**
- `ui_builder.py`: UI 생성 전담 (548줄)
- `ui_components.py`: 재사용 UI 컴포넌트
- `log_panel.py`: 로그 패널 위젯 (80줄)
- `preview_dialog.py`: 이미지 미리보기 (56줄)
- `tooltips.py`: 툴팁 관리 (118줄)

**파일 처리 (4개)**
- `file_matcher.py`: 파일 매칭 로직 (384줄)
- `file_count_worker.py`: 파일 개수 모니터링 (231줄)
- `file_operations.py`: 파일 작업 워커 (539줄)
- `file_operation_manager.py`: 파일 작업 비즈니스 로직 (372줄)

**그룹 및 상태 관리 (3개)**
- `group_manager.py`: 그룹 생성 로직
- `group_state_manager.py`: 그룹 상태 영속성 (149줄)
- `statistics_calculator.py`: 통계 계산 (85줄)

**이미지 처리 (3개)**
- `image_manager.py`: 이미지 로딩 조정 (234줄)
- `image_loader.py`: 백그라운드 이미지 로더
- `image_registry.py`: 이미지-위젯 매핑

**설정 및 상태 (2개)**
- `config_manager.py`: 설정 관리 (173줄)
- `window_state_manager.py`: 윈도우 상태 (63줄)

**유틸리티 (4개)**
- `utils.py`: 범용 유틸리티 (218줄)
- `path_utils.py`: 경로 유틸리티 (271줄)
- `delete_manager.py`: 삭제 관리
- `abnormal_detector.py`: 이상치 감지 (76줄)

**NIR 관련 (2개)**
- `nir_app.py`: NIR 모니터링 GUI (428줄)
- `nir_spectrum_monitor.py`: NIR 스펙트럼 분석 (203줄)

**기타 (2개)**
- `collect_filenames.py`: 파일명 수집
- `README.md`: 프로젝트 개요

### 4.2 이번 리팩토링에서 주로 활용될 모듈

리팩토링 시 다음 모듈들과의 상호작용이 핵심이 될 것으로 예상됩니다:

**필수 활용 모듈:**
- `file_operation_manager.py`: 파일 작업 비즈니스 로직 통합
- `statistics_calculator.py`: 통계 계산 로직 활용
- `group_manager.py`: 그룹 생성 로직 활용
- `group_state_manager.py`: 상태 영속성 활용
- `image_manager.py`: 이미지 로딩 조정 활용

**보조 활용 모듈:**
- `config_manager.py`: 설정 관리
- `window_state_manager.py`: 윈도우 상태 관리
- `abnormal_detector.py`: 이상치 감지
- `path_utils.py`: 경로 유틸리티
- `ui_components.py`: UI 컴포넌트 재사용

### 4.3 주로 참고만 할 모듈

다음 모듈들은 이번 리팩토링에서 직접적인 코드 변경은 없으나, 아키텍처 이해를 위해 참고합니다:

- `file_matcher.py`, `file_count_worker.py`, `file_operations.py`
- `image_loader.py`, `image_registry.py`
- `utils.py`, `delete_manager.py`
- `nir_app.py`, `nir_spectrum_monitor.py`

---

## 5. 기능 요구사항 (Functional Requirements)

리팩토링 이후에도 다음 기능들은 **반드시 동일하게 작동**해야 합니다.

### 5.1 파일 감시 및 매칭

**기능:**
- Run 버튼 클릭 시 watchdog를 통한 실시간 파일 감시 시작
- 일반 카메라, NIR 카메라, 복합 카메라(cam1~6) 파일 자동 감지
- 타임스탬프 기반 파일 그룹화 (±10초, ±3초)
- 10초마다 백그라운드 풀스캔 수행 (WSL 환경 대응)

**검증 방법:**
```bash
conda run -n workspace python monitoring_app.py
# 수동 테스트: Run 버튼 클릭 후 설정된 폴더에 이미지 파일 추가
# 예상 동작: 10초 이내 파일이 감지되고 UI에 그룹으로 표시됨
```

### 5.2 실시간 파일 개수 표시

**기능:**
- NIR1, NIR2, 일반1, 일반2, Cam1~6 폴더의 파일 개수 실시간 표시
- 별도 스레드(`FileCountWorker`)에서 10초마다 카운트 업데이트
- UI 렉 없이 백그라운드에서 동작

**검증 방법:**
```bash
conda run -n workspace python monitoring_app.py
# 수동 테스트: Run 실행 후 파일 추가/삭제 시 통계 바의 파일 개수가 10초 이내 업데이트되는지 확인
```

### 5.3 통계 계산

**기능:**
- 통합 모드: 전체 total, with NIR, without NIR, fail 계산
- 분리 모드: 라인1, 라인2 각각의 통계 계산
- 매칭 실패(누락발생) 그룹 카운트

**검증 방법:**
```bash
conda run -n workspace python monitoring_app.py
# 수동 테스트: 다양한 파일 조합 추가 후 통계 바의 숫자가 실제 그룹과 일치하는지 확인
# "이미지 불러오기" 버튼 클릭 후 통계 값 확인
```

### 5.4 이미지 로딩 및 표시

**기능:**
- "이미지 불러오기" 버튼 클릭 시 전체 그룹의 썸네일 이미지 로드
- 비동기 이미지 로딩 (`ImageLoaderWorker`)
- 디스크 캐시 지원 (옵션)
- 이미지 클릭 시 미리보기 다이얼로그 표시

**검증 방법:**
```bash
conda run -n workspace python monitoring_app.py
# 수동 테스트: "이미지 불러오기" 버튼 클릭 후 모든 이미지가 정상 표시되는지 확인
# 이미지 클릭 시 미리보기 팝업이 뜨는지 확인
```

### 5.5 파일 이동/복사 작업

**기능:**
- 선택된 그룹을 시료 폴더로 이동/복사
- NIR 파일 개수 제한 기능 (이동NIR수 설정)
- 데이터 개수 제한 기능 (이동데이터수 설정)
- 탭별 독립 이동 (라인1, 라인2, 통합)
- 체크박스 선택/해제에 따른 필터링
- 이동 로그 기록 (`config_manager`)

**검증 방법:**
```bash
conda run -n workspace python monitoring_app.py
# 수동 테스트:
# 1. 파일 감지 후 체크박스 선택
# 2. 이동NIR수, 이동데이터수 설정
# 3. "이동" 버튼 클릭 후 타겟 폴더에 파일이 정상 이동되었는지 확인
# 4. 로그 파일에 이동 기록이 남았는지 확인
```

### 5.6 경로 자동 설정

**기능:**
- "작업날짜" 입력란에 YYYYMMDD 형식 날짜 입력
- "경로자동" 버튼 클릭 시 모든 설정 경로의 날짜 부분 자동 교체
- 변경 예정 경로 미리보기 및 확인 대화상자
- 존재하지 않는 폴더 자동 생성

**검증 방법:**
```bash
conda run -n workspace python monitoring_app.py
# 수동 테스트:
# 1. "작업날짜"에 새 날짜 입력 (예: 20250201)
# 2. "경로자동" 버튼 클릭
# 3. 확인 대화상자에서 경로 변경 내역 확인
# 4. "예" 클릭 후 설정 대화상자에서 경로가 변경되었는지 확인
```

### 5.7 그룹 상태 영속성

**기능:**
- 앱 종료 시 현재 그룹 상태를 `groups_state.json`에 저장
- 앱 재시작 시 이전 상태 복원 (옵션)
- 해시 기반 변경 감지로 불필요한 저장 방지

**검증 방법:**
```bash
conda run -n workspace python monitoring_app.py
# 수동 테스트:
# 1. 파일 감지 후 앱 종료
# 2. `{app_dir}/groups_state.json` 파일 존재 확인
# 3. 앱 재시작 후 이전 상태가 복원되는지 확인 (구현 여부에 따라)
```

### 5.8 시료 폴더 자동 생성

**기능:**
- Run 버튼 클릭 시 시료 폴더 자동 생성
- `with NIR`, `without NIR` 하위 폴더 자동 생성
- 분리 모드일 경우 시료명1, 시료명2 모두 생성

**검증 방법:**
```bash
conda run -n workspace python monitoring_app.py
# 수동 테스트:
# 1. 시료명 입력 (예: "테스트시료")
# 2. Run 버튼 클릭
# 3. 이동 대상 폴더에 "테스트시료/with NIR", "테스트시료/without NIR" 폴더 생성 확인
```

### 5.9 드래그 선택 기능

**기능:**
- 마우스 드래그로 여러 행 동시 선택
- 체크박스, 버튼 영역 제외 (이미지나 빈 공간만)

**검증 방법:**
```bash
conda run -n workspace python monitoring_app.py
# 수동 테스트:
# 1. 여러 행이 표시된 상태에서 빈 공간을 드래그
# 2. 드래그한 범위의 체크박스가 선택되는지 확인
```

### 5.10 설정 저장 및 불러오기

**기능:**
- 설정 대화상자를 통한 모든 경로 및 옵션 설정
- 라인 모드 전환 (통합/분리)
- camera 하위폴더 옵션
- 디스크 캐시 옵션
- NIR 매칭 시간 차이 설정
- 윈도우 크기/위치 저장 및 복원

**검증 방법:**
```bash
conda run -n workspace python monitoring_app.py
# 수동 테스트:
# 1. 설정 대화상자에서 다양한 옵션 변경
# 2. 앱 재시작 후 설정이 유지되는지 확인
# 3. 윈도우 크기 변경 후 재시작 시 복원되는지 확인
```

---

## 6. 리팩토링 제약사항 (Constraints)

### 6.1 문서 기반 제약

**C1. MODULE_DOCS_SUMMARY.md 참고 의무**
- 모든 리팩토링 작업 전에 `docs/MODULE_DOCS_SUMMARY.md`를 반드시 검토해야 합니다.
- 기존 모듈의 책임 범위를 명확히 파악한 후 작업을 진행해야 합니다.

**C2. 중복 기능 생성 금지**
- 이미 다른 모듈에서 제공하는 기능을 새로 만들지 않습니다.
- 새 책임이 필요해 보이면, 먼저 기존 모듈 중에서 적절한 위치가 있는지 검토합니다.
- 정말 없을 때만 새로운 모듈 후보로 정의합니다.

### 6.2 승인 기반 제약

**C3. monitoring_app 삭제/수정 시 사용자 승인 필요**
- `monitoring_app.py`의 코드를 삭제하거나 대체하는 결정을 내릴 때는 반드시 사용자에게 확인을 요청해야 합니다.
- 요구사항 문서 및 구현 계획서에 "사용자 승인 필요" 또는 "사용자와 상의 필요"로 명시합니다.
- 특히 다음 사항은 반드시 사용자 확인:
  - `DragSelectWidget` 클래스를 `ui_components.py`로 이동할지 여부
  - 특정 메서드를 완전히 삭제할지, 기존 모듈로 대체할지 여부
  - 새로운 모듈을 생성할지 여부

**C4. 과도한 책임 집중 발견 시 계획 중단 및 재협의**
- 리팩토링 계획 수립 중 특정 모듈/클래스에 책임이 과도하게 몰릴 가능성이 발견되면, 그 사실을 "리스크"로 명시합니다.
- 해당 시점에서 계획을 멈추고 사용자와 설계를 재검토해야 합니다.
- 예시: 새로운 "UI Controller" 모듈에 30개 이상의 메서드가 몰리는 경우

### 6.3 기술적 제약

**C5. PySide6 GUI 프레임워크 유지**
- 현재 PySide6 기반 GUI는 유지되며, 이번 리팩토링에서 변경하지 않습니다.

**C6. 기존 의존성 라이브러리 유지**
- watchdog, Pillow, appdirs 등 현재 사용 중인 외부 라이브러리는 그대로 사용합니다.
- 새로운 의존성 추가는 최소화합니다.

**C7. 점진적 리팩토링 원칙**
- 한 번에 모든 것을 변경하지 않고, 단계별로 리팩토링을 진행합니다.
- 각 단계마다 테스트를 수행하여 기능 동일성을 검증합니다.

**C8. 하위 호환성 유지 (초기 단계)**
- 리팩토링 초기에는 기존 코드와 새 코드가 공존할 수 있도록 합니다.
- 단계적으로 레거시 코드를 제거합니다.

### 6.4 조직적 제약

**C9. 검증 환경 고정**
- 모든 테스트 및 검증은 `conda run -n workspace` 환경에서 수행해야 합니다.
- 다른 환경에서의 테스트는 참고용으로만 사용합니다.

**C10. 문서화 동시 진행**
- 새로운 모듈 생성 시, 반드시 `docs/modules/` 하위에 문서를 함께 작성해야 합니다.
- `MODULE_DOCS_SUMMARY.md`도 함께 업데이트해야 합니다.

---

## 7. 비기능 요구사항 (Non-Functional Requirements)

### 7.1 유지보수성

**NFR-1. 모듈 크기 제한**
- 새로 생성되는 모듈은 500줄 이하를 목표로 합니다.
- 500줄을 초과할 경우, 추가 분리 검토가 필요합니다.

**NFR-2. 명확한 모듈 명명**
- 모듈 이름은 기능을 명확히 드러내야 합니다.
- 예: `tab_controller.py`, `file_operation_orchestrator.py`

**NFR-3. 주석 및 독스트링**
- 모든 공개 함수/메서드는 독스트링을 포함해야 합니다.
- 복잡한 로직에는 주석을 추가합니다.

### 7.2 가독성

**NFR-4. 일관된 코드 스타일**
- 기존 프로젝트의 코딩 스타일을 따릅니다.
- PEP 8 가이드라인을 준수합니다.

**NFR-5. 명확한 변수명**
- 축약어 사용을 최소화하고, 의미가 명확한 변수명을 사용합니다.

### 7.3 테스트 가능성

**NFR-6. 의존성 주입 가능**
- 새로운 클래스/함수는 가능한 한 의존성 주입을 통해 외부 의존성을 받도록 설계합니다.
- 테스트 시 Mock 객체 사용이 용이해야 합니다.

**NFR-7. 단위 테스트 작성 가능성**
- 리팩토링 후 각 모듈은 독립적으로 단위 테스트 작성이 가능해야 합니다.

### 7.4 결합도와 응집도

**NFR-8. 낮은 결합도**
- 모듈 간 의존성을 최소화합니다.
- 가능한 한 인터페이스를 통한 통신을 권장합니다.

**NFR-9. 높은 응집도**
- 한 모듈 내의 기능들은 서로 밀접하게 관련되어 있어야 합니다.

### 7.5 성능

**NFR-10. 성능 저하 없음**
- 리팩토링 후 성능이 저하되어서는 안 됩니다.
- 특히 이미지 로딩, 파일 스캔 속도가 유지되어야 합니다.

**NFR-11. 메모리 사용량 유지**
- 리팩토링으로 인한 메모리 사용량 증가가 없어야 합니다.

### 7.6 확장성

**NFR-12. 플러그인 아키텍처 고려**
- 향후 기능 추가 시, 기존 코드 수정 없이 새 모듈 추가만으로 확장 가능한 구조를 지향합니다.

---

## 8. 리스크 및 열린 이슈 (Risks & Open Questions)

### 8.1 현재 파악되는 리스크

**R1. 암묵적 의존성 발견 가능성**
- `monitoring_app.py`의 일부 메서드들은 다른 메서드의 실행 순서에 의존할 수 있습니다.
- 예: `init_ui()` → `restore_window_bounds()` → `update_line_mode_ui()` 순서
- **대응책**: 리팩토링 중 발견되는 암묵적 의존성은 명시적으로 문서화하고, 테스트로 검증합니다.

**R2. 숨겨진 사이드 이펙트**
- 일부 메서드가 전역 상태를 변경하거나, 예상치 못한 부작용을 일으킬 수 있습니다.
- 예: `process_updates()` 메서드가 여러 개의 인스턴스 변수를 변경
- **대응책**: 사이드 이펙트가 있는 메서드를 명확히 식별하고, 가능한 한 순수 함수로 전환합니다.

**R3. UI와 비즈니스 로직의 강한 결합**
- 현재 UI 이벤트 핸들러 내에 비즈니스 로직이 혼재되어 있습니다.
- **대응책**: 비즈니스 로직을 별도 레이어로 분리하되, 초기에는 기존 구조를 유지하면서 점진적으로 분리합니다.

**R4. 테스트 코드 부재**
- 현재 프로젝트에 단위 테스트가 없어, 리팩토링 후 기능 동일성 검증이 어려울 수 있습니다.
- **대응책**: 리팩토링과 동시에 주요 기능에 대한 통합 테스트 또는 수동 테스트 체크리스트를 작성합니다.

**R5. 대규모 파일 처리 시 성능 이슈**
- 수천 개의 파일을 처리할 때 성능 저하가 발생할 수 있습니다.
- **대응책**: 리팩토링 중 성능 병목 지점을 프로파일링하고, 필요시 최적화를 수행합니다.

**R6. 새 모듈 과다 생성 위험**
- 과도하게 많은 모듈로 분리하면 오히려 복잡도가 증가할 수 있습니다.
- **대응책**: 모듈 생성 시 항상 "정말 새 모듈이 필요한가?"를 질문하고, 기존 모듈 확장을 우선 검토합니다.

### 8.2 사용자와 논의가 필요한 지점 (Open Questions)

> **중요**: 아래 항목들은 리팩토링 계획 수립 전에 반드시 사용자와 논의가 필요합니다.

**Q1. DragSelectWidget 처리 방침**
- **질문**: `DragSelectWidget` 클래스를 `monitoring_app.py`에서 `ui_components.py`로 이동할까요?
- **배경**: `DragSelectWidget`는 재사용 가능한 UI 컴포넌트이지만, 현재 `monitoring_app.py`에서만 사용됩니다.
- **옵션**:
  - A. `ui_components.py`로 이동하여 재사용성 확보
  - B. `monitoring_app.py`에 유지 (현재 상태)
  - C. 별도의 `drag_select_widget.py` 모듈로 분리
- **사용자 결정 필요**: 드레그 위젯은 monitoring_app 에서만 사용됨. 기능 분리를 위해서라면 분리가 필요함. 필요치 않다면 안해도 됨. 스스로 판단하여, 책임 분리를 위해서 분리가 필요하다면 분리를 진행 할것

**Q2. 새 모듈 생성 범위**
- **질문**: 리팩토링 시 새로운 모듈을 최대 몇 개까지 생성할 수 있을까요?
- **배경**: 너무 많은 모듈 생성은 복잡도를 증가시킬 수 있습니다.
- **권장**: 5개 이하의 새 모듈 생성을 목표로 하되, 필요시 사용자와 재논의
- **사용자 결정 필요**: 5개 이하의 새 모듈 생성을 목표로 하고 필요사 논의.

**Q3. 탭 관리 로직 분리 방침**
- **질문**: 탭 전환 및 라인 모드 관리 로직을 별도 모듈로 분리할까요?
- **배경**: `monitoring_app.py`의 `update_line_mode_ui()`, 탭별 데이터 분리 로직 등이 복잡합니다.
- **옵션**:
  - A. 새로운 `tab_controller.py` 또는 `line_mode_manager.py` 모듈 생성
  - B. `monitoring_app.py`에 유지
  - C. `ui_builder.py`로 이관
- **사용자 결정 필요**: A. 현재 ui_builder가 500줄을 넘어가기에 따로 모듈을 만들 것.

**Q4. 파일 작업 실행 로직 위치**
- **질문**: `execute_file_operation()` 메서드(약 300줄)의 로직을 어디에 배치할까요?
- **배경**: 현재 `file_operation_manager.py`가 존재하지만, `execute_file_operation()`는 여전히 `monitoring_app.py`에 있습니다.
- **옵션**:
  - A. `file_operation_manager.py`로 이관
  - B. 새로운 `file_operation_orchestrator.py` 모듈 생성
  - C. `monitoring_app.py`에 유지 (UI 이벤트 핸들러로 취급)
- **사용자 결정 필요**: A.

**Q5. 통계 표시 로직 분리**
- **질문**: 통계 계산은 `statistics_calculator.py`에서 수행하지만, UI 업데이트 로직(`_update_stats()`, `_update_stats_separated()`)은 어디에 둘까요?
- **배경**: 현재 통계 계산과 UI 업데이트가 분리되어 있습니다.
- **옵션**:
  - A. `monitoring_app.py`에 유지 (UI 업데이트는 GUI 책임)
  - B. 새로운 `statistics_presenter.py` 모듈 생성 (Presenter 패턴)
  - C. `ui_builder.py`로 이관
- **사용자 결정 필요**: B. 새로운 모듈 생성

**Q6. 이미지 관련 메서드 중복 검토**
- **질문**: `monitoring_app.py`의 `refresh_visible_images()`, `on_image_loaded()` 등의 메서드가 `image_manager.py`와 중복되는 부분이 있는지 검토가 필요합니다.
- **배경**: 이미지 관리 관련 로직이 여러 곳에 분산되어 있을 가능성이 있습니다.
- **조치**: 코드 리뷰를 통해 중복 여부를 확인하고, 필요시 통합합니다.
- **사용자 결정 필요**: 코드 리뷰를 통해 중복 여부를 확인하고, 이에 대한 문서를 작성 후, 처리 여부를 정하는 페이즈와 처리 여부에 따라 수행하는 페이즈를 반드시 넣을 것.

**Q7. 리팩토링 수행 주체**
- **질문**: 이번 리팩토링은 누가 주도하나요?
- **배경**: AI 에이전트가 자동으로 수행할지, 사용자가 직접 수행할지 명확히 해야 합니다.
- **옵션**:
  - A. AI 에이전트가 구현 계획 수립 + 사용자 승인 후 자동 수행
  - B. 사용자가 직접 수행 (이 문서는 가이드로만 사용)
  - C. 혼합 방식 (일부는 AI, 일부는 사용자)
- **사용자 결정 필요**: A.

**Q8. 삭제 후보 메서드 최종 확인**
- **질문**: 다음 메서드들은 삭제 또는 대체 후보로 보이는데, 최종 결정은 어떻게 하나요?
  - `_groups_to_canonical_json()` → `GroupStateManager`로 이미 위임됨
  - `_calc_group_hash()` → `GroupStateManager`로 이미 위임됨
  - `_calc_groups_hash()` → `GroupStateManager`로 이미 위임됨
  - `_maybe_save_groups_json()` → `GroupStateManager`로 이미 위임됨
  - `_maybe_load_groups_json()` → `GroupStateManager`로 이미 위임됨
- **배경**: 이미 다른 모듈로 위임된 wrapper 메서드들입니다.
- **옵션**:
  - A. 즉시 삭제 (직접 `GroupStateManager` 호출)
  - B. 하위 호환성을 위해 유지
  - C. Deprecated 표시 후 단계적 제거
- **사용자 결정 필요**: A. monitoring_app 에서 삭제 전에, 사용자에게 방침을 알릴 것. 삭제는 사용자가 직접 수행함.

**Q9. 책임 집중도 임계값**
- **질문**: 리팩토링 후 특정 모듈의 메서드 개수가 몇 개를 초과하면 "과도한 책임 집중"으로 판단할까요?
- **권장**: 20개 메서드를 임계값으로 설정
- **사용자 결정 필요**: 20개 정도를 임계값으로 설정하나, 필요에 따라서 너무 분리되어 있다고 한다면 20개를 넘어도 무방함.

**Q10. provisional_nirs 로직 처리**
- **질문**: `monitoring_app.py`의 `provisional_nirs` 딕셔너리 관련 로직은 어디에 배치할까요?
- **배경**: NIR 파일의 임시 매칭 상태를 관리하는 로직입니다.
- **옵션**:
  - A. `file_matcher.py`로 이관
  - B. `group_manager.py`로 이관
  - C. `monitoring_app.py`에 유지
- **사용자 결정 필요**: Q10에 대한 답변은: **"C. monitoring_app.py에 유지"가 아니라 "D. 완전 제거"**입니다. provisional_nirs는 안정화 로직의 잔재이며, 현재 시스템에서는 불필요하므로 제거하는 것이 적절합니다.

---

## 9. 다음 단계 (Next Steps)

본 요구사항 문서 검토 후, 다음 단계를 진행합니다:

1. **사용자 검토 및 질문 해결**
   - 섹션 8.2의 모든 Open Questions에 대한 답변 필요
   - 요구사항 문서에 대한 피드백 수렴

2. **상세 구현 계획 수립**
   - 승인된 방향에 따라 `implementation_plan.md` 작성
   - 모듈별 책임 매핑 테이블 작성
   - 리팩토링 단계별 순서도 작성

3. **프로토타입 검증 (선택)**
   - 가장 위험도 높은 부분(예: `execute_file_operation()` 분리)을 먼저 프로토타입으로 테스트
   - 사용자 피드백 수렴

4. **단계별 리팩토링 실행**
   - 구현 계획에 따라 점진적 리팩토링 수행
   - 각 단계마다 테스트 및 검증

5. **문서 업데이트**
   - 새 모듈 문서 작성
   - `MODULE_DOCS_SUMMARY.md` 업데이트
   - 아키텍처 다이어그램 작성

---

## 10. 검증 기준 (Acceptance Criteria)

리팩토링 완료 시 다음 기준을 모두 만족해야 합니다:

1. ✅ **기능 동일성**: 섹션 5의 모든 기능 요구사항이 정상 작동
2. ✅ **코드 품질**: 섹션 7의 모든 비기능 요구사항 준수
3. ✅ **제약사항 준수**: 섹션 6의 모든 제약사항 충족
4. ✅ **문서 완성**: 모든 새 모듈에 대한 문서 작성 완료
5. ✅ **테스트 통과**: `conda run -n workspace pytest` 실행 시 모든 테스트 통과 (테스트 작성 시)
6. ✅ **성능 유지**: 파일 스캔, 이미지 로딩 속도가 리팩토링 전과 동일하거나 개선
7. ✅ **사용자 승인**: 모든 중요 결정 사항에 대한 사용자 승인 완료

---

## 부록 A: 용어 정리

- **통합 모드**: 라인1과 라인2를 구분하지 않고 하나의 시료로 처리하는 모드
- **분리 모드**: 라인1과 라인2를 각각 독립적인 시료로 처리하는 모드
- **그룹(Group)**: 타임스탬프 기반으로 매칭된 파일들의 집합
- **NIR**: Near-Infrared, 근적외선 카메라로 촬영된 이미지
- **일반 카메라**: NIR이 아닌 일반 RGB 카메라
- **복합 카메라**: cam1~cam6, 여러 각도에서 촬영하는 카메라 시스템
- **watchdog**: 파일 시스템 변경 감지 라이브러리
- **provisional_nirs**: 아직 최종 매칭이 확정되지 않은 임시 NIR 파일

---

## 부록 B: 참고 문서

- `docs/MODULE_DOCS_SUMMARY.md`: 전체 모듈 요약
- `docs/modules/`: 각 모듈별 상세 문서
- `monitoring_app.py`: 현재 리팩토링 대상 파일

---

**문서 버전**: 1.0  
**작성일**: 2025-11-29  
**검토 필요**: 예 (섹션 8.2의 모든 질문에 대한 답변 필요)
