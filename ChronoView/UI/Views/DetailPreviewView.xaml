<UserControl x:Class="ChronoView.UI.Views.DetailPreviewView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ChronoView.UI.Views"
             xmlns:converters="clr-namespace:ChronoView"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <UserControl.Resources>
        <converters:BoolToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>
    
    <Grid Background="#AA000000" Visibility="{Binding IsVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
        <Border Background="White" Margin="50" CornerRadius="8" BorderBrush="#d0d0d0" BorderThickness="1">
            <Border.Effect>
                <DropShadowEffect BlurRadius="20" ShadowDepth="0" Opacity="0.5"/>
            </Border.Effect>
            
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/> <!-- Header -->
                    <RowDefinition Height="*"/>    <!-- Content -->
                    <RowDefinition Height="Auto"/> <!-- Status Bar -->
                </Grid.RowDefinitions>

                <!-- Header -->
                <Border Grid.Row="0" Background="#f8f9fa" BorderBrush="#e9ecef" BorderThickness="0,0,0,1" CornerRadius="8,8,0,0" Padding="16,12">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding SelectedGroup.GroupId}" FontWeight="Bold" FontSize="18" VerticalAlignment="Center"/>
                            <TextBlock Text="{Binding SelectedGroup.CreatedAt, StringFormat='{} ({0:yyyy-MM-dd HH:mm:ss})'}" Foreground="#6c757d" FontSize="14" VerticalAlignment="Center" Margin="10,0,0,0"/>
                        </StackPanel>
                        <Button Grid.Column="1" Command="{Binding CloseCommand}" Content="✕" Width="30" Height="30" Background="Transparent" BorderThickness="0" FontSize="16" Cursor="Hand"/>
                    </Grid>
                </Border>

                <!-- Content -->
                <Grid Grid.Row="1" Margin="20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/> <!-- Main Images -->
                        <ColumnDefinition Width="1*"/> <!-- Camera List -->
                    </Grid.ColumnDefinitions>

                    <!-- Main & NIR Images -->
                    <Grid Grid.Column="0" Margin="0,0,20,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <!-- Main Image -->
                        <Border Grid.Row="0" BorderBrush="#dee2e6" BorderThickness="1" Margin="0,0,0,10" Background="#333">
                            <Grid>
                                <Image Source="{Binding MainImagePath}" Stretch="Uniform"/>
                                <TextBlock Text="Main Image" Foreground="White" Background="#80000000" HorizontalAlignment="Left" VerticalAlignment="Top" Padding="8,4" Margin="4"/>
                            </Grid>
                        </Border>

                        <!-- NIR Image -->
                        <Border Grid.Row="1" BorderBrush="#dee2e6" BorderThickness="1" Margin="0,10,0,0" Background="#333">
                            <Grid>
                                <Image Source="{Binding NirImagePath}" Stretch="Uniform"/>
                                <TextBlock Text="NIR Spectrum" Foreground="White" Background="#80000000" HorizontalAlignment="Left" VerticalAlignment="Top" Padding="8,4" Margin="4"/>
                            </Grid>
                        </Border>
                    </Grid>

                    <!-- Side Cameras -->
                    <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto">
                        <ItemsControl ItemsSource="{Binding CompositeCameras}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Margin="0,0,0,15" BorderBrush="#dee2e6" BorderThickness="1" Background="#f8f9fa">
                                        <StackPanel>
                                            <Border Height="150" Background="#333">
                                                <Image Source="{Binding ImagePath}" Stretch="Uniform"/>
                                            </Border>
                                            <TextBlock Text="{Binding Name}" Padding="8" FontWeight="SemiBold" TextAlignment="Center"/>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>

                <!-- Status Footer -->
                <Border Grid.Row="2" Background="#f8f9fa" BorderBrush="#e9ecef" BorderThickness="0,1,0,0" CornerRadius="0,0,8,8" Padding="16,12">
                    <StackPanel Orientation="Horizontal">
                        <Border Background="{Binding StatusColor}" CornerRadius="4" Padding="8,4" Margin="0,0,10,0">
                            <TextBlock Text="{Binding StatusMessage}" Foreground="White" FontWeight="Bold"/>
                        </Border>
                        <TextBlock Text="{Binding SelectedGroup.NormalLabel}" VerticalAlignment="Center" Foreground="#495057"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>
    </Grid>
</UserControl>
