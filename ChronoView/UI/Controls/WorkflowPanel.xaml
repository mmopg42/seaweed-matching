<UserControl x:Class="ChronoView.UI.Controls.WorkflowPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:res="clr-namespace:ChronoView.Resources"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="300">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel>
            <!-- Camera Status Section -->
            <Expander Header="{x:Static res:Strings.Panel_CameraStatus}" IsExpanded="True" 
                      Background="{StaticResource HoverBrush}" Margin="0,0,0,1">
                <StackPanel Margin="10">
                    <TextBlock Text="{x:Static res:Strings.Panel_SystemStatus}" FontWeight="Bold" Margin="0,0,0,8"/>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                        <TextBlock Text="{x:Static res:Strings.Status_Normal}" Width="80"/>
                        <Ellipse Width="10" Height="10" Fill="{Binding Control.GeneralCameraForeground}" Margin="0,0,5,0"/>
                        <TextBlock Text="{Binding Control.GeneralCameraStatus}"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                        <TextBlock Text="{x:Static res:Strings.Status_NIR}" Width="80"/>
                        <Ellipse Width="10" Height="10" Fill="{Binding Control.NirCameraForeground}" Margin="0,0,5,0"/>
                        <TextBlock Text="{Binding Control.NirCameraStatus}"/>
                    </StackPanel>
                    
                    <!-- NIR Filtering Toggle -->
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                        <TextBlock Text="{x:Static res:Strings.Status_NirFiltering}" Width="80"/>
                        <Ellipse Width="10" Height="10" Fill="{Binding Control.Nir2FilteringForeground}" Margin="0,0,5,0"/>
                        <TextBlock Text="{Binding Control.Nir2FilteringStatus}" Width="80"/>
                        <Button Content="{Binding Control.Nir2FilteringButtonText}" Width="40" Height="24" 
                                Command="{Binding Control.ToggleNir2FilteringCommand}"
                                Background="{Binding Control.Nir2FilteringButtonBackground}"
                                Foreground="White" Margin="4,0"
                                FontWeight="Bold"/>
                    </StackPanel>
                </StackPanel>
            </Expander>
            
            <!-- Sample Move Settings Section -->
            <Expander Header="{x:Static res:Strings.Panel_SampleMoveSettings}" IsExpanded="True" 
                      Background="{StaticResource HoverBrush}" Margin="0,0,0,1">
                <StackPanel Margin="10">
                    <TextBlock Text="{x:Static res:Strings.Label_MoveSettings}" FontWeight="Bold" Margin="0,0,0,8"/>
                    <Label Content="{x:Static res:Strings.Sample_Name}" FontSize="10"/>
                    <TextBox Text="{Binding SampleName, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8"/>
                    <Label Content="{x:Static res:Strings.Sample_MoveNIR}" FontSize="10"/>
                    <TextBox Text="{Binding MoveNir, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8"/>
                    <Label Content="{x:Static res:Strings.Sample_MoveAllData}" FontSize="10"/>
                    <TextBox Text="{Binding MoveAllData, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>
            </Expander>
            
            <!-- Data Status Section -->
            <Expander Header="{x:Static res:Strings.Panel_DataStatus}" IsExpanded="True" 
                      Background="{StaticResource HoverBrush}" Margin="0,0,0,1">
                <StackPanel Margin="10">
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                        <TextBlock Text="{x:Static res:Strings.Status_TotalGroups}" Width="100"/>
                        <TextBlock Text="{Binding Dashboard.TotalGroups}" FontWeight="Bold"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                        <TextBlock Text="{x:Static res:Strings.Label_WithNIR}" Width="100"/>
                        <TextBlock Text="{Binding Dashboard.WithNirCount}" FontWeight="Bold"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                        <TextBlock Text="{x:Static res:Strings.Label_WithoutNIR}" Width="100"/>
                        <TextBlock Text="{Binding Dashboard.WithoutNirCount}" FontWeight="Bold"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                        <TextBlock Text="{x:Static res:Strings.Label_Abnormal}" Width="100"/>
                        <TextBlock Text="{Binding Dashboard.AbnormalCount}" FontWeight="Bold" Foreground="Orange"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{x:Static res:Strings.Status_Failures}" Width="100"/>
                        <TextBlock Text="{Binding Dashboard.FailedCount}" FontWeight="Bold" Foreground="Red"/>
                    </StackPanel>
                </StackPanel>
            </Expander>
        </StackPanel>
    </ScrollViewer>
</UserControl>
