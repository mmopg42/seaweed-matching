---
phase: 01-infra
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [skills_scripts/ui_automation/ui_automation.csproj, skills_scripts/ui_automation/Program.cs]
autonomous: true
---

<objective>
FlaUI UIA3 프로젝트 초기화 및 빌드 가능 상태로 만들기

Purpose: FlaUI 5.x 기반 C# CLI 도구의 프로젝트 구조를 생성하고 빌드할 수 있게 함
Output: skills_scripts/ui_automation/ 디렉토리에 .csproj, Program.cs, UiAutomation.cs 생성
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/STACK.md

# ChronoView 프로젝트 구조 참고
@ChronoView/ChronoView.csproj
</context>

<tasks>

<task type="auto">
  <name>Task 1: 프로젝트 디렉토리 및 .csproj 생성</name>
  <files>skills_scripts/ui_automation/ui_automation.csproj</files>
  <action>
1. skills_scripts/ui_automation/ 디렉토리 생성
2. ui_automation.csproj 파일 생성:
   - TargetFramework: net10.0-windows
   - OutputType: Exe
   - ImplicitUsings: enable
   - Nullable: enable
   - PackageReference Include="FlaUI.UIA3" Version="5.0.0"
   - PackageReference Include="System.CommandLine" Version="2.0.0-beta4.22272.1"
3. .gitignore 추가 (bin/, obj/)
  </action>
  <verify>dotnet restore 실행 시 패키지 복원 성공</verify>
  <done>프로젝트 파일 생성 및 NuGet 패키지 복원 가능</done>
</task>

<task type="auto">
  <name>Task 2: Program.cs 진입점 생성</name>
  <files>skills_scripts/ui_automation/Program.cs</files>
  <action>
Program.cs 생성:
- using System.CommandLine
- RootCommand "Windows UI Automation - FlaUI 5.x 기반"
- 기본 명령: list (모든 윈도우 나열), find (윈도우 찾기)
- 간단한 테스트를 위한 Hello World 수준 구조
  </action>
  <verify>dotnet build 실행 시 컴파일 성공</verify>
  <done>Program.cs 생성 및 빌드 성공</done>
</task>

<task type="auto">
  <name>Task 3: 기본 UiAutomation.cs 클래스 생성</name>
  <files>skills_scripts/ui_automation/UiAutomation.cs</files>
  <action>
UiAutomation.cs 생성:
- using FlaUI.Core.AutomationElements, FlaUI.UIA3, FlaUI.Core.Conditions
- UIA3Automation 래퍼 필드
- FindWindowByProcess(string processName) 메서드
- FindWindowByTitle(string title) 메서드
- ListElements(AutomationElement) 메서드 (디버깅용)
- IDisposable 패턴 구현
  </action>
  <verify>dotnet build 실행 시 컴파일 성공</verify>
  <done>UiAutomation.cs 생성 및 빌드 성공</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] dotnet restore succeeds
- [ ] dotnet build succeeds without errors
- [ ] Can run `dotnet run -- list` to list windows
</verification>

<success_criteria>
- 프로젝트가 빌드 가능한 상태
- FlaUI.UIA3 5.0.0 패키지 참조됨
- 기본 UIAutomation 클래스 구현됨
- CLI 도구가 실행 가능함
</success_criteria>

<output>
After completion, create `.planning/phases/01-infra/01-01-SUMMARY.md`
</output>
