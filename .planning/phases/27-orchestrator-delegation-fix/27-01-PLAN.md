---
phase: 27-orchestrator-delegation-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/agents/test-orchestrator.md
autonomous: true
must_haves:
  truths:
    - test-orchestrator.md contains explicit "NEVER use Bash for execution" section
    - "Delegation Issues" section added to reporting format
    - Concrete examples of forbidden patterns documented
    - Retry and error handling guidance for delegation failures present
  artifacts:
    - path: .claude/agents/test-orchestrator.md
      contains: "NEVER use Bash"
      contains: "ABSOLUTELY FORBIDDEN"
      contains: "Delegation Issues"
      min_lines: 400  # Will grow from current 352 lines
  key_links:
    - from: "test-orchestrator delegation section"
      to: "test-executor agent"
      via: "Task tool with clear operation specifications"
    - from: "test-orchestrator delegation section"
      to: "log-analyst agent"
      via: "Task tool with log location and analysis focus"
---

<objective>
Strengthen test-orchestrator delegation pattern with explicit Bash tool prohibitions.

Purpose: Prevent test-orchestrator from bypassing sub-agents (test-executor, log-analyst) by adding strong, explicit prohibitions against direct Bash tool usage for execution tasks.
Output: Updated test-orchestrator.md with "NEVER use Bash for execution" section and enhanced reporting format with "Delegation Issues".
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/27-orchestrator-delegation-fix/27-CONTEXT.md
@.planning/phases/27-orchestrator-delegation-fix/27-RESEARCH.md
@.claude/agents/test-orchestrator.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add explicit Bash tool prohibitions</name>
  <files>.claude/agents/test-orchestrator.md</files>
  <action>
    Add a new section "## NEVER Use Bash Tool for Execution" after line 149 (after existing "## Delegation Pattern" section). This section must include:

    1. **ABSOLUTELY FORBIDDEN subsection** with concrete "What NOT to do" examples:
       - "Do NOT build directly" - showing wrong pattern like "Bash: dotnet build..."
       - "Do NOT run application directly" - showing wrong pattern like "Bash: ui_automation.exe app launch"
       - "Do NOT execute UI automation directly" - showing wrong pattern like "Bash: ui_automation.exe toolbar start"
       - "Do NOT generate test data directly" - showing wrong pattern like "Bash: python data_simulator.py..."
       - "Do NOT analyze logs directly" - showing wrong pattern for Grep/Read log analysis

    2. **What TO do instead subsection** showing correct delegation patterns:
       - For execution: "test-executor, build the solution and launch the application" with Task tool usage
       - For log analysis: "log-analyst, analyze the logs at [path]" with Task tool usage

    3. **Allowed Tool Usage subsection** explicitly stating:
       - READ TOOLS (for scenario planning): Read, Grep, Glob are OK for codebase exploration
       - FORBIDDEN: Bash for ANY execution, log analysis, or test data generation

    4. **If Delegation Fails subsection** with explicit rule:
       - "NEVER fall back to direct execution"
       - Report failure in "Delegation Issues" section
       - Retry once before reporting failure
       - Never attempt direct Bash execution as fallback

    Use clear visual markers: ❌ for forbidden, ✅ for allowed.
  </action>
  <verify>
    grep -n "NEVER.*Bash.*Execution\|ABSOLUTELY FORBIDDEN" .claude/agents/test-orchestrator.md
    grep -n "Do NOT.*build\|Do NOT.*run\|Do NOT.*execute" .claude/agents/test-orchestrator.md
    grep -n "FORBIDDEN.*Bash" .claude/agents/test-orchestrator.md
  </verify>
  <done>
    test-orchestrator.md contains explicit "NEVER use Bash for Execution" section with concrete examples of forbidden patterns, allowed alternatives, and delegation failure handling.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Delegation Issues to reporting format</name>
  <files>.claude/agents/test-orchestrator.md</files>
  <action>
    Modify the "## Reporting Format" section (starting at line 150) to add a new "### Delegation Issues" subsection.

    Insert this section after "### Summary" (after line ~169) and before "### TIER 1":

    ```markdown
    ### Delegation Issues

    **Status:** NONE / DETECTED

    If any delegation failures occurred:
    - test-executor: [success / failed / retried]
    - log-analyst: [success / failed / retried]

    **If delegation failed:**
    - Which sub-agent: [test-executor or log-analyst]
    - Error: [what went wrong - error message or description]
    - Retry attempt: [result if retried - e.g., "retry succeeded" or "retry also failed"]
    - Impact: [how this affects the test results - e.g., "TIER 1 execution incomplete", "log analysis skipped"]

    ```
    This section should appear early in the report (right after Summary) so delegation problems are immediately visible to the user.
  </action>
  <verify>
    grep -n "Delegation Issues\|test-executor.*success.*failed.*retried" .claude/agents/test-orchestrator.md
  </verify>
  <done>
    Reporting format includes "Delegation Issues" section immediately after Summary, with status tracking for both sub-agents and structured failure reporting format.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update Critical Reminders section</name>
  <files>.claude/agents/test-orchestrator.md</files>
  <action>
    Strengthen the "## Critical Reminders" section (lines 328-336) to explicitly reference the new "NEVER Use Bash Tool for Execution" section.

    Replace or augment the current reminders to add:
    - "**ABSOLUTE PROHIBITION:** See 'NEVER Use Bash Tool for Execution' section above - any Bash tool usage for execution is a critical failure"
    - "**Delegation failures MUST be reported** - never fall back to direct execution"

    Keep the existing structure but add cross-reference to the new explicit prohibition section.
  </action>
  <verify>
    grep -n "ABSOLUTE PROHIBITION\|NEVER Use Bash Tool" .claude/agents/test-orchestrator.md | tail -5
  </verify>
  <done>
    Critical Reminders section now explicitly references the "NEVER Use Bash Tool for Execution" section and reiterates the absolute prohibition with cross-reference.
  </done>
</task>

</tasks>

<verification>
After all tasks complete, verify:

1. All three grep patterns from Task 1 produce output (forbidden patterns documented)
2. "Delegation Issues" grep pattern from Task 2 produces output (reporting section added)
3. "ABSOLUTE PROHIBITION" grep pattern from Task 3 produces output (reminders strengthened)
4. File line count increased from current 352 to ~400+ lines

Verification command:
```bash
echo "=== Checking Bash prohibitions ===" && \
grep -c "NEVER.*Bash\|ABSOLUTELY FORBIDDEN" .claude/agents/test-orchestrator.md && \
echo "=== Checking Delegation Issues section ===" && \
grep -c "Delegation Issues" .claude/agents/test-orchestrator.md && \
echo "=== Checking file size ===" && \
wc -l .claude/agents/test-orchestrator.md
```
</verification>

<success_criteria>
- test-orchestrator.md contains explicit "NEVER Use Bash Tool for Execution" section with 5+ forbidden examples
- "Delegation Issues" section added to reporting format with structured failure reporting
- Critical Reminders section cross-references the prohibition section
- File is >400 lines (increased from 352)
- All grep patterns produce expected output
</success_criteria>

<output>
After completion, create `.planning/phases/27-orchestrator-delegation-fix/27-01-SUMMARY.md`
</output>
