---
phase: 30-executor-skill-translation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [.claude/agents/test-executor.md]
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Executor can parse skill names from orchestrator prompts"
    - "Executor validates skill names against registry before execution"
    - "Unknown skill errors include helpful suggestions"
    - "Retry logic checks skill's retryable flag"
    - "Error messages include skill context"
  artifacts:
    - path: ".claude/agents/test-executor.md"
      provides: "Executor agent with skill translation documentation"
      contains: "Skill Translation section"
    - path: ".claude/agents/test-executor-skills.md"
      provides: "Skill registry for validation"
      contains: "92 skill definitions"
  key_links:
    - from: ".claude/agents/test-executor.md"
      to: ".claude/agents/test-executor-skills.md"
      via: "Skill registry reference"
      pattern: "test-executor-skills.md"
---

<objective>
Add skill-to-CLI translation documentation to test-executor.md, enabling the executor agent to parse skill names from orchestrator prompts, validate against the registry, and translate to CLI commands.

Purpose: This completes the executor's responsibility in the skill encapsulation architecture - the orchestrator expresses intent (skill names), and the executor translates that to implementation (CLI commands).

Output: Updated test-executor.md with skill translation patterns, validation, and error handling guidance.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/30-executor-skill-translation/30-RESEARCH.md

@.claude/agents/test-executor.md
@.claude/agents/test-executor-skills.md
@.claude/agents/test-orchestrator.md

@.planning/phases/29-orchestrator-skill-integration/29-01-SUMMARY.md
@.planning/phases/28-skill-registry-definition/28-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Skill Translation Section to test-executor.md</name>
  <files>.claude/agents/test-executor.md</files>
  <action>
Add a new section "## Skill Translation" after "## Understanding TIERs" section (around line 36). This section explains how the executor parses skill names from orchestrator prompts and translates them to CLI commands.

**Content to add:**

1. **Section heading and overview:**
   - Explain that test-orchestrator sends skill names in prompts
   - Show the prompt format: "Execute skill: SKILL_NAME [with args: {...}]"
   - Reference test-executor-skills.md as the registry

2. **Prompt Parsing Pattern:**
   - Show regex pattern for extracting skill name and args
   - Example: `@"Execute\s+skill:\s*(?<skill>[A-Z_][A-Z0-9_]*)(?:\s+with\s+args:\s*(?<args>\{.*?\}))?"`
   - Parse examples:
     - "Execute skill: APP_LAUNCH" -> skillName = "APP_LAUNCH", args = ""
     - "Execute skill: FILE_OPS_MOVE_ROWS with args: {"rows": [0,1,2]}" -> skillName = "FILE_OPS_MOVE_ROWS", args = '{"rows": [0,1,2]}'

3. **Skill Validation:**
   - Check skill name exists in test-executor-skills.md registry
   - If not found: find similar skills using Levenshtein distance logic
   - Show example error response with suggestions

4. **CLI Construction:**
   - Read CLI template from registry (e.g., "app launch [--json]")
   - Substitute args into CLI command
   - Always append --json if skill supports it
   - Execute via Bash tool

**Do NOT modify:** Any existing sections except "## Timeout and Retry Guidelines" which needs retryable flag check added in Task 3.
</action>
  <verify>grep -n "## Skill Translation" .claude/agents/test-executor.md returns the line number of the new section</verify>
  <done>Skill Translation section exists with prompt parsing pattern, validation, and CLI construction examples</done>
</task>

<task type="auto">
  <name>Task 2: Add Unknown Skill Error Handling</name>
  <files>.claude/agents/test-executor.md</files>
  <action>
In the Skill Translation section, add a subsection "### Unknown Skill Error Handling" that shows how to report errors for invalid skill names with helpful suggestions.

**Content to add:**

1. **Error response format:**
```json
{
  "error": "Unknown skill: APP_LAUNCHC",
  "errorCode": 4,
  "suggestions": ["APP_LAUNCH", "APP_STOP", "APP_RESTART"],
  "validSkills": ["APP_LAUNCH", "APP_STOP", "APP_RESTART", "APP_STATUS"],
  "category": "APP"
}
```

2. **Suggestion logic:**
   - Find top 3 skills with lowest Levenshtein distance
   - Also show all skills in same category (inferred from skill name prefix)
   - Provide guidance to check test-executor-skills.md

3. **Include in Error Handling section:**
   - Update "## Error Handling (Streamlined)" section to reference skill errors
   - Add example of unknown skill error report

4. **Example message:**
```
Unknown skill: FILE_OPS_MOVE_ROWS
Did you mean:
  - FILE_OPS_MOVE_ROWS (move by row indices)
  - FILE_OPS_MOVE_GROUP_IDS (move by GroupIds)
  - FILE_OPS_MOVE_PREFIX (move by prefix)

See test-executor-skills.md for complete skill registry.
```
</action>
  <verify>grep -n "Unknown skill" .claude/agents/test-executor.md returns results in both Skill Translation and Error Handling sections</verify>
  <done>Unknown skill errors documented with suggestion format and examples</done>
</task>

<task type="auto">
  <name>Task 3: Update Retry Logic for retryable Flag</name>
  <files>.claude/agents/test-executor.md</files>
  <action>
Update "## Timeout and Retry Guidelines (REDUCED)" section to include retryable flag checking. Add a note that the executor should check the skill's retryable flag from the registry before retrying.

**Content to add:**

1. **Before the retry limits table, add:**
```
**Skill Retryable Flag:**
Some skills are marked as non-retryable in the registry (e.g., delete operations).
Before retrying, check the skill's retryable status from test-executor-skills.md.
- If retryable=false: Do NOT retry on failure
- If retryable=true or unspecified: Follow retry limits below
```

2. **Add retryable column to retry limits table:**
```
| Operation Type | Max Retries | Total Attempts | Respects retryable |
|----------------|-------------|----------------|-------------------|
| App Launch | 1 | 2 | yes |
| App Stop | 0 | 1 | yes |
| UI Automation | 1 | 2 | yes |
| Delete Operations | 0 | 1 | ALWAYS (non-retryable) |
| Connectivity Check | 0 | 1 | yes |
| Data Generation | 1 | 2 | yes |
```

3. **Update "When to STOP immediately" list:**
   - Add "Skill marked as retryable=false in registry"
```
- Exit code 127 (command not found) -> Don't retry
- Exit code 2 (not found) -> Skip this check
- Same error twice -> Report and continue
- Timeout after 1 retry -> Report and continue
- Skill marked as non-retryable -> Do not retry
```
</action>
  <verify>grep -n "retryable" .claude/agents/test-executor.md returns at least 3 results (Skill Translation section, retry table, and stop conditions)</verify>
  <done>Retry logic includes retryable flag checking in documentation and tables</done>
</task>

<task type="auto">
  <name>Task 4: Add Skill Context to Error Messages</name>
  <files>.claude/agents/test-executor.md</files>
  <action>
Update error handling sections to include skill context in error messages. This ensures that when errors occur, the orchestrator knows which skill failed.

**Content to add:**

1. **In Skill Translation section, add "### Error Context" subsection:**
```
**Error Response Format:**
When a skill execution fails, always include:
- skill: The skill name that failed
- cli: The CLI command that was executed
- exitCode: The exit code from CLI
- error: Human-readable error message
- suggestion: Actionable suggestion for common failures

Example error response:
{
  "skill": "TOOLBAR_START",
  "cli": "ui_automation.exe toolbar start",
  "exitCode": 3,
  "error": "Timeout waiting for Start button",
  "suggestion": "Check if MainWindow is active. Try 'WINDOWS_MAIN' first.",
  "retryable": true
}
```

2. **Update "## Error Handling (Streamlined)" section:**
   - Add bullet: "Include skill name and CLI command in error reports"
   - Add example showing skill-aware error message
```
If execution fails:
1. Note the skill name, CLI command, and exit code
2. Include skill context in error report
3. Check if app is still running (once only)
...
```
</action>
  <verify>grep -n "\"skill\":" .claude/agents/test-executor.md returns results showing error context examples</verify>
  <done>Error messages include skill context in all relevant sections</done>
</task>

<task type="auto">
  <name>Task 5: Add Complete Translation Workflow Example</name>
  <files>.claude/agents/test-executor.md</files>
  <action>
Add a complete end-to-end example showing the translation workflow from orchestrator prompt to CLI execution. This serves as a reference for the executor agent's reasoning flow.

**Content to add:**

1. **Add "## Skill Translation Workflow" section after Skill Translation section:**
```
**Complete Workflow Example:**

Orchestrator sends:
"Execute skill: FILE_OPS_MOVE_ROWS with args: {"rows": [0, 1, 2]}"

Executor reasoning:
1. Parse prompt: skillName = "FILE_OPS_MOVE_ROWS", args = {"rows": [0,1,2]}
2. Validate: Check test-executor-skills.md -> FILE_OPS_MOVE_ROWS exists (PASS)
3. Lookup CLI template: "file-ops move rows --rows <indices>"
4. Substitute args: --rows 0,1,2
5. Add --json flag: "file-ops move rows --rows 0,1,2 --json"
6. Execute: ui_automation.exe file-ops move rows --rows 0,1,2 --json
7. Parse response and return to orchestrator

**Success Response:**
{
  "skill": "FILE_OPS_MOVE_ROWS",
  "success": true,
  "data": {
    "selected": 3,
    "moved": 3
  }
}

**Error Response (unknown skill):**
{
  "skill": "FILE_OPS_MOVE_ROWS_INVALID",
  "error": "Unknown skill: FILE_OPS_MOVE_ROWS_INVALID",
  "errorCode": 4,
  "suggestions": ["FILE_OPS_MOVE_ROWS", "FILE_OPS_MOVE_GROUP_IDS", "FILE_OPS_MOVE_PREFIX"],
  "retryable": false
}
```

2. **Add registry reference link:**
```
**Skill Registry:** See test-executor-skills.md for all 92 available skills.
```
</action>
  <verify>grep -n "## Skill Translation Workflow" .claude/agents/test-executor.md returns the workflow section with complete example</verify>
  <done>Complete workflow example exists showing translation from prompt to CLI execution</done>
</task>

</tasks>

<verification>
After completing all tasks, verify:

1. **Section Structure:**
   - "## Skill Translation" section exists with subsections for parsing, validation, CLI construction, error handling
   - "## Skill Translation Workflow" section with complete example

2. **Content Coverage:**
   - Prompt parsing regex pattern documented
   - Skill validation against registry explained
   - Unknown skill error handling with suggestions
   - Retryable flag checking in retry guidelines
   - Skill context in error messages
   - Complete workflow example

3. **Cross-references:**
   - test-executor-skills.md referenced multiple times
   - Orchestrator prompt format aligned with Phase 29

4. **No conflicts:**
   - Existing CLI command reference not removed (executor still needs to know CLI patterns)
   - Retry limits table enhanced, not replaced
   - Error handling section augmented with skill context
</verification>

<success_criteria>
1. test-executor.md includes skill-to-CLI translation patterns (EXEC-01)
2. Skill validation against registry documented (EXEC-02)
3. Unknown skill error handling with suggestions documented (EXEC-03)
4. Retry logic respects retryable flag (EXEC-04)
5. Error messages include skill context (EXEC-05)
6. All requirements EXEC-01 through EXEC-05 satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/30-executor-skill-translation/30-01-SUMMARY.md`
</output>
