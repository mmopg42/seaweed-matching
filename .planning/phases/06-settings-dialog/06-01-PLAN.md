---
phase: 06-settings-dialog
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [skills_scripts/ui_automation/ChronoSettingsController.cs, skills_scripts/ui_automation/Program.cs]
autonomous: true
---

<objective>
Implement SettingsDialog automation foundation: dialog opening via Settings button, structure inspection, and ChronoSettingsController class following the ChronoToolbarController pattern.

Purpose: Enable programmatic access to ChronoView's configuration interface for automated testing and setup. This plan establishes the SettingsDialog lifecycle management.

Output: ChronoSettingsController class with dialog open/close/inspect methods, CLI settings commands for inspection and dialog control.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior phase decisions relevant:
@.planning/phases/02-window-detection/02-02-SUMMARY.md  # SettingsDialog detection established (FindSettingsDialog)
@.planning/phases/03-toolbar-control/03-04-SUMMARY.md  # Toolbar button clicking pattern (InvokePattern)

# Key existing patterns:
- ChronoWindowFinder.FindSettingsDialog() returns Window by bilingual title search ("Settings"/"설정")
- Toolbar button click uses button.Patterns.Invoke.Pattern.Invoke()
- ChronoToolbarController pattern: injection constructor + parameterless constructor + IDisposable
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ChronoSettingsController class</name>
  <files>skills_scripts/ui_automation/ChronoSettingsController.cs</files>
  <action>Create new controller class following ChronoToolbarController pattern:
- Constructor: ChronoSettingsController(UIA3Automation automation) for injection, parameterless creating own automation
- Implements IDisposable for automation cleanup
- FindMainWindow() delegates to ChronoWindowFinder
- FindSettingsDialog() delegates to ChronoWindowFinder
- OpenSettingsDialog(Window? mainWindow): Finds Settings toolbar button (Name contains "설정" or "Settings"), clicks using InvokePattern, waits up to 5000ms for dialog to appear using WaitForWindow()
- CloseSettingsDialog(): Clicks Cancel button (Name contains "취소" or "Cancel"), waits for dialog to close
- IsSettingsDialogOpen(): Uses ChronoWindowFinder.IsWindowOpen("Settings") and "설정" fallback
- InspectSettingsDialog(): Lists element tree at depth=2 using ListElements() for structure analysis

Use existing patterns from ChronoToolbarController for button clicking and ChronoWindowFinder for window operations.</action>
  <verify>dotnet build skills_scripts/ui_automation/ui_automation.csproj succeeds with no errors</verify>
  <done>ChronoSettingsController class compiles, follows ChronoToolbarController pattern, has dialog open/close/inspect methods</done>
</task>

<task type="auto">
  <name>Task 2: Add CLI settings commands</name>
  <files>skills_scripts/ui_automation/Program.cs</files>
  <action>Add "settings" command with subcommands:
- "settings open": Opens SettingsDialog via Settings button click
- "settings close": Closes SettingsDialog via Cancel button
- "settings inspect": Inspects SettingsDialog element structure (depth=2)
- "settings status": Reports whether SettingsDialog is currently open

Add using alias: Settings = SkillsScripts.UiAutomation.ChronoSettingsController

Follow toolbar command pattern established in Phase 03 for consistent CLI structure.</action>
  <verify>dotnet build succeeds, help output shows settings subcommands</verify>
  <done>CLI has settings command with open/close/inspect/status subcommands following toolbar pattern</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] dotnet build skills_scripts/ui_automation/ui_automation.csproj succeeds without errors
- [ ] ChronoSettingsController follows ChronoToolbarController pattern consistently
- [ ] CLI settings commands are registered and functional
- [ ] All methods use bilingual text search (English first, Korean fallback)
</verification>

<success_criteria>

- ChronoSettingsController class created with dialog lifecycle methods
- SettingsDialog can be opened programmatically via Settings button click
- SettingsDialog can be closed via Cancel button click
- Element structure inspection available for subsequent plans
- CLI interface for settings dialog control
  </success_criteria>

<output>
After completion, create `.planning/phases/06-settings-dialog/06-01-SUMMARY.md`
</output>
