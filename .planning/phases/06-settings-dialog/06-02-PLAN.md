---
phase: 06-settings-dialog
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified: [skills_scripts/ui_automation/ChronoSettingsController.cs, skills_scripts/ui_automation/Program.cs]
autonomous: true
---

<objective>
Implement TabControl navigation and Path settings automation for SettingsDialog.

Purpose: Enable automated configuration of ChronoView folder paths (Line 1/Line 2 camera paths, output, quarantine) via programmatic tab selection and TextBox value read/write.

Output: Tab navigation methods, path TextBox get/set operations, CLI settings path commands.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior phase decisions:
@.planning/phases/05-workflow-control/05-03-SUMMARY.md  # ValuePattern for TextBox I/O established
@.planning/phases/06-settings-dialog/06-01-SUMMARY.md  # SettingsDialog opening and ChronoSettingsController

# Key existing patterns:
- TextBox reading: valuePattern.Value for read, ValuePattern.SetValue() for write
- Label-TextBox association: Find Text label, traverse parent to find sibling Edit control
- SelectionItemPattern for TabItem selection (standard WPF pattern)

# SettingsDialog structure (from SettingsDialog.xaml):
- TabControl with 5 tabs: Paths, Data Sequence, UI Options, Advanced, External Programs
- Paths tab has Line 1 (Nir1, Normal1, Cam1-3) and Line 2 (Nir2, Normal2, Cam4-6) paths
- Each path row: Label + TextBox + Browse button + Open button
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add TabControl navigation methods</name>
  <files>skills_scripts/ui_automation/ChronoSettingsController.cs</files>
  <action>Add tab navigation methods to ChronoSettingsController:
- SelectTab(Window? dialog, string tabName): Finds TabItem by Header text (substring search), uses SelectionItemPattern.Select() to activate tab
- GetSelectedTab(Window? dialog): Returns currently selected TabItem header text
- SelectPathsTab(), SelectDataSequenceTab(), SelectUiOptionsTab(), SelectAdvancedTab(), SelectExternalProgramsTab(): Convenience methods

Tab selection pattern:
1. Find TabControl (ControlType.Tab)
2. Find TabItem children (ControlType.TabItem)
3. Match by Header property (Name) containing tabName
4. Get SelectionItemPattern and call Select()

Support bilingual tab headers (English first, Korean fallback for "Paths" = "경로"):</action>
  <verify>dotnet build succeeds, tab selection methods compile</verify>
  <done>TabControl navigation methods using SelectionItemPattern, bilingual support</done>
</task>

<task type="auto">
  <name>Task 2: Add Path TextBox find/read/write methods</name>
  <files>skills_scripts/ui_automation/ChronoSettingsController.cs</files>
  <action>Add path TextBox automation methods to ChronoSettingsController:
- FindPathTextBox(Window? dialog, string labelText, string? scopeSection = null): Finds Label by text, traverses parent to find sibling Edit control. Use scopeSection to search within specific section (e.g., "Line 1", "Line 2") for disambiguation.
- GetPathTextBoxValue(Window? dialog, string labelText): Returns current TextBox value using ValuePattern.Value with Name property fallback
- SetPathTextBoxValue(Window? dialog, string labelText, string value): Sets TextBox value using ValuePattern.SetValue()
- GetLine1Paths(): Returns Dictionary with Nir1Path, Normal1Path, Camera1Path, Camera2Path, Camera3Path
- GetLine2Paths(): Returns Dictionary with Nir2Path, Normal2Path, Camera4Path, Camera5Path, Camera6Path
- GetOutputPath(): Returns OutputPath value
- GetQuarantinePath(): Returns DeleteQuarantinePath value
- SetLine1Path(string pathKey, string value): Sets specific Line 1 path
- SetLine2Path(string pathKey, string value): Sets specific Line 2 path

Label text mappings (Korean primary, English fallback):
- "NIR 1 경로" / "NIR1 Path"
- "일반 1 경로" / "Normal1 Path"
- "카메라 1 경로" / "Camera1 Path"
- ... (similar for all paths)

Re-use FindPathTextBox pattern from ChronoWorkflowController (05-03).</action>
  <verify>dotnet build succeeds, path methods compile without errors</verify>
  <done>Path TextBox finding and I/O methods using ValuePattern, bilingual label support</done>
</task>

<task type="auto">
  <name>Task 3: Add CLI settings path commands</name>
  <files>skills_scripts/ui_automation/Program.cs</files>
  <action>Add "settings path" subcommand group:
- "settings path get-all": Gets all path values as JSON
- "settings path get-line1": Gets Line 1 paths
- "settings path get-line2": Gets Line 2 paths
- "settings path get-output": Gets output path
- "settings path get-quarantine": Gets quarantine path
- "settings path set <key> <value>": Sets path by key (e.g., nir1, normal1, cam1-6, output, quarantine)

JSON output format for get-all:
```json
{
  "line1": { "nir1": "...", "normal1": "...", "cam1": "...", "cam2": "...", "cam3": "..." },
  "line2": { "nir2": "...", "normal2": "...", "cam4": "...", "cam5": "...", "cam6": "..." },
  "output": "...",
  "quarantine": "..."
}
```</action>
  <verify>dotnet build succeeds, help shows settings path subcommands</verify>
  <done>CLI settings path commands with get-all JSON output and set capability</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] dotnet build skills_scripts/ui_automation/ui_automation.csproj succeeds without errors
- [ ] Tab navigation uses SelectionItemPattern correctly
- [ ] Path TextBox I/O uses ValuePattern (consistent with Phase 05)
- [ ] Bilingual label/header support implemented
- [ ] CLI commands follow existing pattern
</verification>

<success_criteria>

- TabControl can be navigated programmatically (all 5 tabs accessible)
- Path TextBox values can be read for all configured paths
- Path TextBox values can be set for all configured paths
- Line 1/Line 2 paths are properly scoped and disambiguated
- CLI provides complete path configuration interface
  </success_criteria>

<output>
After completion, create `.planning/phases/06-settings-dialog/06-02-SUMMARY.md`
</output>
