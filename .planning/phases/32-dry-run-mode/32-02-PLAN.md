---
phase: 32-dry-run-mode
plan: 02
type: execute
wave: 2
depends_on: [32-01]
files_modified:
  - skills_scripts/ui_automation/Commands/AppLifecycleCommands.cs
  - skills_scripts/ui_automation/Commands/WindowsCommands.cs
  - skills_scripts/ui_automation/Commands/ToolbarCommands.cs
  - skills_scripts/ui_automation/Commands/DataPanelCommands.cs
  - skills_scripts/ui_automation/Commands/WorkflowCommands.cs
  - skills_scripts/ui_automation/Commands/SettingsCommands.cs
  - skills_scripts/ui_automation/Commands/FileOpsCommands.cs
  - skills_scripts/ui_automation/Commands/TestCommands.cs
  - skills_scripts/ui_automation/Commands/UtilityCommands.cs
  - skills_scripts/ui_automation/Commands/SetupCommands.cs
  - skills_scripts/ui_automation/Commands/LegacyCommands.cs
autonomous: true

must_haves:
  truths:
    - All command handlers accept --dry-run option
    - Dry-run mode returns command info without execution
    - Dry-run validates skill name before returning
    - Real execution path unchanged when --dry-run not specified
  artifacts:
    - path: "skills_scripts/ui_automation/Commands/*Commands.cs"
      provides: "--dry-run option support on all commands"
      contains: "dryRunOption"
    - path: "skills_scripts/ui_automation/Commands/CommandRegistry.cs"
      provides: "Centralized dry-run option registration"
      contains: "RegisterGlobalDryRunOption"
  key_links:
    - from: "All command handlers"
      to: "DryRunValidation.ValidateSkill"
      via: "DryRunHandler checks skill existence"
      pattern: "DryRunValidator\.ValidateSkill"
    - from: "All command handlers"
      to: "JsonResponseHelper.PrintDryRun"
      via: "DryRunHandler outputs validation result"
      pattern: "PrintDryRun\("
---

<objective>
Add --dry-run option to all CLI command handlers for safe command validation. Dry-run validates syntax and skill existence without executing any commands or checking runtime state.

Purpose: Enable test-executor to verify commands before execution. Dry-run is like a compiler - validates syntax without running the program.

Output: All 11 command handlers support --dry-run flag with validation output
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-dry-run-mode/32-CONTEXT.md
@.planning/phases/32-dry-run-mode/32-01-SUMMARY.md
@skills_scripts/ui_automation/Commands/AppLifecycleCommands.cs
@skills_scripts/ui_automation/Commands/JsonResponseHelper.cs
@skills_scripts/ui_automation/Commands/DryRunValidation.cs
@.claude/agents/test-executor-skills.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add global --dry-run option to Program.cs and CommandRegistry</name>
  <files>skills_scripts/ui_automation/Program.cs</files>
  <action>
    Add global --dry-run option to Program.cs (after --verbose option):

    ```csharp
    var dryRunOption = new Option<bool>(
        ["--dry-run"],
        "Validate command syntax without execution")
    {
        Arity = ArgumentArity.ZeroOrOne
    };
    ```

    Add to root command global options:
    ```csharp
    rootCommand.AddGlobalOption(dryRunOption);
    ```

    Store dry-run state like existing quiet/verbose options:
    ```csharp
    static bool s_isDryRun = false;
    ```

    Parse and store in Main after other options:
    ```csharp
    s_isDryRun = parseResult.GetValueForOption(dryRunOption) == true;
    ```

    Add public getter for command handlers to check:
    ```csharp
    public static bool IsDryRun => s_isDryRun;
    ```
  </action>
  <verify>
    grep -n "IsDryRun" skills_scripts/ui_automation/Program.cs
  </verify>
  <done>
    --dry-run option added as global option
    Program.IsDryRun returns dry-run state for handlers
  </done>
</task>

<task type="auto">
  <name>Task 2: Create base class for dry-run handling in command handlers</name>
  <files>skills_scripts/ui_automation/Commands/DryRunHandler.cs</files>
  <action>
    Create DryRunHandler.cs helper for consistent dry-run handling:

    ```csharp
    using System.CommandLine;
    using System.CommandLine.Invocation;
    using static UiAutomation.Commands.JsonResponseHelper;
    using static UiAutomation.Commands.ExitCodes;

    namespace UiAutomation.Commands;

    /// <summary>
    /// Base helper for handling dry-run mode in command handlers.
    /// Provides consistent dry-run behavior across all commands.
    /// </summary>
    public static class DryRunHandler
    {
        /// <summary>
        /// Checks if dry-run mode is active and handles accordingly.
        /// Returns true if caller should skip real execution.
        /// </summary>
        /// <param name="context">Invocation context for accessing parsed options</param>
        /// <param name="skillName">Skill name to validate</param>
        /// <param name="cliTemplate">CLI command template (e.g., "app launch")</param>
        /// <param name="args">Optional arguments dictionary</param>
        /// <returns>True if in dry-run mode (caller should skip execution), false otherwise</returns>
        public static bool CheckDryRun(
            InvocationContext context,
            string skillName,
            string cliTemplate,
            Dictionary<string, object>? args = null)
        {
            // Access dry-run option from global options
            // The option is registered globally, so we can get it from root command
            var dryRunOption = context.ParseResult.RootCommandResult.Command
                .Options.OfType<Option<bool>>()
                .FirstOrDefault(o => o.HasAlias("--dry-run"));

            if (dryRunOption == null || !context.ParseResult.GetValueForOption(dryRunOption))
            {
                return false; // Not in dry-run mode, proceed with execution
            }

            // Dry-run mode: validate and return command info
            args ??= new Dictionary<string, object>();

            // Validate skill name
            var validation = DryRunValidator.ValidateSkill(skillName);
            if (!validation.IsValid)
            {
                // Skill validation failed - print error
                PrintError(
                    validation.ErrorMessage ?? "Unknown skill",
                    validation.ErrorCode ?? INVALID_ARGUMENT,
                    validation.Suggestion
                );
                context.ExitCode = validation.ErrorCode ?? INVALID_ARGUMENT;
                return true; // Skip execution
            }

            // Build full CLI command
            var cliCommand = args.Count > 0
                ? $"{cliTemplate} {FormatArgs(args)}"
                : cliTemplate;

            // Print dry-run response
            PrintDryRun(skillName, cliCommand, args);

            context.ExitCode = SUCCESS;
            return true; // Skip real execution
        }

        /// <summary>
        /// Formats arguments dictionary for CLI display.
        /// </summary>
        private static string FormatArgs(Dictionary<string, object> args)
        {
            var parts = new List<string>();
            foreach (var (key, value) in args)
            {
                if (value is bool b && b)
                {
                    parts.Add($"--{key}");
                }
                else if (value is string s)
                {
                    parts.Add($"--{key} \"{s}\"");
                }
                else if (value is int i)
                {
                    parts.Add($"--{key} {i}");
                }
                else if (value is IEnumerable<int> ints)
                {
                    parts.Add($"--{key} {string.Join(",", ints)}");
                }
                else
                {
                    parts.Add($"--{key} {value}");
                }
            }
            return string.Join(" ", parts);
        }

        /// <summary>
        /// Skill name mapping from CLI commands to skill names.
        /// Maps command paths to their corresponding skill names from test-executor-skills.md.
        /// </summary>
        private static readonly Dictionary<string, string> SkillMapping = new()
        {
            // APP
            ["app launch"] = "APP_LAUNCH",
            ["app stop"] = "APP_STOP",
            ["app restart"] = "APP_RESTART",
            ["app status"] = "APP_STATUS",
            // BATCH
            ["batch select-and-move"] = "BATCH_SELECT_AND_MOVE",
            ["batch select-and-delete"] = "BATCH_SELECT_AND_DELETE",
            ["batch export-all"] = "BATCH_EXPORT_ALL",
            // CONSOLE_LOGS
            ["console-logs list"] = "CONSOLE_LOGS_LIST",
            ["console-logs tail"] = "CONSOLE_LOGS_TAIL",
            ["console-logs search"] = "CONSOLE_LOGS_SEARCH",
            // DATA_PANEL
            ["stats"] = "DATA_PANEL_STATS",
            ["datagrid headers"] = "DATA_PANEL_HEADERS",
            ["datagrid rows"] = "DATA_PANEL_ROWS",
            ["datagrid data"] = "DATA_PANEL_DATA",
            ["datagrid info"] = "DATA_PANEL_INFO",
            ["datagrid cell"] = "DATA_PANEL_CELL",
            ["datagrid export"] = "DATA_PANEL_EXPORT",
            // FILE_OPS
            ["file-ops select row-index"] = "FILE_OPS_SELECT_ROW_INDEX",
            ["file-ops select group-id"] = "FILE_OPS_SELECT_GROUP_ID",
            ["file-ops select prefix"] = "FILE_OPS_SELECT_PREFIX",
            ["file-ops select-all"] = "FILE_OPS_SELECT_ALL",
            ["file-ops clear-selection"] = "FILE_OPS_CLEAR_SELECTION",
            ["file-ops selected"] = "FILE_OPS_SELECTED",
            ["file-ops move rows"] = "FILE_OPS_MOVE_ROWS",
            ["file-ops move group-ids"] = "FILE_OPS_MOVE_GROUP_IDS",
            ["file-ops move prefix"] = "FILE_OPS_MOVE_PREFIX",
            ["file-ops delete rows"] = "FILE_OPS_DELETE_ROWS",
            ["file-ops delete group-ids"] = "FILE_OPS_DELETE_GROUP_IDS",
            ["file-ops wait move"] = "FILE_OPS_WAIT_MOVE",
            ["file-ops wait delete"] = "FILE_OPS_WAIT_DELETE",
            ["file-ops confirm"] = "FILE_OPS_CONFIRM",
            ["file-ops verify deleted"] = "FILE_OPS_VERIFY_DELETED",
            ["file-ops verify row-count"] = "FILE_OPS_VERIFY_ROW_COUNT",
            // LOGS
            ["logs get"] = "LOGS_GET",
            ["logs tail"] = "LOGS_TAIL",
            ["logs filter"] = "LOGS_FILTER",
            ["logs search"] = "LOGS_SEARCH",
            // SETTINGS_DIALOG
            ["settings-dialog open"] = "SETTINGS_DIALOG_OPEN",
            ["settings-dialog close"] = "SETTINGS_DIALOG_CLOSE",
            ["settings-dialog inspect"] = "SETTINGS_DIALOG_INSPECT",
            ["settings-dialog status"] = "SETTINGS_DIALOG_STATUS",
            ["settings-dialog path get-all"] = "SETTINGS_DIALOG_PATH_GET_ALL",
            ["settings-dialog path get-line1"] = "SETTINGS_DIALOG_PATH_GET_LINE1",
            ["settings-dialog path get-line2"] = "SETTINGS_DIALOG_PATH_GET_LINE2",
            ["settings-dialog path get-output"] = "SETTINGS_DIALOG_PATH_GET_OUTPUT",
            ["settings-dialog path get-quarantine"] = "SETTINGS_DIALOG_PATH_GET_QUARANTINE",
            ["settings-dialog path set"] = "SETTINGS_DIALOG_PATH_SET",
            ["settings-dialog checkbox get"] = "SETTINGS_DIALOG_CHECKBOX_GET",
            ["settings-dialog checkbox set"] = "SETTINGS_DIALOG_CHECKBOX_SET",
            ["settings-dialog checkbox list"] = "SETTINGS_DIALOG_CHECKBOX_LIST",
            ["settings-dialog action save"] = "SETTINGS_DIALOG_ACTION_SAVE",
            ["settings-dialog action apply"] = "SETTINGS_DIALOG_ACTION_APPLY",
            ["settings-dialog action cancel"] = "SETTINGS_DIALOG_ACTION_CANCEL",
            ["settings-dialog action reset"] = "SETTINGS_DIALOG_ACTION_RESET",
            // SETUP
            ["setup verify-config"] = "SETUP_VERIFY_CONFIG",
            ["setup complete-full"] = "SETUP_COMPLETE_FULL",
            ["setup open-settings"] = "SETUP_OPEN_SETTINGS",
            ["setup camera-states"] = "SETUP_CAMERA_STATES",
            // TEST
            ["test connectivity"] = "TEST_CONNECTIVITY",
            ["test capabilities"] = "TEST_CAPABILITIES",
            ["test datagrid"] = "TEST_DATAGRID",
            // TOOLBAR
            ["toolbar start"] = "TOOLBAR_START",
            ["toolbar stop"] = "TOOLBAR_STOP",
            ["toolbar settings"] = "TOOLBAR_SETTINGS",
            ["toolbar refresh"] = "TOOLBAR_REFRESH",
            ["toolbar move"] = "TOOLBAR_MOVE",
            ["toolbar delete"] = "TOOLBAR_DELETE",
            ["toolbar list"] = "TOOLBAR_LIST",
            ["toolbar click"] = "TOOLBAR_CLICK",
            ["toolbar enabled"] = "TOOLBAR_ENABLED",
            // UTILITY
            ["inspect workflow"] = "UTILITY_INSPECT_WORKFLOW",
            ["inspect log"] = "UTILITY_INSPECT_LOG",
            ["config path"] = "UTILITY_CONFIG_PATH",
            ["config read"] = "UTILITY_CONFIG_READ",
            ["config get"] = "UTILITY_CONFIG_GET",
            // WINDOWS
            ["windows main"] = "WINDOWS_MAIN",
            ["windows setup"] = "WINDOWS_SETUP",
            ["windows setup-complete"] = "WINDOWS_SETUP_COMPLETE",
            ["windows settings"] = "WINDOWS_SETTINGS",
            ["windows preview"] = "WINDOWS_PREVIEW",
            ["windows all"] = "WINDOWS_ALL",
            // WORKFLOW
            ["workflow launch-general"] = "WORKFLOW_LAUNCH_GENERAL",
            ["workflow launch-nir"] = "WORKFLOW_LAUNCH_NIR",
            ["workflow launch-nir2"] = "WORKFLOW_LAUNCH_NIR2",
            ["workflow toggle-filtering"] = "WORKFLOW_TOGGLE_FILTERING",
            ["workflow camera-states"] = "WORKFLOW_CAMERA_STATES",
            ["workflow path get-line1"] = "WORKFLOW_PATH_GET_LINE1",
            ["workflow path get-line2"] = "WORKFLOW_PATH_GET_LINE2",
            ["workflow path get-all"] = "WORKFLOW_PATH_GET_ALL",
            ["workflow path set-line1"] = "WORKFLOW_PATH_SET_LINE1",
            ["workflow path set-line2"] = "WORKFLOW_PATH_SET_LINE2",
            ["workflow select-tab"] = "WORKFLOW_SELECT_TAB",
        };

        /// <summary>
        /// Gets the skill name for a CLI command.
        /// </summary>
        public static string? GetSkillName(string cliCommand) =>
            SkillMapping.TryGetValue(cliCommand, out var skill) ? skill : null;
    }
    ```

    This helper:
    - Provides CheckDryRun() method for all handlers to use
    - Maps CLI commands to skill names
    - Validates skill existence via DryRunValidator
    - Returns dry-run response on validation success
    - Returns error response on validation failure
  </action>
  <verify>
    grep -n "class DryRunHandler" skills_scripts/ui_automation/Commands/DryRunHandler.cs
  </verify>
  <done>
    DryRunHandler.cs exists with CheckDryRun, GetSkillName methods
    SkillMapping contains all 90+ command-to-skill mappings
  </done>
</task>

<task type="auto">
  <name>Task 3: Add dry-run handling to AppLifecycleCommands</name>
  <files>skills_scripts/ui_automation/Commands/AppLifecycleCommands.cs</files>
  <action>
    Add dry-run check at start of each command handler in AppLifecycleCommands.cs.

    Pattern for launch command (add at start of SetHandler):
    ```csharp
    launchCommand.SetHandler(async (InvocationContext context) =>
    {
        // Dry-run check - return early if in dry-run mode
        if (DryRunHandler.CheckDryRun(context, "APP_LAUNCH", "app launch"))
        {
            return; // Dry-run response already printed
        }

        try
        {
            // ... existing handler code ...
    ```

    Apply same pattern to:
    - app stop: Use DryRunHandler.CheckDryRun(context, "APP_STOP", "app stop")
    - app restart: Use DryRunHandler.CheckDryRun(context, "APP_RESTART", "app restart")
    - app status: Use DryRunHandler.CheckDryRun(context, "APP_STATUS", "app status")

    Add using statement: `using static UiAutomation.Commands.DryRunHandler;`

    Key: Dry-run check happens BEFORE any UI automation or process interaction.
  </action>
  <verify>
    grep -n "CheckDryRun" skills_scripts/ui_automation/Commands/AppLifecycleCommands.cs
  </verify>
  <done>
    All 4 app commands have dry-run check at start
    Dry-run returns without executing real command
  </done>
</task>

<task type="auto">
  <name>Task 4: Add dry-run handling to remaining command handlers</name>
  <files>skills_scripts/ui_automation/Commands/WindowsCommands.cs
skills_scripts/ui_automation/Commands/ToolbarCommands.cs
skills_scripts/ui_automation/Commands/DataPanelCommands.cs
skills_scripts/ui_automation/Commands/WorkflowCommands.cs
skills_scripts/ui_automation/Commands/SettingsCommands.cs
skills_scripts/ui_automation/Commands/FileOpsCommands.cs
skills_scripts/ui_automation/Commands/TestCommands.cs
skills_scripts/ui_automation/Commands/UtilityCommands.cs
skills_scripts/ui_automation/Commands/SetupCommands.cs
skills_scripts/ui_automation/Commands/LegacyCommands.cs</files>
  <action>
    Apply dry-run pattern to all remaining command handlers.

    For each command in each handler file:
    1. Add `using static UiAutomation.Commands.DryRunHandler;` at top
    2. Add dry-run check at start of SetHandler lambda
    3. Use correct skill name from SkillMapping (via GetSkillName or hardcoded)

    Example for WindowsCommands (main command):
    ```csharp
    mainCommand.SetHandler((InvocationContext context) =>
    {
        if (CheckDryRun(context, "WINDOWS_MAIN", "windows main"))
        {
            return;
        }
        // ... existing code ...
    ```

    Bulk approach: Create a script to add dry-run checks using pattern:
    - Find all SetHandler lambdas
    - Insert CheckDryRun call at start with appropriate skill name
    - Skill name from DryRunHandler.GetSkillName() method

    Handlers to update:
    - WindowsCommands: 6 commands (main, setup, setup-complete, settings, preview, all)
    - ToolbarCommands: 8 commands (start, stop, settings, refresh, move, delete, list, click, enabled)
    - DataPanelCommands: 7 commands (stats, headers, rows, data, info, cell, export)
    - WorkflowCommands: 11 commands (launch-general, launch-nir, launch-nir2, toggle-filtering, camera-states, path get-line1, path get-line2, path get-all, path set-line1, path set-line2, select-tab)
    - SettingsCommands: 15 commands (open, close, inspect, status, path get-all, path get-line1, path get-line2, path get-output, path get-quarantine, path set, checkbox get, checkbox set, checkbox list, action save, action apply, action cancel, action reset)
    - FileOpsCommands: 12 commands (select row-index, select group-id, select prefix, select-all, clear-selection, selected, move rows, move group-ids, move prefix, delete rows, delete group-ids, wait move, wait delete, confirm, verify deleted, verify row-count)
    - TestCommands: 3 commands (connectivity, capabilities, datagrid)
    - UtilityCommands: 5 commands (inspect workflow, inspect log, config path, config read, config get)
    - SetupCommands: 4 commands (verify-config, complete-full, open-settings, camera-states)
    - LegacyCommands: Any remaining commands

    Use consistent pattern: dry-run check is FIRST thing in each handler, before any validation or execution.
  </action>
  <verify>
    grep -rn "CheckDryRun" skills_scripts/ui_automation/Commands/ --include="*.cs" | wc -l
  </verify>
  <done>
    All 90+ commands have dry-run check at start
    Dry-run returns without executing any real command
  </done>
</task>

</tasks>

<verification>
Build verification:
```bash
dotnet build skills_scripts/ui_automation/ui_automation.csproj
```

Functional verification (dry-run only):
```bash
# Test valid skill
./ui_automation.exe app launch --dry-run

# Test invalid skill (should error with suggestion)
./ui_automation.exe invalid-command --dry-run

# Test dry-run with existing flags
./ui_automation.exe toolbar start --json --dry-run
```

Expected outputs:
- Valid skill: Returns DryRunResponse with skill, cli, args fields
- Invalid skill: Returns ErrorResponse with errorCode 4, retryable: false, suggestion
</verification>

<success_criteria>
1. All 90+ commands accept --dry-run flag
2. Dry-run validates skill name against test-executor-skills.md
3. Dry-run returns command info without execution (no app launch, no UI interaction)
4. Unknown skills return errorCode 4 with helpful suggestions
5. Real execution unchanged when --dry-run not specified
6. Build succeeds with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/32-dry-run-mode/32-02-SUMMARY.md`
</output>
