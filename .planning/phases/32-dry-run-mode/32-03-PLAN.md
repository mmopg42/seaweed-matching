---
phase: 32-dry-run-mode
plan: 03
type: execute
wave: 3
depends_on: [32-01, 32-02]
files_modified:
  - .claude/agents/test-executor.md
  - .claude/agents/test-orchestrator.md
autonomous: true

must_haves:
  truths:
    - test-executor.md documents --dry-run usage pattern
    - test-executor.md includes dry-run response schema examples
    - test-orchestrator.md references dry-run for validation
    - Documentation includes error handling examples
  artifacts:
    - path: ".claude/agents/test-executor.md"
      provides: "Dry-run mode documentation for executor agent"
      contains: "Dry-Run Mode"
    - path: ".claude/agents/test-orchestrator.md"
      provides: "Dry-run reference for orchestrator agent"
      contains: "dry-run"
  key_links:
    - from: "test-executor.md"
      to: "32-CONTEXT.md"
      via: "Reference to phase context for dry-run design"
      pattern: "32-dry-run-mode"
    - from: "test-orchestrator.md"
      to: "test-executor.md"
      via: "Cross-reference to executor dry-run documentation"
      pattern: "test-executor\.md"
---

<objective>
Document dry-run mode usage patterns in test-executor.md and test-orchestrator.md. Documentation includes response schemas, error handling, and validation workflows for safe command testing.

Purpose: Enable agents to use dry-run mode for command validation before real execution. Documentation is the source of truth for dry-run behavior.

Output: Updated test-executor.md with dry-run section, test-orchestrator.md with validation reference
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-dry-run-mode/32-CONTEXT.md
@.planning/phases/32-dry-run-mode/32-01-SUMMARY.md
@.planning/phases/32-dry-run-mode/32-02-SUMMARY.md
@.claude/agents/test-executor.md
@.claude/agents/test-orchestrator.md
@.claude/agents/test-executor-skills.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dry-run section to test-executor.md</name>
  <files>.claude/agents/test-executor.md</files>
  <action>
    Add comprehensive dry-run documentation to test-executor.md. Insert after "Skill Translation Workflow" section, before "## JSON Response Schemas".

    Content to add:

    ```markdown
    ## Dry-Run Mode

    Dry-run mode enables safe command validation without execution. Use dry-run to verify skill names, parameter syntax, and schema compliance before running real commands.

    ### Usage

    Add `--dry-run` flag to any CLI command:

    ```bash
    ui_automation.exe app launch --dry-run
    ui_automation.exe toolbar start --json --dry-run
    ui_automation.exe file-ops move rows --rows 0,1,2 --dry-run
    ```

    ### What Dry-Run Validates

    Dry-run is like a compiler - it validates syntax without running the program:

    **Validates:**
    - Skill name exists in test-executor-skills.md registry
    - Required arguments are provided
    - Argument types match schema (int, string, bool, array)
    - Flag names are valid

    **Skips:**
    - Runtime state checks (is app running?)
    - UI automation (no window interaction)
    - Process operations (no launch/stop)
    - File system access (no file reads)

    ### Success Response

    On successful validation, dry-run returns the command that would execute:

    ```json
    {
      "success": true,
      "dryRun": true,
      "timestamp": "2026-01-22T00:30:20Z",
      "data": {
        "skill": "APP_LAUNCH",
        "cli": "app launch",
        "args": {}
      }
    }
    ```

    Key fields:
    - `dryRun: true` - Distinguishes from real execution
    - `data.skill` - Validated skill name from registry
    - `data.cli` - CLI command that would execute
    - `data.args` - Arguments that would be passed

    ### Error Response

    On validation failure, dry-run returns error with suggestion:

    ```json
    {
      "success": false,
      "dryRun": true,
      "error": "Unknown skill: APP_LAUNCHX",
      "errorCode": 4,
      "retryable": false,
      "suggestion": "Did you mean: APP_LAUNCH? See .claude/agents/test-executor-skills.md",
      "timestamp": "2026-01-22T00:30:20Z"
    }
    ```

    Key fields:
    - `errorCode: 4` - INVALID_ARGUMENT (all dry-run errors)
    - `retryable: false` - Syntax errors never succeed on retry
    - `suggestion` - Helpful hint for common errors

    ### Validation Error Types

    | Error | ErrorCode | Example | Suggestion |
    |-------|-----------|---------|------------|
    | Unknown skill | 4 | `APP_LAUNCHX` | "Did you mean: APP_LAUNCH?" |
    | Missing required arg | 4 | Missing `--rows` | "Required: --rows <indices>" |
    | Invalid flag name | 4 | `--invalid-flag` | "Valid flags: --rows, --json" |
    | Type mismatch | 4 | `--rows abc` | "--rows requires integers" |

    ### Workflow Example

    **Before real execution:**

    1. **Validate syntax with dry-run:**
       ```bash
       ui_automation.exe file-ops move rows --rows 0,1,2 --dry-run
       # Returns: {"success": true, "dryRun": true, "data": {...}}
       ```

    2. **If validation passes, execute:**
       ```bash
       ui_automation.exe file-ops move rows --rows 0,1,2 --json
       # Performs actual move operation
       ```

    3. **If validation fails, fix and retry:**
       ```bash
       ui_automation.exe file-ops move rowsX --rows 0,1,2 --dry-run
       # Returns: {"error": "Unknown subcommand: rowsX", ...}
       # Fix: Change rowsX to rows
       ```

    ### When to Use Dry-Run

    **Use dry-run:**
    - Before executing commands in automated tests
    - When validating skill names from orchestrator
    - During development of new test scenarios
    - When debugging command construction issues

    **Skip dry-run:**
    - When running already-verified commands
    - In production test execution (performance)
    - When command has no arguments (simple commands)

    ### Integration with Skill Translation

    Dry-run integrates with skill translation workflow:

    ```
    Orchestrator: "Execute skill: FILE_OPS_MOVE_ROWS with args: {...}"
        ↓
    Executor: Parse skill, validate against registry
        ↓
    Executor: ui_automation.exe file-ops move rows --dry-run
        ↓
    CLI: Validate skill exists, return DryRunResponse
        ↓
    Executor: If dryRun: true, proceed to real execution
    ```
    ```

    Place this section after "Skill Translation Workflow" and before "## JSON Response Schemas".
  </action>
  <verify>
    grep -n "## Dry-Run Mode" .claude/agents/test-executor.md
  </verify>
  <done>
    test-executor.md has complete dry-run section
    Section includes usage, validation, response schemas, errors, workflow example
  </done>
</task>

<task type="auto">
  <name>Task 2: Update JSON Response Schemas section with dry-run</name>
  <files>.claude/agents/test-executor.md</files>
  <action>
    Update the "JSON Response Schemas" section in test-executor.md to include dry-run response schema.

    Add after "Success Response Schema":

    ```markdown
    ### Dry-Run Response Schema

    Dry-run responses use the same structure as success responses but include `dryRun: true`:

    ```json
    {
      "success": true,
      "dryRun": true,
      "timestamp": "2026-01-22T00:30:20Z",
      "data": {
        "skill": "TOOLBAR_START",
        "cli": "toolbar start",
        "args": {}
      }
    }
    ```

    **TypeScript interface:**
    ```typescript
    interface DryRunResponse {
      success: true;
      dryRun: true;
      timestamp: string;  // ISO 8601 format
      data: {
        skill: string;    // Validated skill name
        cli: string;      // CLI command that would execute
        args: Record<string, unknown>;  // Arguments (may be empty)
      };
    }
    ```

    **Note:** The `dryRun` field is the key differentiator - always check `response.dryRun === true` before treating a response as real execution.
    ```

    Also update error response section to mention dry-run context:

    ```markdown
    Dry-run errors include `dryRun: true` and always have `errorCode: 4` (INVALID_ARGUMENT):

    ```json
    {
      "success": false,
      "dryRun": true,
      "error": "Unknown skill: INVALID_SKILL",
      "errorCode": 4,
      "retryable": false,
      "suggestion": "Did you mean: APP_LAUNCH? See test-executor-skills.md",
      "timestamp": "2026-01-22T00:30:20Z"
    }
    ```
    ```
  </action>
  <verify>
    grep -n "DryRunResponse" .claude/agents/test-executor.md
  </verify>
  <done>
    JSON Response Schemas section includes dry-run schema
    DryRunResponse TypeScript interface documented
  </done>
</task>

<task type="auto">
  <name>Task 3: Add dry-run reference to test-orchestrator.md</name>
  <files>.claude/agents/test-orchestrator.md</files>
  <action>
    Add dry-run validation reference to test-orchestrator.md.

    Find the delegation or command execution section and add:

    ```markdown
    ### Dry-Run Validation

    Before delegating execution commands, consider using dry-run mode for validation:

    ```bash
    # Validate command syntax first
    Bash.execute("ui_automation.exe app launch --dry-run")

    # If validation passes, execute real command
    Bash.execute("ui_automation.exe app launch")
    ```

    Dry-run validates:
    - Skill name exists in executor registry
    - Parameter syntax is correct
    - No runtime state is checked (fast validation)

    **Dry-run response:**
    ```json
    {
      "success": true,
      "dryRun": true,
      "data": {
        "skill": "APP_LAUNCH",
        "cli": "app launch",
        "args": {}
      }
    }
    ```

    Use dry-run when:
    - Testing new skill names
    - Validating command construction
    - Debugging delegation issues

    See [test-executor.md](test-executor.md#dry-run-mode) for complete dry-run documentation.
    ```

    Add this in the delegation section where executor interaction is described.
  </action>
  <verify>
    grep -n "dry-run" .claude/agents/test-orchestrator.md
  </verify>
  <done>
    test-orchestrator.md references dry-run for validation
    Cross-reference to test-executor.md dry-run section
  </done>
</task>

</tasks>

<verification>
Documentation verification:
```bash
# Check dry-run section exists
grep -n "## Dry-Run Mode" .claude/agents/test-executor.md

# Check schema documentation
grep -n "DryRunResponse" .claude/agents/test-executor.md

# Check orchestrator reference
grep -n "dry-run" .claude/agents/test-orchestrator.md
```

Content verification:
- test-executor.md has dry-run usage examples
- test-executor.md has response schema documentation
- test-orchestrator.md has validation reference
- All references use consistent terminology
</verification>

<success_criteria>
1. test-executor.md has "Dry-Run Mode" section with complete documentation
2. Dry-run response schema documented with TypeScript interface
3. Error response types documented with errorCode 4
4. Workflow example shows dry-run before execution
5. test-orchestrator.md references dry-run for validation
6. All documentation uses consistent field names (dryRun, skill, cli, args)
</success_criteria>

<output>
After completion, create `.planning/phases/32-dry-run-mode/32-03-SUMMARY.md`
</output>
