---
phase: 12-windows-commands
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills_scripts/ui_automation/Commands/WindowsCommands.cs
  - skills_scripts/ui_automation/Program.cs
autonomous: true
---

<objective>
Extract all windows/* commands to WindowsCommands.cs module following the command extraction pattern established in Phase 11-02.

Purpose: Reduce Program.cs line count by extracting window detection commands into a dedicated handler module. Continue the code quality refactoring toward the 500-line target per file.

Output: WindowsCommands class implementing ICommandHandler with 6 subcommands (main, setup, setup-complete, settings, preview, all), and Program.cs reduced by ~360 lines.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-commands-architecture/11-01-SUMMARY.md
@.planning/phases/11-commands-architecture/11-02-SUMMARY.md
@skills_scripts/ui_automation/Program.cs
@skills_scripts/ui_automation/Commands/ICommandHandler.cs
@skills_scripts/ui_automation/Commands/LegacyCommands.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create WindowsCommands class implementing ICommandHandler</name>
  <files>skills_scripts/ui_automation/Commands/WindowsCommands.cs</files>
  <action>Create WindowsCommands.cs in Commands/ folder with:
- Namespace: UiAutomation.Commands
- Implements: ICommandHandler interface
- Method: RegisterCommands(RootCommand rootCommand) - empty for now
- Required using statements: System.CommandLine, System.Linq, System.Text.Json, FlaUI.Core.AutomationElements, FlaUI.Core.Definitions, SkillsScripts.UiAutomation, SkillsScripts.UiAutomation.ChronoWindowFinder

Follow the exact structure of LegacyCommands.cs from Phase 11-02 as a template. Do NOT copy any handler logic yet - just create the class shell.</action>
  <verify>dotnet build succeeds with empty RegisterCommands method</verify>
  <done>WindowsCommands class created, compiles successfully</done>
</task>

<task type="auto">
  <name>Task 2: Extract windows command handlers from Program.cs to WindowsCommands</name>
  <files>skills_scripts/ui_automation/Commands/WindowsCommands.cs, skills_scripts/ui_automation/Program.cs</files>
  <action>Extract the entire windows command group (lines 108-468 in current Program.cs) to WindowsCommands.cs:

1. Copy the following from Program.cs to WindowsCommands.RegisterCommands():
   - jsonOption definition (local to the handler)
   - windowsCommand parent Command with description "ChronoView 윈도우 찾기"
   - All 6 subcommands: main, setup, setup-complete, settings, preview, all
   - All their SetHandler delegates with complete logic

2. IMPORTANT - Preserve exact handler logic verbatim (pure migration):
   - Keep all UIAutomation using statements
   - Keep all PrintJsonOutput, PrintOutput, PrintVerbose calls
   - Keep all Environment.Exit calls with exit code constants
   - Keep Korean/English text strings unchanged

3. Remove from Program.cs:
   - Lines 108-468 (the entire windows command block)
   - The rootCommand.AddCommand(windowsCommand) call

4. Reference the Program.cs exit code constants:
   - Add const int fields matching Program.cs: EXIT_SUCCESS=0, EXIT_ERROR=1, EXIT_NOT_FOUND=2, EXIT_TIMEOUT=3
   - Or use static access if shared constants exist

5. Add missing helper method TryGetAutomationId() if referenced:
   - Implement as: w.TryGetAutomationId(out var id) ? id : null

This is pure migration - do NOT refactor the handler logic.</action>
  <verify>1. dotnet build succeeds
2. ui_automation.exe windows --help shows all 6 subcommands
3. ui_automation.exe windows main executes correctly (if ChronoView running)</verify>
  <done>All 6 windows commands (main, setup, setup-complete, settings, preview, all) work identically to before extraction</done>
</task>

<task type="auto">
  <name>Task 3: Register WindowsCommands in Program.cs and verify all commands</name>
  <files>skills_scripts/ui_automation/Program.cs</files>
  <action>In Program.cs Main() method:

1. After the CommandRegistry instantiation (line ~99), add:
   registry.RegisterHandler(new WindowsCommands());

2. Keep the registry.RegisterAllCommands(rootCommand) call where it is (after all inline commands, before return statement).

3. Verify Program.cs line reduction:
   - Before: 3429 lines
   - After: ~3070 lines (~360 line reduction expected)

4. Build and verify all CLI commands:
   - ui_automation.exe --help (should show windows command)
   - ui_automation.exe windows --help (should show all 6 subcommands)
   - ui_automation.exe windows main --help
   - ui_automation.exe windows setup --help
   - ui_automation.exe windows setup-complete --help
   - ui_automation.exe windows settings --help
   - ui_automation.exe windows preview --help
   - ui_automation.exe windows all --help

5. Test at least one windows command if ChronoView is available.</action>
  <verify>1. dotnet build succeeds with no errors
2. All windows subcommands visible in --help output
3. Program.cs line count reduced by ~360 lines (verify with wc -l)
4. At least one functional test passes (windows main or similar)</verify>
  <done>WindowsCommands registered, Program.cs reduced to ~3070 lines, all CLI commands verified working</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] dotnet build succeeds with no errors or warnings
- [ ] ui_automation.exe --help shows windows command in correct position
- [ ] ui_automation.exe windows --help shows all 6 subcommands
- [ ] Program.cs line count reduced from 3429 to ~3070 lines
- [ ] At least one windows command tested successfully
- [ ] WindowsCommands.cs follows same structure as LegacyCommands.cs
</verification>

<success_criteria>
- All tasks completed
- WindowsCommands.cs implements ICommandHandler with all 6 subcommands
- Program.cs reduced by ~360 lines (tracking toward 500-line target)
- All windows commands work identically to pre-extraction behavior
- No changes to command help text or functionality
</success_criteria>

<output>
After completion, create `.planning/phases/12-windows-commands/12-01-SUMMARY.md` with:
- Phase frontmatter (phase, plan, subsystem, tags, tech-stack, key-files, key-decisions)
- Performance metrics (duration, tasks completed)
- Accomplishments (WindowsCommands created, commands extracted, line reduction)
- Task commits (one per task)
- Files created/modified
- Commands migrated (table with command, description, subcommands)
- Verification commands used
- Next phase readiness (pattern confirmed for remaining extractions)
</output>
