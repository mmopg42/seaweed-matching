---
phase: 03-toolbar-control
plan: 04
type: execute
wave: 2
depends_on: ["03-01", "03-02", "03-03"]
files_modified: [skills_scripts/ui_automation/ChronoToolbarController.cs, skills_scripts/ui_automation/Program.cs]
autonomous: true
---

<objective>
Implement comprehensive toolbar control skill class and CLI integration.

Purpose: Consolidate all toolbar automation capabilities into a cohesive ChronoToolbarController class that provides a complete API for toolbar interactions.

Output: Complete toolbar automation API ready for Phase 9 CLI integration.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-toolbar-control/03-01-SUMMARY.md
@.planning/phases/03-toolbar-control/03-02-SUMMARY.md
@.planning/phases/03-toolbar-control/03-03-SUMMARY.md
@ChronoView/MainWindow.xaml
@skills_scripts/ui_automation/ChronoWindowFinder.cs
@skills_scripts/ui_automation/UiAutomation.cs
</context>

<tasks>
<task type="auto">
  <name>Task 1: Create ChronoToolbarController class</name>
  <files>skills_scripts/ui_automation/ChronoToolbarController.cs</files>
  <action>Create a new file ChronoToolbarController.cs that provides a high-level API for toolbar interactions:

1. Take a UIA3Automation instance in constructor
2. Have a private ChronoWindowFinder field to reuse window detection
3. Methods:
   - FindMainWindow() - Gets MainWindow via ChronoWindowFinder
   - FindToolbarButton(string buttonText) - From UiAutomation
   - ClickButton(AutomationElement) - From UiAutomation
   - ClickStartButton(), ClickStopButton(), ClickSettingsButton(), ClickRefreshButton(), ClickMoveButton(), ClickDeleteButton()
   - ClickToolbarButton(string buttonText) - Generic method
   - bool IsButtonEnabled(string buttonText) - Check if button is enabled
   - string[] GetAvailableButtons() - List all available toolbar buttons

Use namespace SkillsScripts.UiAutomation

This consolidates all toolbar automation into a dedicated, testable class following the ChronoWindowFinder pattern.</action>
  <verify>dotnet build succeeds. ChronoToolbarController class compiles with all methods.</verify>
  <done>ChronoToolbarController.cs created with comprehensive toolbar API</done>
</task>

<task type="auto">
  <name>Task 2: Integrate ChronoToolbarController into CLI</name>
  <files>skills_scripts/ui_automation/Program.cs</files>
  <action>Add a "toolbar" command to Program.cs with subcommands:
1. "toolbar start" - Click Start button
2. "toolbar stop" - Click Stop button
3. "toolbar settings" - Click Settings button
4. "toolbar refresh" - Click Refresh button
5. "toolbar move" - Click Move button
6. "toolbar delete" - Click Delete button
7. "toolbar list" - List all available buttons
8. "toolbar click <text>" - Generic click by button text

The command should:
1. Create UiAutomation and ChronoToolbarController instances
2. Call appropriate ChronoToolbarController method
3. Report results
4. Properly dispose resources

Replace individual click-* commands with the unified toolbar command structure.</action>
  <verify>dotnet build succeeds. All toolbar subcommands work.</verify>
  <done>CLI toolbar command with all subcommands implemented</done>
</task>

<task type="auto">
  <name>Task 3: Add WaitForButtonEnabled and verification helpers</name>
  <files>skills_scripts/ui_automation/ChronoToolbarController.cs</files>
  <action>Add utility methods to ChronoToolbarController for robust automation:

1. WaitForButtonEnabled(string buttonText, int timeoutMs = 5000)
   - Polls every 200ms until button.IsEnabled == true or timeout
   - Returns true if enabled, false if timeout

2. WaitForButtonDisabled(string buttonText, int timeoutMs = 5000)
   - Polls every 200ms until button.IsEnabled == false or timeout
   - Returns true if disabled, false if timeout

3. bool ClickButtonAndWait(string buttonText, int waitMs = 500)
   - Clicks button then waits for UI state to settle
   - Returns click success result

These helpers are essential for reliable automation workflows where buttons may be disabled during certain states.</action>
  <verify>dotnet build succeeds. Wait helpers handle timeouts correctly.</verify>
  <done>Wait and verification helper methods added</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] dotnet build succeeds without errors
- [ ] ChronoToolbarController class exists with all methods
- [ ] CLI toolbar command works with all subcommands
- [ ] Wait helpers enable robust automation workflows
</verification>

<success_criteria>
- ChronoToolbarController provides complete toolbar automation API
- CLI can click any toolbar button on demand
- Wait helpers enable robust state-based automation
- Phase 3 complete - ready for Phase 4 (Data Panel)
</success_criteria>

<output>
After completion, create `.planning/phases/03-toolbar-control/03-04-SUMMARY.md`
</output>
