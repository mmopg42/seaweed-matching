---
phase: 03-toolbar-control
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [skills_scripts/ui_automation/UiAutomation.cs, skills_scripts/ui_automation/Program.cs]
autonomous: true
---

<objective>
Identify and automate the Settings (Setup) and Refresh buttons in ChronoView's toolbar.

Purpose: Complete toolbar automation by handling the Settings dialog trigger and Refresh button.

Output: Settings and Refresh buttons can be reliably found and clicked programmatically.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-toolbar-control/03-01-SUMMARY.md
@.planning/phases/03-toolbar-control/03-02-SUMMARY.md
@ChronoView/MainWindow.xaml
@skills_scripts/ui_automation/UiAutomation.cs
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add ClickSettingsButton method</name>
  <files>skills_scripts/ui_automation/UiAutomation.cs</files>
  <action>Add ClickSettingsButton(Window? mainWindow) method that:
1. Calls FindToolbarButton(mainWindow, "설정")
2. Calls ClickButton on the found element
3. Returns success/failure boolean
4. Logs operations

Note: This button opens the SetupWindow (Click="Setup_Click" at line 50 of MainWindow.xaml), not the SettingsDialog. The SettingsDialog is opened from within SetupWindow.</action>
  <verify>dotnet build succeeds. Method follows ClickToolbarButton pattern.</verify>
  <done>ClickSettingsButton method added</done>
</task>

<task type="auto">
  <name>Task 2: Add ClickRefreshButton method</name>
  <files>skills_scripts/ui_automation/UiAutomation.cs</files>
  <action>Add ClickRefreshButton(Window? mainWindow) method that:
1. Calls FindToolbarButton(mainWindow, "새로고침")
2. Calls ClickButton on the found element
3. Returns success/failure boolean
4. Logs operations

The Refresh button uses Command="{Binding RefreshCommand}" (line 58 of MainWindow.xaml).</action>
  <verify>dotnet build succeeds. Method follows ClickToolbarButton pattern.</verify>
  <done>ClickRefreshButton method added</done>
</task>

<task type="auto">
  <name>Task 3: Add CLI commands for Settings and Refresh</name>
  <files>skills_scripts/ui_automation/Program.cs</files>
<action>Add two CLI commands to Program.cs:
1. "click-settings" - Calls ClickSettingsButton
2. "click-refresh" - Calls ClickRefreshButton

Each command should:
1. Create and dispose UiAutomation and ChronoWindowFinder instances
2. Find MainWindow
3. Call the appropriate Click method
4. Report result (Success/Failed)
5. Handle errors gracefully

For click-settings, note that it opens SetupWindow which can be verified with ChronoWindowFinder.FindSetupWindow().</action>
  <verify>dotnet build succeeds. CLI commands compile and work correctly.</verify>
  <done>click-settings and click-refresh CLI commands implemented</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] dotnet build succeeds without errors
- [ ] ClickSettingsButton can open the Setup dialog
- [ ] ClickRefreshButton triggers refresh
- [ ] CLI commands click-settings and click-refresh work
</verification>

<success_criteria>
- Settings (Setup) button can be clicked to open SetupWindow
- Refresh button can be clicked to trigger data refresh
- All 6 toolbar buttons are now automatable via CLI
- Ready for Phase 03-04 (toolbar control skill consolidation)
</success_criteria>

<output>
After completion, create `.planning/phases/03-toolbar-control/03-03-SUMMARY.md`
</output>
