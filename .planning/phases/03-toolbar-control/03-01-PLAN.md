---
phase: 03-toolbar-control
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [skills_scripts/ui_automation/UiAutomation.cs, skills_scripts/ui_automation/Program.cs]
autonomous: true
---

<objective>
Identify and automate the Start button in ChronoView's toolbar.

Purpose: Establish reliable button identification and click patterns for the Start monitoring button. This is the foundation for toolbar automation.

Output: Start button can be reliably found and clicked programmatically.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-window-detection/02-03-SUMMARY.md
@ChronoView/MainWindow.xaml
@skills_scripts/ui_automation/ChronoWindowFinder.cs
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add FindToolbarButton method to UiAutomation</name>
  <files>skills_scripts/ui_automation/UiAutomation.cs</files>
  <action>Add a generic FindToolbarButton(Window mainWindow, string buttonText) method that:
1. Takes a Window element (MainWindow) and button text to find
2. Searches for Button control type within the window
3. Filters by Name property containing the buttonText (case-insensitive)
4. Returns the first matching Button element or null
5. Logs search progress and results

The toolbar buttons have Name text like "시작", "중지", "설정", "새로고침", "이동", "삭제" (from MainWindow.xaml lines 38, 46, 55, 63, 72, 78).</action>
  <verify>dotnet build succeeds. Method can find toolbar buttons by text.</verify>
  <done>FindToolbarButton method added to UiAutomation.cs</done>
</task>

<task type="auto">
  <name>Task 2: Add ClickButton method for interaction</name>
  <files>skills_scripts/ui_automation/UiAutomation.cs</files>
  <action>Add a ClickButton(AutomationElement? button) method that:
1. Checks if button is null - return false if so
2. Uses button.TryGetClickPattern() to get InvokePattern
3. Calls pattern.Invoke() to click the button
4. Returns true on success, false on failure
5. Logs all operations with [UiAutomation] prefix

This follows FlaUI's recommended pattern for button clicking via InvokePattern.</action>
  <verify>dotnet build succeeds. Method has proper null handling and logging.</verify>
  <done>ClickButton method added to UiAutomation.cs</done>
</task>

<task type="auto">
  <name>Task 3: Add ClickStartButton helper and CLI command</name>
  <files>skills_scripts/ui_automation/UiAutomation.cs, skills_scripts/ui_automation/Program.cs</files>
  <action>Add ClickStartButton(Window? mainWindow) method to UiAutomation that:
1. Calls FindToolbarButton(mainWindow, "시작")
2. Calls ClickButton on the found element
3. Returns success/failure boolean

Add "click-start" CLI command to Program.cs that:
1. Creates UiAutomation and ChronoWindowFinder instances
2. Finds MainWindow using ChronoWindowFinder
3. Calls ClickStartButton
4. Reports result (Success/Failed with reason)
5. Properly disposes resources

The button is at MainWindow.xaml line 33 with Command="{Binding StartCommand}".</action>
  <verify>dotnet build succeeds. CLI command exists and compiles.</verify>
  <done>ClickStartButton and click-start CLI command implemented</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] dotnet build succeeds without errors
- [ ] FindToolbarButton can find buttons by text
- [ ] ClickButton uses InvokePattern correctly
- [ ] click-start CLI command is functional
</verification>

<success_criteria>
- Start button can be reliably found using FindToolbarButton
- ClickButton works with FlaUI's InvokePattern
- CLI click-start command can trigger the Start button
- Foundation ready for remaining toolbar buttons
</success_criteria>

<output>
After completion, create `.planning/phases/03-toolbar-control/03-01-SUMMARY.md`
</output>
