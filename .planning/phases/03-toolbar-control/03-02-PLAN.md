---
phase: 03-toolbar-control
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [skills_scripts/ui_automation/UiAutomation.cs, skills_scripts/ui_automation/Program.cs]
autonomous: true
---

<objective>
Identify and automate the Stop, Move, and Delete buttons in ChronoView's toolbar.

Purpose: Complete toolbar button automation for remaining command-based buttons (Stop, Move, Delete).

Output: Stop, Move, and Delete buttons can be reliably found and clicked programmatically.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-toolbar-control/03-01-SUMMARY.md
@ChronoView/MainWindow.xaml
@skills_scripts/ui_automation/UiAutomation.cs
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add ClickStopButton, ClickMoveButton, ClickDeleteButton methods</name>
  <files>skills_scripts/ui_automation/UiAutomation.cs</files>
  <action>Add three methods following the ClickStartButton pattern:
1. ClickStopButton(Window? mainWindow) - searches for "중지" (line 46 in MainWindow.xaml)
2. ClickMoveButton(Window? mainWindow) - searches for "이동" (line 72 in MainWindow.xaml)
3. ClickDeleteButton(Window? mainWindow) - searches for "삭제" (line 78 in MainWindow.xaml)

Each method should:
- Call FindToolbarButton with the appropriate Korean text
- Call ClickButton on the found element
- Return success/failure boolean
- Log operations

All three buttons use Command bindings (StopCommand, MoveCommand, DeleteCommand).</action>
  <verify>dotnet build succeeds. All three methods follow the same pattern as ClickStartButton.</verify>
  <done>ClickStopButton, ClickMoveButton, ClickDeleteButton methods added</done>
</task>

<task type="auto">
  <name>Task 2: Add CLI commands for Stop, Move, Delete buttons</name>
  <files>skills_scripts/ui_automation/Program.cs</files>
  <action>Add three CLI commands to Program.cs:
1. "click-stop" - Calls ClickStopButton
2. "click-move" - Calls ClickMoveButton
3. "click-delete" - Calls ClickDeleteButton

Each command should:
1. Create and dispose UiAutomation and ChronoWindowFinder instances
2. Find MainWindow
3. Call the appropriate Click method
4. Report result (Success/Failed)
5. Handle errors gracefully

Use the same pattern as the click-start command from 03-01.</action>
  <verify>dotnet build succeeds. All CLI commands compile successfully.</verify>
  <done>click-stop, click-move, click-delete CLI commands implemented</done>
</task>

<task type="auto">
  <name>Task 3: Add generic ClickToolbarButton helper</name>
  <files>skills_scripts/ui_automation/UiAutomation.cs</files>
  <action>Add a generic ClickToolbarButton(Window? mainWindow, string buttonText) method that:
1. Calls FindToolbarButton(mainWindow, buttonText)
2. Calls ClickButton on the found element
3. Returns success/failure

This consolidates the logic and makes the code DRY. The specific methods (ClickStartButton, etc.) can now call this generic method internally, or we can keep them as convenience wrappers.

Update the specific methods to use this generic helper internally.</action>
  <verify>dotnet build succeeds. Code is more DRY with shared helper.</verify>
  <done>Generic ClickToolbarButton helper method added</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] dotnet build succeeds without errors
- [ ] ClickStopButton, ClickMoveButton, ClickDeleteButton work correctly
- [ ] CLI commands click-stop, click-move, click-delete exist
- [ ] Generic ClickToolbarButton helper reduces code duplication
</verification>

<success_criteria>
- Stop, Move, Delete buttons can be clicked via CLI
- Generic ClickToolbarButton helper reduces code duplication
- All command-based toolbar buttons are now automatable
- Ready for Settings/Refresh button automation (different patterns)
</success_criteria>

<output>
After completion, create `.planning/phases/03-toolbar-control/03-02-SUMMARY.md`
</output>
