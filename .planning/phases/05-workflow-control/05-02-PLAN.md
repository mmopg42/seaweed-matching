---
phase: 05-workflow-control
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified: [skills_scripts/ui_automation/ChronoWorkflowController.cs, skills_scripts/ui_automation/Program.cs]
autonomous: true
---

<objective>
Create ChronoWorkflowController class for camera control automation.

Purpose: Dedicated controller class following ChronoWindowFinder/ChronoToolbarController pattern for camera launch button clicks.
Output: ChronoWorkflowController with camera button click methods, CLI workflow camera command.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-toolbar-control/03-04-SUMMARY.md
@ChronoView/UI/Controls/WorkflowPanel.xaml
@skills_scripts/ui_automation/ChronoWindowFinder.cs
@skills_scripts/ui_automation/ChronoToolbarController.cs
@skills_scripts/ui_automation/UiAutomation.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ChronoWorkflowController class</name>
  <files>skills_scripts/ui_automation/ChronoWorkflowController.cs</files>
  <action>
    Create new ChronoWorkflowController.cs following ChronoToolbarController pattern:

    Class structure:
    - Takes UIA3Automation in constructor
    - Has parameterless constructor creating its own UIA3Automation
    - Implements IDisposable
    - Contains ChronoWindowFinder instance for MainWindow location

    Methods to implement:
    1. FindWorkflowPanel(Window? mainWindow) - finds panel within MainWindow
       - Searches ControlType.Custom with Name containing "WorkflowPanel"
       - Returns AutomationElement?

    2. FindCameraButton(AutomationElement? workflowPanel, string buttonText) - finds camera launch button
       - Searches for Button with Name containing buttonText
       - Returns AutomationElement?

    3. ClickButton(AutomationElement? button) - clicks using InvokePattern
       - Reuses FlaUI button.Patterns.Invoke.Pattern

    4. ClickGeneralCameraButton() - clicks General camera launch button
       - Text contains "실행" or "실행 및 활성화" (tooltip)
       - Located in Camera Status section

    5. ClickNirCameraButton() - clicks NIR 1 camera launch button
       - Similar to General but for NIR camera

    6. ClickNir2CameraButton() - clicks NIR 2 camera launch button
       - NIR 2 button (line 59-66 in WorkflowPanel.xaml)

    7. ToggleNir2Filtering() - clicks NIR Filtering toggle button
       - Toggles Nir2FilteringState (line 69-82)

    8. GetCameraStates() - reads all camera state indicators
       - Returns Dictionary<string, string> with camera names and states
       - Reads Ellipse Fill color via helper or button Content/Background

    Follow logging pattern: "[ChronoWorkflowController]" prefix for all Console.WriteLine
  </action>
  <verify>Build succeeds, class compiles without errors</verify>
  <done>ChronoWorkflowController class exists with all camera methods</done>
</task>

<task type="auto">
  <name>Task 2: Add CLI workflow camera command</name>
  <files>skills_scripts/ui_automation/Program.cs</files>
  <action>
    Add "workflow" command with "camera" subcommand:

    Structure:
    - workflow launch-general - Click General camera button
    - workflow launch-nir - Click NIR 1 camera button
    - workflow launch-nir2 - Click NIR 2 camera button
    - workflow toggle-filtering - Toggle NIR filtering
    - workflow camera-states - Read and display all camera states

    Add using alias: Ctrl = SkillsScripts.UiAutomation.ChronoWorkflowController

    Follow existing CLI pattern from toolbar command (Program.cs ~line 150-200).
    Each subcommand creates ChronoWorkflowController instance, calls appropriate method.
  </action>
  <verify>
    Run: ui_automation.exe workflow camera-states
    Expected: Shows all camera states (NotReady, Ready, Active)
  </verify>
  <done>CLI workflow command with camera subcommands functional</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Build succeeds: dotnet build skills_scripts/ui_automation/ui_automation.csproj
- [ ] workflow camera-states shows all camera states
- [ ] workflow launch-general clicks General camera button
- [ ] Follows ChronoToolbarController pattern consistently
</verification>

<success_criteria>
- ChronoWorkflowController provides complete camera control API
- CLI workflow camera commands work end-to-end
- Camera state reading functional for test automation
- Follows established controller class pattern
</success_criteria>

<output>
After completion, create `.planning/phases/05-workflow-control/05-02-SUMMARY.md`

Include:
- Camera button text discoveries (actual Name properties)
- State indicator reading method (Ellipse Fill or other)
- Any differences between XAML and UI Automation tree
</output>
