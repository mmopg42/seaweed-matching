---
phase: 05-workflow-control
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [skills_scripts/ui_automation/Program.cs]
autonomous: true
---

<objective>
Identify and document WorkflowPanel UI structure for automation.

Purpose: Understand WorkflowPanel's element hierarchy, AutomationIds, and control types to enable camera and path automation.
Output: CLI inspect command for WorkflowPanel with detailed element structure.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@ChronoView/UI/Controls/WorkflowPanel.xaml
@skills_scripts/ui_automation/ChronoWindowFinder.cs
@skills_scripts/ui_automation/UiAutomation.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add FindWorkflowPanel method to UiAutomation</name>
  <files>skills_scripts/ui_automation/UiAutomation.cs</files>
  <action>
    Add FindWorkflowPanel method that:
    - Takes Window? mainWindow as parameter
    - Searches for ControlType.Custom with Name or ClassName containing "WorkflowPanel"
    - Returns AutomationElement? if found, null otherwise
    - Logs finding results with control type, name, automationId, className

    Pattern reference: Follow existing FindStatisticsPanel method structure (UiAutomation.cs:671-714).
  </action>
  <verify>Build succeeds, method compiles without errors</verify>
  <done>FindWorkflowPanel method exists and follows established patterns</done>
</task>

<task type="auto">
  <name>Task 2: Add CLI inspect-workflow command</name>
  <files>skills_scripts/ui_automation/Program.cs</files>
  <action>
    Add "inspect-workflow" subcommand under existing "inspect" command or as standalone:
    - Finds ChronoView MainWindow via ChronoWindowFinder
    - Calls FindWorkflowPanel to locate the panel
    - If found: Lists element tree at depth=3 using ListElements()
    - Outputs: ControlType, Name, AutomationId, ClassName for each element
    - Focus on identifying:
      * Camera status buttons (General, NIR, NIR2, NIR Filtering)
      * Path TextBox controls (Line1SampleName, Line1MoveNir, Line1MoveAllData, etc.)
      * Expander headers (Camera Status, Sample Move Settings, Data Status)

    Reference existing CLI patterns from "detect" and "inspect" commands.
  </action>
  <verify>
    Run: ui_automation.exe inspect-workflow
    Expected: Shows WorkflowPanel UI tree with depth=3
  </verify>
  <done>CLI command outputs WorkflowPanel structure with all elements visible</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Build succeeds: dotnet build skills_scripts/ui_automation/ui_automation.csproj
- [ ] inspect-workflow command runs without errors
- [ ] UI tree output shows Camera buttons, TextBox controls, and Expanders
</verification>

<success_criteria>
- WorkflowPanel can be reliably found via FindWorkflowPanel()
- CLI inspect-workflow command outputs complete element structure
- Element hierarchy documented for next plans (camera buttons, path textboxes)
</success_criteria>

<output>
After completion, create `.planning/phases/05-workflow-control/05-01-SUMMARY.md`

Include:
- WorkflowPanel structure findings (AutomationIds, control types)
- Camera button identification (Korean text labels)
- Path TextBox identification (binding properties)
- Any deviations from XAML structure in actual UI Automation tree
</output>
