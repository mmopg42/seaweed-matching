---
phase: 11-commands-architecture
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified: [skills_scripts/ui_automation/Program.cs, skills_scripts/ui_automation/Commands/LegacyCommands.cs]
autonomous: true
---

<objective>
Extract legacy CLI commands (detect, list, find, click) to separate LegacyCommands module.

Purpose: First migration to command handler pattern, reducing Program.cs by ~500 lines.
Output: LegacyCommands.cs implementing ICommandHandler with detect/list/find/click commands.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CONVENTIONS.md
@.planning/phases/11-commands-architecture/11-01-SUMMARY.md
@skills_scripts/ui_automation/Program.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create LegacyCommands class implementing ICommandHandler</name>
  <files>skills_scripts/ui_automation/Commands/LegacyCommands.cs</files>
  <action>
    Create LegacyCommands.cs with:
    - Namespace: UiAutomation.Commands
    - Implements: ICommandHandler
    - Method: public void RegisterCommands(RootCommand rootCommand)

    Include XML doc: /// <summary>Legacy commands from early development (detect, list, find, click). These are superseded by more specific command groups but retained for compatibility.</summary>

    Leave RegisterCommands empty for this task - will populate in Task 2.
  </action>
  <verify>dotnet build succeeds</verify>
  <done>LegacyCommands class exists and implements ICommandHandler</done>
</task>

<task type="auto">
  <name>Task 2: Extract detect, list, find, click command handlers to LegacyCommands</name>
  <files>skills_scripts/ui_automation/Commands/LegacyCommands.cs, skills_scripts/ui_automation/Program.cs</files>
  <action>
    Move the following commands from Program.cs to LegacyCommands.RegisterCommands():

    1. **detect command** (lines ~103-109 in Program.cs)
       - Command: "detect" with description "ChronoView MainWindow 감지 및 속성 출력"
       - Handler: Uses UiAuto.PrintMainWindowInfo()

    2. **list command** (lines ~112-119)
       - Command: "list" with description "모든 윈도우 나열"
       - Handler: Uses automation.FindAllChronoViewWindows()

    3. **find command** (lines ~122-171)
       - Command: "find" with --title/-t and --process/-p options
       - Handler: Window finding by title or process

    4. **click command** (lines ~538-649)
       - Command: "click" with subcommands: start, stop, settings, refresh, move, delete
       - All six subcommands with their handlers

    IMPORTANT: Copy the EXACT handler code including all the inline logic. Do not simplify or refactor - this is a migration.

    From Program.cs:
    - Keep using static PrintOutput, PrintVerbose, PrintJsonOutput, PrintError helpers
    - Keep using s_isQuiet, s_isVerbose static state
    - Keep EXIT_* constants usage

    After extraction, DELETE these command registrations from Program.cs (the var *Command = new Command(...) and rootCommand.AddCommand(...) calls).
  </action>
  <verify>
    dotnet build succeeds
    Test: ui_automation.exe detect works
    Test: ui_automation.exe list works
    Test: ui_automation.exe find --title "ChronoView" works
    Test: ui_automation.exe click start works
  </verify>
  <done>All four command groups moved to LegacyCommands, removed from Program.cs</done>
</task>

<task type="auto">
  <name>Task 3: Register LegacyCommands in Program.cs and verify</name>
  <files>skills_scripts/ui_automation/Program.cs</files>
  <action>
    In Program.cs Main method:
    1. After CommandRegistry instantiation, add: registry.RegisterHandler(new LegacyCommands());
    2. After all existing command registrations, add: registry.RegisterAllCommands(rootCommand);
    3. Remove the TODO comment from 11-01

    Verify that:
    - Program.cs is ~500 lines shorter
    - All legacy commands still work
    - All other commands (windows, toolbar, stats, etc.) still work

    Run tests:
    - ui_automation.exe detect
    - ui_automation.exe list
    - ui_automation.exe click start
    - ui_automation.exe windows main (verify other commands still work)
  </action>
  <verify>
    dotnet build succeeds
    All CLI commands work correctly
    Program.cs line count reduced by ~500 lines (check: wc -l Program.cs)
  </verify>
  <done>LegacyCommands registered via CommandRegistry, all commands verified working</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] dotnet build succeeds
- [ ] LegacyCommands.cs exists with all four command groups
- [ ] detect, list, find, click commands removed from Program.cs
- [ ] ui_automation.exe detect works
- [ ] ui_automation.exe list works
- [ ] ui_automation.exe find --title X works
- [ ] ui_automation.exe click start works
- [ ] Other commands (windows, toolbar) still work
- [ ] Program.cs is ~500 lines shorter
</verification>

<success_criteria>

- LegacyCommands implements ICommandHandler
- All legacy commands extracted from Program.cs
- Commands registered via CommandRegistry.RegisterAllCommands()
- All CLI functionality preserved
- Program.cs reduced by ~500 lines
- Pattern established for remaining phases (12-18)
  </success_criteria>

<output>
After completion, create `.planning/phases/11-commands-architecture/11-02-SUMMARY.md`
</output>
