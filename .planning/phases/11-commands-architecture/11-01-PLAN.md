---
phase: 11-commands-architecture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [skills_scripts/ui_automation/Program.cs, skills_scripts/ui_automation/Commands/ICommandHandler.cs, skills_scripts/ui_automation/Commands/CommandRegistry.cs]
autonomous: true
---

<objective>
Create command registration infrastructure to enable extracting Program.cs commands into separate modules.

Purpose: Reduce Program.cs from 3,604 lines to <100 lines by creating a reusable command registration pattern.
Output: ICommandHandler interface, CommandRegistry class, and updated Program.cs using the registry pattern.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STRUCTURE.md
@skills_scripts/ui_automation/Program.cs
@.planning/phases/09-cli-interface/09-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ICommandHandler interface for command registration pattern</name>
  <files>skills_scripts/ui_automation/Commands/ICommandHandler.cs</files>
  <action>
    Create ICommandHandler.cs interface with:
    - Namespace: UiAutomation.Commands
    - Method: void RegisterCommands(RootCommand rootCommand)
    - XML documentation: /// <summary>Defines a command handler that registers its commands with the root command.</summary>

    This interface will be implemented by all command modules (LegacyCommands, WindowsCommands, ToolbarCommands, etc.).

    Location: Create new Commands/ directory under skills_scripts/ui_automation/
  </action>
  <verify>dotnet build skills_scripts/ui_automation/ui_automation.csproj succeeds with no errors</verify>
  <done>ICommandHandler interface exists with RegisterCommands method signature</done>
</task>

<task type="auto">
  <name>Task 2: Create CommandRegistry class for centralized command registration</name>
  <files>skills_scripts/ui_automation/Commands/CommandRegistry.cs</files>
  <action>
    Create CommandRegistry.cs class with:
    - Namespace: UiAutomation.Commands
    - Field: private List<ICommandHandler> _handlers = new();
    - Method: void RegisterHandler(ICommandHandler handler) - adds handler to list
    - Method: void RegisterAllCommands(RootCommand rootCommand) - iterates handlers and calls RegisterCommands on each
    - XML documentation on all public members

    This registry allows Program.cs to add command handlers without knowing their internals.
  </action>
  <verify>dotnet build succeeds</verify>
  <done>CommandRegistry class exists with RegisterHandler and RegisterAllCommands methods</done>
</task>

<task type="auto">
  <name>Task 3: Update Program.cs to use CommandRegistry pattern</name>
  <files>skills_scripts/ui_automation/Program.cs</files>
  <action>
    Modify Program.cs Main method to:
    1. Create CommandRegistry instance after RootCommand creation
    2. Keep existing command registration code unchanged for now (Phase 11-02 will migrate first set)
    3. Add comment indicating where handler registration will occur: "// TODO: Register command handlers via registry in Phase 11-02+"

    Do NOT remove any existing command registration yet - just add the infrastructure.
    Add using statement: using UiAutomation.Commands;

    The Main method should now have:
    - RootCommand creation (existing)
    - Global options (existing)
    - CommandRegistry instantiation (new)
    - Existing command registrations (unchanged)
    - InvokeAsync (existing)
  </action>
  <verify>dotnet build succeeds, all existing CLI commands still work (test with: ui_automation.exe detect, ui_automation.exe windows main)</verify>
  <done>Program.cs compiles with CommandRegistry infrastructure, all existing commands functional</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] dotnet build skills_scripts/ui_automation/ui_automation.csproj succeeds
- [ ] ICommandHandler.cs exists in Commands/ directory
- [ ] CommandRegistry.cs exists in Commands/ directory
- [ ] Program.cs references CommandRegistry
- [ ] All existing CLI commands still work (manual test: ui_automation.exe --help)
</verification>

<success_criteria>

- ICommandHandler interface defined with RegisterCommands method
- CommandRegistry class implements registration pattern
- Program.cs updated with registry infrastructure
- No existing functionality broken
- Ready for 11-02 to migrate first commands
  </success_criteria>

<output>
After completion, create `.planning/phases/11-commands-architecture/11-01-SUMMARY.md`
</output>
