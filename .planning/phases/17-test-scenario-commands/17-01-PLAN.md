---
phase: 17-test-scenario-commands
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [skills_scripts/ui_automation/Commands/TestCommands.cs, skills_scripts/ui_automation/Program.cs]
autonomous: true
---

<objective>
Extract all `test/*`, `scenario/*`, and `batch/*` commands from Program.cs to TestCommands handler module.

Purpose: Continue Program.cs refactoring by extracting high-level orchestration commands (tests, scenarios, batch operations) to a focused handler class. These commands orchestrate multiple controllers and represent end-to-end workflows. This reduces Program.cs from 2045 to approximately 1450 lines (~595 line reduction).

Output: TestCommands class with ~590 lines covering test connectivity, scenario workflows, and batch operations, registered via CommandRegistry.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior command extraction patterns established:
@.planning/phases/11-commands-architecture/11-01-SUMMARY.md
@.planning/phases/14-data-panel-commands/14-01-SUMMARY.md
@.planning/phases/15-workflow-settings-commands/15-01-SUMMARY.md

# Existing infrastructure to use:
@skills_scripts/ui_automation/Commands/ICommandHandler.cs
@skills_scripts/ui_automation/Commands/CommandRegistry.cs
@skills_scripts/ui_automation/ChronoWindowFinder.cs
@skills_scripts/ui_automation/ChronoDataPanelReader.cs
@skills_scripts/ui_automation/ChronoWorkflowController.cs
@skills_scripts/ui_automation/ChronoFileOperationsController.cs
@skills_scripts/ui_automation/ChronoSettingsController.cs
@skills_scripts/ui_automation/UiAutomation.cs

# Source to extract from:
@skills_scripts/ui_automation/Program.cs (lines 1131-1889 contain test, scenario, and batch commands)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TestCommands class shell with ICommandHandler</name>
  <files>skills_scripts/ui_automation/Commands/TestCommands.cs</files>
  <action>Create TestCommands.cs in Commands/ directory with:
  1. namespace UiAutomation.Commands
  2. using aliases for all controllers:
     - using UiAuto = SkillsScripts.UiAutomation.UiAutomation
     - using Finder = SkillsScripts.UiAutomation.ChronoWindowFinder
     - using Toolbar = SkillsScripts.UiAutomation.ChronoToolbarController
     - using Workflow = SkillsScripts.UiAutomation.ChronoWorkflowController
     - using DataReader = SkillsScripts.UiAutomation.ChronoDataPanelReader
     - using Settings = SkillsScripts.UiAutomation.ChronoSettingsController
     - using FileOps = SkillsScripts.UiAutomation.ChronoFileOperationsController
  3. using System.CommandLine
  4. class TestCommands : ICommandHandler
  5. RegisterCommands(RootCommand rootCommand) method with empty body
  6. XML summary comment describing test/scenario/batch command groups

  DO NOT add any command implementations yet - just the shell structure.</action>
  <verify>dotnet build skills_scripts/ui_automation/ui_automation.csproj succeeds</verify>
  <done>TestCommands.cs created with ICommandHandler interface, empty RegisterCommands method</done>
</task>

<task type="auto">
  <name>Task 2: Extract all test, scenario, and batch commands to TestCommands</name>
  <files>skills_scripts/ui_automation/Commands/TestCommands.cs</files>
  <action>Copy the entire test/scenario/batch command section from Program.cs (lines 1131-1889) to TestCommands.RegisterCommands method.

  Commands to extract:
  1. test commands (3 subcommands):
     - connectivity: Check if ChronoView is running
     - capabilities: List available automation features
     - datagrid: Verify DataGrid accessibility
  2. scenario commands (3 subcommands):
     - start-monitoring: Complete monitoring startup workflow
     - configure-paths: Complete path configuration workflow
     - move-groups: Complete file group move workflow
  3. batch commands (3 subcommands):
     - select-and-move: Multi-row selection and move
     - select-and-delete: Multi-row selection and delete
     - export-all: Export all ChronoView data

  Pattern to follow from prior extractions:
  - Use all controller aliases (UiAuto, Finder, Workflow, DataReader, FileOps, Settings)
  - Create new controller instances in each handler
  - Keep all console output messages unchanged
  - Keep all command structures and options identical
  - Maintain exit code constants pattern (SUCCESS, ERROR, NOT_FOUND, INVALID_ARGUMENT)

  DO NOT refactor the command logic - copy verbatim.</action>
  <verify>dotnet build skills_scripts/ui_automation/ui_automation.csproj succeeds</verify>
  <done>All 9 command groups (test + scenario + batch) extracted to TestCommands with identical behavior</done>
</task>

<task type="auto">
  <name>Task 3: Register TestCommands in Program.cs and remove extracted commands</name>
  <files>skills_scripts/ui_automation/Program.cs</files>
  <action>In Program.cs Main method:
  1. After the FileOpsCommands registration line, add:
     registry.RegisterHandler(new TestCommands());
  2. Delete the entire test/scenario/batch command section (lines 1131-1889)
  3. Verify no other code references the removed commands

  Expected result: Program.cs reduces from ~1650 to approximately 1055 lines (~595 line reduction).</action>
  <verify>
  1. dotnet build skills_scripts/ui_automation/ui_automation.csproj succeeds
  2. ./bin/Debug/net10.0-windows/ui_automation.exe test --help shows all subcommands
  3. ./bin/Debug/net10.0-windows/ui_automation.exe scenario --help shows all subcommands
  4. ./bin/Debug/net10.0-windows/ui_automation.exe batch --help shows all subcommands
  5. powershell -Command "(Get-Content 'skills_scripts\ui_automation\Program.cs').Count" shows ~1055 lines
  </verify>
  <done>TestCommands registered, test/scenario/batch commands removed from Program.cs, line count reduced by ~595</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] dotnet build succeeds with no errors
- [ ] `./bin/Debug/net10.0-windows/ui_automation.exe test --help` displays connectivity, capabilities, datagrid
- [ ] `./bin/Debug/net10.0-windows/ui_automation.exe scenario --help` displays start-monitoring, configure-paths, move-groups
- [ ] `./bin/Debug/net10.0-windows/ui_automation.exe batch --help` displays select-and-move, select-and-delete, export-all
- [ ] `./bin/Debug/net10.0-windows/ui_automation.exe --help` includes test, scenario, batch in command list
- [ ] Program.cs line count is approximately 1055 lines
- [ ] All subcommands have their help text displayed correctly
</verification>

<success_criteria>

- TestCommands class created with ICommandHandler implementation
- All 9 command groups extracted (3 test + 3 scenario + 3 batch)
- Commands use multiple controller APIs (ChronoWindowFinder, ChronoToolbarController, ChronoDataPanelReader, ChronoWorkflowController, ChronoFileOperationsController)
- Program.cs reduced from ~1650 to approximately 1055 lines
- All CLI commands work identically to before extraction
- TestCommands registered via CommandRegistry pattern
  </success_criteria>

<output>
After completion, create `.planning/phases/17-test-scenario-commands/17-01-SUMMARY.md` following the template from prior phases
</output>
