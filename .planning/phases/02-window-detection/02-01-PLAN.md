---
phase: 02-window-detection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [skills_scripts/ui_automation/UiAutomation.cs]
autonomous: true
---

<objective>
Identify and analyze ChronoView MainWindow for UI automation.

Purpose: Establish reliable window identification patterns for ChronoView's main window using FlaUI. This is the foundation for all subsequent UI automation.

Output: MainWindow can be reliably found and its key UI properties are documented.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infra/01-01-SUMMARY.md
@.planning/phases/01-infra/01-02-SUMMARY.md
@ChronoView/MainWindow.xaml
@skills_scripts/ui_automation/UiAutomation.cs
</context>

<tasks>
<task type="auto">
  <name>Task 1: Verify MainWindow title detection</name>
  <files>skills_scripts/ui_automation/UiAutomation.cs</files>
  <action>Test FindWindowByTitle method with ChronoView's MainWindow title. According to MainWindow.xaml line 14, the Title is "ChronoView Pro - Desktop Application". Create a simple test method FindChronoViewMainWindow() that:
1. Uses FindWindowByTitle("ChronoView Pro") with substring=true to match the title
2. Returns the Window element if found
3. Logs success with window properties (Name, ClassName, AutomationId)

DO NOT create a separate test file - just add the method to UiAutomation.cs for now.</action>
  <verify>Manually run dotnet run --project skills_scripts/ui_automation/ui_automation.csproj with a test command that calls FindChronoViewMainWindow()</verify>
  <done>Method exists and can find ChronoView MainWindow when app is running</done>
</task>

<task type="auto">
  <name>Task 2: Extract MainWindow UI properties</name>
  <files>skills_scripts/ui_automation/UiAutomation.cs</files>
  <action>Add method GetWindowProperties(Window window) that extracts and prints:
1. Window.Name (title text)
2. Window.ClassName
3. Window.AutomationId
4. Window.Properties.NativeWindowHandle (if supported)
5. Window.Bounds (position and size)

Add method PrintMainWindowInfo() that:
1. Finds ChronoView main window
2. Calls GetWindowProperties to output all properties
3. Uses ListElements to show immediate children (depth=2) for toolbar identification later

These methods use existing FindWindowByTitle and ListElements methods.</action>
  <verify>dotnet build succeeds. Method can be called and outputs window properties.</verify>
  <done>GetWindowProperties and PrintMainWindowInfo methods added to UiAutomation.cs</done>
</task>

<task type="auto">
  <name>Task 3: Add CLI command for window detection</name>
  <files>skills_scripts/ui_automation/Program.cs</files>
  <action>Add a "detect" command to the CLI that:
1. Creates UiAutomation instance
2. Calls FindChronoViewMainWindow()
3. If found, calls PrintMainWindowInfo()
4. Properly disposes the UiAutomation instance

Replace the TODO comments in the existing find command with actual implementation using UiAutomation class methods.</action>
  <verify>dotnet build succeeds. Running "dotnet run --project skills_scripts/ui_automation/ui_automation.csproj detect" shows ChronoView window info when app is running.</verify>
  <done>CLI detect command works and outputs MainWindow properties and UI tree</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] dotnet build succeeds without errors
- [ ] CLI command can find and list ChronoView MainWindow properties
- [ ] MainWindow UI tree structure is visible for reference
</verification>

<success_criteria>
- ChronoView MainWindow can be reliably found by title
- Window properties (Name, Class, AutomationId, Handle, Bounds) are extractable
- UI element inspection works for MainWindow children
- Foundation ready for Phase 3 (toolbar button identification)
</success_criteria>

<output>
After completion, create `.planning/phases/02-window-detection/02-01-SUMMARY.md`
</output>
