---
phase: 02-window-detection
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [skills_scripts/ui_automation/UiAutomation.cs, skills_scripts/ui_automation/Program.cs]
autonomous: true
---

<objective>
Identify ChronoView dialog windows (Setup, Settings, ImagePreview) for UI automation.

Purpose: Establish reliable identification patterns for all ChronoView dialogs. These dialogs are critical for configuration workflows and image viewing automation.

Output: All dialog windows can be reliably found and their properties documented.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infra/01-01-SUMMARY.md
@.planning/phases/01-infra/01-02-SUMMARY.md
@ChronoView/UI/Views/SetupWindow.xaml
@ChronoView/UI/Views/SettingsDialog.xaml
@ChronoView/UI/Views/ImagePreviewWindow.xaml
@skills_scripts/ui_automation/UiAutomation.cs
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add SetupWindow detection</name>
  <files>skills_scripts/ui_automation/UiAutomation.cs</files>
  <action>Add method FindSetupWindow() that:
1. Uses FindWindowByTitle with substring matching
2. Searches for "Setup" in title (SetupWindow.xaml line 4 shows "Setup - ChronoView Pro")
3. Returns the Window element or null if not found

The window has WindowStyle="None" and AllowsTransparency="True" (lines 6-7 of SetupWindow.xaml), which may affect detection. Use substring search to ensure reliability.</action>
  <verify>dotnet build succeeds. Method can be called directly.</verify>
  <done>FindSetupWindow method added to UiAutomation.cs</done>
</task>

<task type="auto">
  <name>Task 2: Add SettingsDialog detection</name>
  <files>skills_scripts/ui_automation/UiAutomation.cs</files>
  <action>Add method FindSettingsDialog() that:
1. Uses FindWindowByTitle with substring matching
2. Searches for title from resource string {x:Static res:Strings.Dialog_Settings}
3. The dialog is shown via SettingsDialog.xaml with WindowStartupLocation="CenterOwner" (line 7)
4. Returns the Window element or null if not found

Note: The exact title comes from localization resources, so use substring "Settings" for English or "설정" for Korean fallback.</action>
  <verify>dotnet build succeeds. Method can be called directly.</verify>
  <done>FindSettingsDialog method added to UiAutomation.cs</done>
</task>

<task type="auto">
  <name>Task 3: Add ImagePreviewWindow detection</name>
  <files>skills_scripts/ui_automation/UiAutomation.cs</files>
  <action>Add method FindImagePreviewWindow() that:
1. Uses FindWindowByTitle with exact or substring matching
2. Searches for "Image Preview" (ImagePreviewWindow.xaml line 4)
3. The window has WindowStyle="None" (line 10) which may affect detection
4. Returns the Window element or null

Also add method FindAllChronoViewWindows() that:
1. Uses FindWindowByTitle with substring="ChronoView"
2. Returns all matching windows as a List<Window>
3. Logs count and titles of all found windows

This gives us both specific dialog finders and a bulk finder.</action>
  <verify>dotnet build succeeds. All dialog finder methods work.</verify>
  <done>FindImagePreviewWindow and FindAllChronoViewWindows methods added</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] dotnet build succeeds without errors
- [ ] All dialog finder methods exist in UiAutomation.cs
- [ ] FindAllChronoViewWindows can enumerate all ChronoView windows
</verification>

<success_criteria>
- SetupWindow can be found when open
- SettingsDialog can be found when open
- ImagePreviewWindow can be found when open
- FindAllChronoViewWindows can list all ChronoView windows
- Foundation ready for Phase 6 (SettingsDialog automation)
</success_criteria>

<output>
After completion, create `.planning/phases/02-window-detection/02-02-SUMMARY.md`
</output>
