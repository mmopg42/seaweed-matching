---
phase: 02-window-detection
plan: 03
type: execute
wave: 2
depends_on: ["02-01", "02-02"]
files_modified: [skills_scripts/ui_automation/UiAutomation.cs, skills_scripts/ui_automation/Program.cs]
autonomous: true
---

<objective>
Implement comprehensive window detection skill methods for ChronoView automation.

Purpose: Consolidate window detection capabilities into a cohesive skill interface that Phase 9 CLI can use. This plan completes the window detection phase.

Output: Complete window detection API ready for CLI integration and subsequent automation phases.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infra/01-01-SUMMARY.md
@.planning/phases/01-infra/01-02-SUMMARY.md
@.planning/phases/02-window-detection/02-01-SUMMARY.md
@.planning/phases/02-window-detection/02-02-SUMMARY.md
@skills_scripts/ui_automation/UiAutomation.cs
</context>

<tasks>
<task type="auto">
  <name>Task 1: Create ChronoWindowFinder helper class</name>
  <files>skills_scripts/ui_automation/ChronoWindowFinder.cs</files>
  <action>Create a new file ChronoWindowFinder.cs that provides a high-level API for finding ChronoView windows. The class should:

1. Take a UIA3Automation instance in constructor
2. Have methods:
   - Window? FindMainWindow() - Finds "ChronoView Pro" main window
   - Window? FindSetupWindow() - Finds Setup dialog
   - Window? FindSettingsDialog() - Finds Settings dialog
   - Window? FindImagePreviewWindow() - Finds Image Preview dialog
   - List<Window> FindAllChronoViewWindows() - Returns all ChronoView windows
   - bool IsWindowOpen(string titleSubstring) - Checks if window exists

3. Each method should use the ConditionFactory pattern from UiAutomation
4. Include null-safe error handling with Console logging

Use namespace SkillsScripts.UiAutomation

This consolidates all window detection logic into a dedicated, testable class.</action>
  <verify>dotnet build succeeds. ChronoWindowFinder class compiles and has all required methods.</verify>
  <done>ChronoWindowFinder.cs created with comprehensive window detection methods</done>
</task>

<task type="auto">
  <name>Task 2: Integrate ChronoWindowFinder into CLI</name>
  <files>skills_scripts/ui_automation/Program.cs</files>
  <action>Update Program.cs to use ChronoWindowFinder:

1. Create "windows" command with subcommands:
   - "windows main" - Finds and prints MainWindow info
   - "windows setup" - Finds SetupWindow
   - "windows settings" - Finds SettingsDialog
   - "windows preview" - Finds ImagePreviewWindow
   - "windows all" - Lists all ChronoView windows

2. Each subcommand should:
   - Create UiAutomation instance
   - Create ChronoWindowFinder instance
   - Call appropriate finder method
   - Print results (found/not found, window properties)
   - Properly dispose resources

3. Add JSON output option (--json) for programmatic access

Replace the existing TODO implementations with functional code.</action>
  <verify>dotnet build succeeds. All subcommands work when ChronoView is running.</verify>
  <done>CLI windows command with all subcommands implemented</done>
</task>

<task type="auto">
  <name>Task 3: Add wait and retry helpers</name>
  <files>skills_scripts/ui_automation/ChronoWindowFinder.cs</files>
  <action>Add utility methods to ChronoWindowFinder for robust automation:

1. Window? WaitForWindow(string titleSubstring, int timeoutMs = 5000)
   - Polls every 200ms until window found or timeout
   - Returns window or null

2. bool WaitForWindowToClose(string titleSubstring, int timeoutMs = 5000)
   - Polls every 200ms until window disappears or timeout
   - Returns true if closed, false if timeout

3. Add method IsMainWindowReady() that checks:
   - MainWindow exists
   - IsEnabled (not during startup/shutdown)
   - Has loaded (try/catch around accessing properties)

These helpers are essential for reliable automation workflows in later phases.</action>
  <verify>dotnet build succeeds. Methods handle timeout correctly.</verify>
  <done>Wait and retry helper methods added to ChronoWindowFinder</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] dotnet build succeeds without errors
- [ ] ChronoWindowFinder class exists with all methods
- [ ] CLI windows command works with all subcommands
- [ ] Wait/retry methods handle timeouts correctly
</verification>

<success_criteria>
- ChronoWindowFinder provides complete window detection API
- CLI can find any ChronoView window on demand
- Wait helpers enable robust automation workflows
- Phase 2 complete - ready for Phase 3 (toolbar control)
</success_criteria>

<output>
After completion, create `.planning/phases/02-window-detection/02-03-SUMMARY.md`
</output>
