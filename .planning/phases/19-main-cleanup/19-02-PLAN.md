---
phase: 19-main-cleanup
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified: [.planning/phases/19-main-cleanup/19-02-SUMMARY.md]
autonomous: true
---

<objective>
Verify that all CLI commands work correctly after the refactoring, ensuring no behavioral changes from the original monolithic Program.cs.

Purpose: After extracting all commands to handlers (Phases 11-18) and cleaning up Program.cs (Phase 19-01), we must verify that the CLI still works identically. This is the final validation step before declaring the refactoring milestone complete.

Output: Test results showing all commands accessible, build succeeds, CLI behavior unchanged
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# All command handlers from Phase 11-18:
@skills_scripts/ui_automation/Commands/LegacyCommands.cs
@skills_scripts/ui_automation/Commands/WindowsCommands.cs
@skills_scripts/ui_automation/Commands/ToolbarCommands.cs
@skills_scripts/ui_automation/Commands/DataPanelCommands.cs
@skills_scripts/ui_automation/Commands/WorkflowCommands.cs
@skills_scripts/ui_automation/Commands/SettingsCommands.cs
@skills_scripts/ui_automation/Commands/FileOpsCommands.cs
@skills_scripts/ui_automation/Commands/TestCommands.cs
@skills_scripts/ui_automation/Commands/UtilityCommands.cs

# Clean Program.cs after Phase 19-01:
@skills_scripts/ui_automation/Program.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build project and verify no compilation errors</name>
  <files>skills_scripts/ui_automation/</files>
  <action>Run dotnet build to ensure the project compiles successfully with no errors or warnings. This confirms all handler registrations are valid and dependencies are correctly resolved.

  Command: dotnet build skills_scripts/ui_automation/ui_automation.csproj

  Verify output shows:
  - Build succeeded
  - 0 errors, 0 warnings (or only expected warnings)</action>
  <verify>dotnet build skills_scripts/ui_automation/ui_automation.csproj succeeds</verify>
  <done>Project builds without errors</done>
</task>

<task type="auto">
  <name>Task 2: Verify root help shows all command groups</name>
  <files>skills_scripts/ui_automation/</files>
  <action>Run the CLI with --help flag and verify all 9 command groups are displayed:
  - detect (legacy)
  - windows
  - toolbar
  - stats, datagrid
  - workflow, logs
  - settings-dialog, console-logs
  - file-ops
  - test, scenario, batch
  - inspect, config

  Command: ./bin/Debug/net10.0-windows/ui_automation.exe --help

  Capture output and verify all command groups are listed.</action>
  <verify>--help output shows all 9 command groups</verify>
  <done>All command groups visible in help output</done>
</task>

<task type="auto">
  <name>Task 3: Smoke test key commands</name>
  <files>skills_scripts/ui_automation/</files>
  <action>Run a selection of commands from each handler to verify they work correctly. These are smoke tests to confirm basic functionality - we're not testing the actual automation (which requires ChronoView running), just that commands parse and respond correctly.

  Test commands:
  1. ./bin/Debug/net10.0-windows/ui_automation.exe detect --help (LegacyCommands)
  2. ./bin/Debug/net10.0-windows/ui_automation.exe windows --help (WindowsCommands)
  3. ./bin/Debug/net10.0-windows/ui_automation.exe toolbar --help (ToolbarCommands)
  4. ./bin/Debug/net10.0-windows/ui_automation.exe stats --help (DataPanelCommands)
  5. ./bin/Debug/net10.0-windows/ui_automation.exe workflow --help (WorkflowCommands)
  6. ./bin/Debug/net10.0-windows/ui_automation.exe settings-dialog --help (SettingsCommands)
  7. ./bin/Debug/net10.0-windows/ui_automation.exe file-ops --help (FileOpsCommands)
  8. ./bin/Debug/net10.0-windows/ui_automation.exe test --help (TestCommands)
  9. ./bin/Debug/net10.0-windows/ui_automation.exe inspect --help (UtilityCommands)
  10. ./bin/Debug/net10.0-windows/ui_automation.exe config --help (UtilityCommands)

  Each should display subcommand help or usage information.</action>
  <verify>All 10 help commands display help text successfully</verify>
  <done>All command groups respond to --help</done>
</task>

<task type="auto">
  <name>Task 4: Verify global options work</name>
  <files>skills_scripts/ui_automation/</files>
  <action>Test that global options (--quiet, --verbose) are recognized. These options should be available for all commands.

  Command: ./bin/Debug/net10.0-windows/ui_automation.exe --help

  Verify the help output shows:
  - Options: --quiet, -q
  - Options: --verbose, -v

  These should be listed under "Global Options" or "Options" section.</action>
  <verify>--help shows --quiet and --verbose global options</verify>
  <done>Global options are available</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] dotnet build succeeds
- [ ] All 9 command groups visible in root help
- [ ] All 10 smoke test commands display help
- [ ] Global options (--quiet, --verbose) visible in help
</verification>

<success_criteria>

- Project builds successfully
- All command groups accessible
- No behavioral regressions detected
- CLI help output complete
- Ready to declare Phase 19 complete
  </success_criteria>

<output>
After completion, create `.planning/phases/19-main-cleanup/19-02-SUMMARY.md` with:
- Build verification results
- Help output verification
- Smoke test results
- Final milestone summary (Phase 11-19 complete, 98% code reduction achieved)
</output>
