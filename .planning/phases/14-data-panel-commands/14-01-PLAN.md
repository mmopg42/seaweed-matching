---
phase: 14-data-panel-commands
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills_scripts/ui_automation/Commands/DataPanelCommands.cs
  - skills_scripts/ui_automation/Program.cs
autonomous: true
---

<objective>
Extract all stats/* and datagrid/* commands from Program.cs to DataPanelCommands handler module.

Purpose: Continue code quality refactoring toward 500-line target per file, following the proven extraction pattern from Phases 12-13.
Output: DataPanelCommands.cs handler class with 7 commands registered via CommandRegistry.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior phase summary for pattern reference:
@.planning/phases/13-toolbar-commands/13-01-SUMMARY.md

# Existing controller to reference:
@skills_scripts/ui_automation/ChronoDataPanelReader.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DataPanelCommands class shell</name>
  <files>skills_scripts/ui_automation/Commands/DataPanelCommands.cs</files>
  <action>
Create DataPanelCommands.cs in Commands/ folder following ToolbarCommands pattern:

1. Add using statements: System.CommandLine, System.Text.Json, DataReader alias
2. Implement ICommandHandler interface
3. Add private exit code constants (SUCCESS, ERROR, NOT_FOUND, INVALID_ARGUMENT)
4. Create empty RegisterCommands(RootCommand) method that compiles
5. Add class XML doc summary

DO NOT extract any command handlers yet - just create the shell.
  </action>
  <verify>dotnet build skills_scripts/ui_automation/ui_automation.csproj succeeds</verify>
  <done>DataPanelCommands.cs compiles with empty RegisterCommands() method</done>
</task>

<task type="auto">
  <name>Task 2: Extract stats and datagrid commands to DataPanelCommands</name>
  <files>skills_scripts/ui_automation/Commands/DataPanelCommands.cs</files>
  <action>
Extract all 7 data panel commands from Program.cs (lines ~107-503) to DataPanelCommands.cs:

Commands to extract (verbatim migration - no refactoring):
- stats (GetAllStatistics - StatisticsPanel data)
- datagrid headers (GetDataGridHeaders - column names)
- datagrid rows (GetDataRowCount - row count)
- datagrid data (GetAllDataGridData - all rows)
- datagrid info (headers + row count summary)
- datagrid cell (GetCellText - specific cell value)
- datagrid export (JSON export of all data)

Follow ToolbarCommands pattern:
- Create local jsonOption in RegisterCommands()
- Copy PrintJsonOutput, PrintOutput helpers
- Use "DataReader" alias for ChronoDataPanelReader
- Keep all Korean/English text unchanged
- Maintain --json output support for all commands
- Create rowArgument and colArgument for datagrid cell command

Remove the extracted command definitions from Program.cs after copying.
  </action>
  <verify>
grep -c "statsCommand\|datagridCommand" skills_scripts/ui_automation/Program.cs returns 0 (no stats/datagrid commands in Program.cs)
  </verify>
  <done>All 7 data panel commands in DataPanelCommands.cs, removed from Program.cs</done>
</task>

<task type="auto">
  <name>Task 3: Register DataPanelCommands in Program.cs</name>
  <files>skills_scripts/ui_automation/Program.cs</files>
  <action>
Add DataPanelCommands registration in Program.cs Main():

After the ToolbarCommands registration (after line ~2861):
```csharp
// Phase 14-01: Register data panel commands
var dataPanelCommands = new DataPanelCommands();
dataPanelCommands.RegisterAllCommands(rootCommand);
```

Note: Use RegisterAllCommands (not RegisterCommands) if ToolbarCommands uses that naming, otherwise use RegisterCommands.
  </action>
  <verify>dotnet build skills_scripts/ui_automation/ui_automation.csproj succeeds</verify>
  <done>DataPanelCommands registered, all stats/datagrid commands accessible via CLI</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] dotnet build succeeds without errors
- [ ] ./bin/Debug/net10.0-windows/ui_automation.exe --help shows stats command
- [ ] ./bin/Debug/net10.0-windows/ui_automation.exe --help shows datagrid command
- [ ] ./bin/Debug/net10.0-windows/ui_automation.exe stats --help works
- [ ] ./bin/Debug/net10.0-windows/ui_automation.exe datagrid --help shows all 6 subcommands
- [ ] Program.cs line count reduced by ~400 lines
</verification>

<success_criteria>

- DataPanelCommands.cs created with all 7 commands
- All commands use ChronoDataPanelReader API
- JSON output support maintained
- Program.cs reduced from 2871 to ~2470 lines (~400 line reduction)
- All CLI commands work identically to before extraction
  </success_criteria>

<output>
After completion, create `.planning/phases/14-data-panel-commands/14-01-SUMMARY.md`
</output>
