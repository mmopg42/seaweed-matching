---
phase: 09-cli-interface
plan: 02
type: execute
wave: 1
depends_on: ["09-01"]
files_modified: [skills_scripts/ui_automation/Program.cs]
autonomous: true
---

<objective>
Implement high-level agent helper commands for common test scenarios and workflow automation.

Purpose: Provide agent-friendly commands that combine multiple operations into single test workflows.
Output: New "test" and "scenario" command groups for end-to-end automation.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-cli-interface/09-01-PLAN.md
@.planning/phases/03-toolbar-control/03-04-SUMMARY.md
@.planning/phases/05-workflow-control/05-03-SUMMARY.md
@skills_scripts/ui_automation/Program.cs
@skills_scripts/ui_automation/ChronoToolbarController.cs
@skills_scripts/ui_automation/ChronoWorkflowController.cs
@skills_scripts/ui_automation/ChronoDataPanelReader.cs
@skills_scripts/ui_automation/ChronoSettingsController.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add "test" command group for connectivity tests</name>
  <files>skills_scripts/ui_automation/Program.cs</files>
  <action>
Add "test" parent command with connectivity and capability subcommands:

test connectivity: Verify ChronoView is running and accessible
- Using ChronoWindowFinder, attempt to find MainWindow
- Returns: success=true with {connected: bool, windowFound: bool, appName: string}
- Exit code: 0 if connected, 2 if not found

test capabilities: List all available automation capabilities
- Returns: success=true with {windows: [...], controllers: [...], commands: [...]}
- Lists which UI elements can be automated (MainWindow, SetupWindow, SettingsDialog, etc.)

test datagrid: Verify DataGrid is accessible and return row count
- Using ChronoDataPanelReader, find DataGrid and get row count
- Returns: success=true with {accessible: bool, rowCount: int, headers: [...]}
- Exit code: 0 if accessible, 2 if not found

All test commands support --json option and use standardized output structure from 09-01.
  </action>
  <verify>Build succeeds, test commands work</verify>
  <done>Test command group added for connectivity verification</done>
</task>

<task type="auto">
  <name>Task 2: Add "scenario" command group for workflow automation</name>
  <files>skills_scripts/ui_automation/Program.cs</files>
  <action>
Add "scenario" parent command with end-to-end workflow subcommands:

scenario start-monitoring: Complete workflow to start monitoring
1. Verify MainWindow exists
2. Click Start button using ChronoToolbarController
3. Wait for button state change (disabled → enabled)
4. Check StatisticsPanel for active monitoring indicators
Returns: success=true with {started: bool, buttonState: string, stats: {...}}

scenario configure-paths: Complete workflow to configure monitoring paths
Arguments: --line1-nir <path>, --line1-normal <path>, --output <path>
1. Open SettingsDialog
2. Navigate to Paths tab
3. Set each specified path
4. Click Save button
5. Wait for dialog to close
6. Verify paths were saved (re-read from SettingsDialog)
Returns: success=true with {configured: [...], verified: bool}

scenario move-groups: Complete workflow to move file groups
Argument: --group-ids <ids> or --row-indices <indices>
1. Find DataGrid
2. Select specified rows
3. Click Move button
4. Wait for operation completion (up to 30s)
5. Verify row count changed
Returns: success=true with {moved: int, originalCount: int, newCount: int}

All scenario commands support --json option and return detailed execution results.
  </action>
  <verify>Build succeeds, scenario commands execute workflows</verify>
  <done>Scenario command group added for workflow automation</done>
</task>

<task type="auto">
  <name>Task 3: Add "batch" command group for bulk operations</name>
  <files>skills_scripts/ui_automation/Program.cs</files>
  <action>
Add "batch" parent command for multi-row operations:

batch select-and-move: Select multiple rows and move them
Arguments: --start-index <int>, --count <int>, or --group-ids <ids>
1. Clear existing selection
2. Select rows by range or GroupId list
3. Click Move button
4. Wait for completion
Returns: success=true with {selected: int, moved: int, duration: string}

batch select-and-delete: Select multiple rows and delete them
Arguments: --start-index <int>, --count <int>, or --group-ids <ids>
1. Clear existing selection
2. Select rows by range or GroupId list
3. Click Delete button
4. Handle confirmation dialog if present
5. Wait for completion
Returns: success=true with {selected: int, deleted: int, confirmed: bool}

batch export-all: Export all available data from ChronoView
1. Get StatisticsPanel statistics
2. Get all DataGrid rows
3. Get all camera states from WorkflowPanel
4. Combine into single JSON output
Returns: success=true with {timestamp: string, statistics: {...}, dataGrid: [...], cameraStates: {...}}

All batch commands use ChronoFileOperationsController and ChronoDataPanelReader.
  </action>
  <verify>Build succeeds, batch commands work correctly</verify>
  <done>Batch command group added for bulk operations</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] dotnet build succeeds without errors
- [ ] test connectivity command returns correct results
- [ ] scenario commands execute complete workflows
- [ ] batch commands handle multiple operations
- [ ] All commands use standardized output from 09-01
</verification>

<success_criteria>

- "test" command group with connectivity, capabilities, and datagrid tests
- "scenario" command group with start-monitoring, configure-paths, move-groups workflows
- "batch" command group with select-and-move, select-and-delete, export-all operations
- All commands use exit codes and JSON output from 09-01
- High-level commands simplify agent automation workflows
  </success_criteria>

<output>
After completion, create `.planning/phases/09-cli-interface/09-02-SUMMARY.md`
</output>
