---
phase: 09-cli-interface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [skills_scripts/ui_automation/Program.cs]
autonomous: true
---

<objective>
Standardize CLI interface for agent consumption with consistent exit codes, error handling, and output formatting.

Purpose: Make the CLI robust and predictable for programmatic agent use.
Output: Enhanced CLI with proper exit codes, structured error handling, and standardized JSON output.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-toolbar-control/03-04-SUMMARY.md
@.planning/phases/04-data-panel/04-03-SUMMARY.md
@skills_scripts/ui_automation/Program.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add exit code constants and helper methods</name>
  <files>skills_scripts/ui_automation/Program.cs</files>
  <action>
Add exit code constants at the top of Program class:
- const int EXIT_SUCCESS = 0;
- const int EXIT_ERROR = 1;
- const int EXIT_NOT_FOUND = 2;
- const int EXIT_TIMEOUT = 3;
- const int EXIT_INVALID_ARGUMENT = 4;

Create helper method PrintJsonOutput(object data) for consistent JSON output.
Create helper method PrintError(string message, int exitCode) for error output.
All existing JSON output should use JsonSerializer.Serialize with consistent formatting.

Place these after the "class Program" declaration and before Main method.
  </action>
  <verify>Build succeeds, constants and helpers compile</verify>
  <done>Exit code constants and output helper methods added</done>
</task>

<task type="auto">
  <name>Task 2: Refactor command handlers to use proper exit codes</name>
  <files>skills_scripts/ui_automation/Program.cs</files>
  <action>
Update command handlers to return proper exit codes:

Current pattern: SetHandler(() => { ... })
New pattern: SetHandler(() => { ...; return exitCode; })

Key commands to update (minimum set):
- All "windows" subcommands (main, setup, settings, preview, all) - return EXIT_NOT_FOUND when window not found
- All "toolbar" subcommands - return EXIT_ERROR on button click failure
- "datagrid" commands - return EXIT_NOT_FOUND when DataGrid not found
- "workflow" commands - return EXIT_ERROR on operation failure
- "logs get/tail/filter/search" - return EXIT_SUCCESS with data, EXIT_ERROR on failure
- "settings-dialog" commands - return EXIT_NOT_FOUND when dialog not found
- "file-ops" commands - return EXIT_ERROR on operation failure

Handlers should return 0 on success, non-zero on failure.
Update Main to return the actual exit code from rootCommand.InvokeAsync.
  </action>
  <verify>Build succeeds, all command handlers return proper exit codes</verify>
  <done>Command handlers updated with proper exit codes</done>
</task>

<task type="auto">
  <name>Task 3: Add --quiet and --verbose options for agent control</name>
  <files>skills_scripts/ui_automation/Program.cs</files>
  <action>
Add global options to RootCommand:
- Option<bool> quietOption = new(["--quiet", "-q"], "Suppress all output except errors")
- Option<bool> verboseOption = new(["--verbose", "-v"], "Enable detailed logging")

Add these options to RootCommand and propagate to handlers.

Handlers should check these options:
- When --quiet is set, suppress Console.WriteLine except for errors
- When --verbose is set, add detailed debug output (e.g., element search details)
- Error output (via PrintError helper) should never be suppressed

Example pattern for handlers:
```
if (!verbose) Console.WriteLine($"[command] Some output");
if (verbose) Console.WriteLine($"[command] DEBUG: element = {element.Name}");
```

Store options in static fields or pass through closure context for handler access.
  </action>
  <verify>Build succeeds, --quiet and --verbose options work as expected</verify>
  <done>Quiet and verbose options added for agent control</done>
</task>

<task type="auto">
  <name>Task 4: Standardize JSON output across all commands</name>
  <files>skills_scripts/ui_automation/Program.cs</files>
  <action>
Ensure all commands with --json option return consistent JSON structure:

Success response:
```json
{
  "success": true,
  "data": { ... }
}
```

Error response (when --json is set):
```json
{
  "success": false,
  "error": "error message",
  "errorCode": 1
}
```

Commands to standardize (all that have --json option):
- windows (main, setup, settings, preview, all)
- stats, datagrid (all subcommands)
- workflow (camera-states, path subcommands)
- logs (get, tail, filter, search)
- settings-dialog (status, path, checkbox subcommands)
- file-ops (selected)

Update JSON serialization to use consistent structure with success boolean at top level.
  </action>
  <verify>Build succeeds, JSON output is consistent across commands</verify>
  <done>JSON output standardized with success/error wrapper</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] dotnet build succeeds without errors
- [ ] Exit codes are properly returned (test with invalid input)
- [ ] --quiet option suppresses non-error output
- [ ] --verbose option shows debug information
- [ ] JSON output has consistent structure with success field
</verification>

<success_criteria>

- CLI has standardized exit codes (0=success, 1=error, 2=not found, 3=timeout, 4=invalid argument)
- --quiet and --verbose options work for all commands
- JSON output is consistent with success/error wrapper
- Error messages are never suppressed by --quiet
- Program.cs returns proper exit codes from Main()
  </success_criteria>

<output>
After completion, create `.planning/phases/09-cli-interface/09-01-SUMMARY.md`
</output>
