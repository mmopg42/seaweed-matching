---
phase: 23-performance-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/agents/test-executor.md
  - skills_scripts/ui_automation/ChronoFileOperationsController.cs
autonomous: true

must_haves:
  truths:
    - "Test execution uses minimal CLI calls without redundant pre-checks"
    - "Inter-operation delays are optimized while maintaining stability"
    - "Documentation reflects optimized execution patterns"
  artifacts:
    - path: ".claude/agents/test-executor.md"
      provides: "Agent guidance for fast test execution"
      contains: "Execution Patterns (OPTIMIZED)"
    - path: "skills_scripts/ui_automation/ChronoFileOperationsController.cs"
      provides: "File operation automation with minimal delays"
      min_lines: 1100
  key_links:
    - from: "test-executor.md"
      to: "skills_scripts/ui_automation/bin/Debug/net10.0-windows/ui_automation.exe"
      via: "Direct CLI execution pattern"
      pattern: "ui_automation\\.exe"
---

## Objective

Optimize test execution speed by minimizing unnecessary delays and redundant pre-checks while preserving all safety validations. Target 20-30% speedup through delay optimization and documentation updates.

**Purpose:** Tests currently have conservative delays between operations. By optimizing these while keeping all safety checks, we achieve faster execution without compromising reliability.

**Output:** Optimized controller code and updated execution guidance in test-executor.md

---

## Execution Context

@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md

## Context

@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/23-performance-documentation/23-RESEARCH.md
@.planning/phases/23-performance-documentation/23-CONTEXT.md

@skills_scripts/ui_automation/ChronoFileOperationsController.cs
@.claude/agents/test-executor.md

---

## Tasks

<task type="auto">
  <name>Task 1: Optimize ChronoFileOperationsController delays</name>
  <files>skills_scripts/ui_automation/ChronoFileOperationsController.cs</files>
  <action>
Review ChronoFileOperationsController.cs for optimization opportunities:

1. Line 771, 821, 871, 921: `Thread.Sleep(100)` - KEEP (minimal, necessary for UI update)
2. Line 1042: `Thread.Sleep(200)` in HandleDeleteConfirmationDialog - REDUCE to 100ms

Changes:
- Line 1042: Change `Thread.Sleep(200)` to `Thread.Sleep(100)` - 200ms is excessive for dialog close

DO NOT change:
- DefaultPollIntervalMs (200ms) - this is the wait loop polling interval
- Thread.Sleep(100) after row selections - minimal delay for UI update
- All timeout values (WaitForMoveComplete, WaitForDeleteComplete, etc.) - keep at 30000ms for file ops
- All button enabled/disabled wait timeouts - keep at 5000ms

The goal is reducing inter-operation delays, NOT reducing operation timeouts or wait poll intervals.
</action>
  <verify>grep -n "Thread.Sleep" skills_scripts/ui_automation/ChronoFileOperationsController.cs shows only 100ms delays (except poll interval)</verify>
  <done>Inter-operation delays optimized to 100ms, all timeouts preserved</done>
</task>

<task type="auto">
  <name>Task 2: Update test-executor.md execution patterns</name>
  <files>.claude/agents/test-executor.md</files>
  <action>
Update test-executor.md to emphasize optimized execution patterns:

1. In "Execution Patterns (OPTIMIZED)" section (around line 86):
   - Add note: "All CLI commands should be executed directly without pre-checks"
   - Emphasize that connectivity checks are only for debugging, not normal flow

2. In "Timeout and Retry Guidelines (REDUCED)" section (around line 303):
   - Add note: "Pre-checks before commands add unnecessary overhead. Execute commands directly first."

3. In "CLI Command Quick Reference" section:
   - Keep the current concise format - it's already optimized

Key message: "Execute first, verify on failure" rather than "Verify before execute"
</action>
  <verify>grep -c "Execute first" .claude/agents/test-executor.md > 0</verify>
  <done>Documentation emphasizes direct execution without redundant pre-checks</done>
</task>

<task type="auto">
  <name>Task 3: Verify no excessive delays in other controllers</name>
  <files>skills_scripts/ui_automation/Chrono*.cs</files>
  <action>
Grep check all controller files for Thread.Sleep patterns:

```bash
grep -rn "Thread.Sleep" skills_scripts/ui_automation/Chrono*.cs | grep -E "[2-9][0-9][0-9]"
```

Any delays >= 200ms (except DefaultPollIntervalMs constant) should be reviewed:
- If it's a one-time UI settle delay: consider reducing to 100ms
- If it's a wait loop poll interval: DO NOT change (200ms is appropriate)
- If it's a timeout value: DO NOT change (keep safety margins)

Report findings:
- If all delays are reasonable: Document "No optimization needed"
- If excessive delays found: List them for consideration (but don't change without justification)
</action>
  <verify>Output of grep shows only reasonable delays</verify>
  <done>All inter-operation delays are optimized, all safety timeouts preserved</done>
</task>

---

## Verification

1. ChronoFileOperationsController.cs has minimal 100ms delays for UI updates
2. test-executor.md emphasizes direct execution without pre-checks
3. No excessive delays (>200ms) found in controllers except for legitimate timeouts
4. All safety checks (window detection, button enabled waits) remain intact

---

## Success Criteria

- Test execution speed improved through reduced inter-operation delays
- No flaky tests introduced (all safety checks preserved)
- Documentation guides agents to use efficient execution patterns
- 20-30% speedup target achievable in typical test scenarios

---

## Output

After completion, create `.planning/phases/23-performance-documentation/23-01-SUMMARY.md`
