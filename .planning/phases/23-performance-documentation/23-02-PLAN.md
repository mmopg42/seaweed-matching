---
phase: 23-performance-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/agents/test-executor.md
  - .claude/agents/test-orchestrator.md
autonomous: true

must_haves:
  truths:
    - "test-executor.md documents setup workflow commands"
    - "test-orchestrator.md documents config verification step"
    - "CLI reference tables include setup commands"
  artifacts:
    - path: ".claude/agents/test-executor.md"
      provides: "Agent documentation with setup commands"
      contains: "setup verify-config"
    - path: ".claude/agents/test-orchestrator.md"
      provides: "Orchestrator documentation with setup workflow"
      contains: "setup complete-full"
  key_links:
    - from: "test-executor.md"
      to: "setup verify-config"
      via: "CLI command reference"
      pattern: "setup (verify-config|complete-full)"
    - from: "test-orchestrator.md"
      to: "Setup Commands"
      via: "Command reference table"
      pattern: "setup"
---

## Objective

Update agent documentation (test-executor.md and test-orchestrator.md) to include the new setup workflow commands from Phase 22. Use concise table format grouped by use case.

**Purpose:** Agents need to know about `setup verify-config` and `setup complete-full` commands for automated testing of ChronoView setup workflow.

**Output:** Updated documentation with setup commands in CLI reference tables

---

## Execution Context

@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md

## Context

@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/23-performance-documentation/23-RESEARCH.md
@.planning/phases/23-performance-documentation/23-CONTEXT.md

@.claude/agents/test-executor.md
@.claude/agents/test-orchestrator.md
@skills_scripts/ui_automation/Commands/SetupCommands.cs

---

## Tasks

<task type="auto">
  <name>Task 1: Add setup commands to test-executor.md CLI reference</name>
  <files>.claude/agents/test-executor.md</files>
  <action>
In test-executor.md, add setup commands to the CLI Command Quick Reference section (around line 320).

Add a new "Setup Commands" subsection with this format:

```markdown
### Setup Commands (NEW)
| Intent | Exact Command |
|--------|--------------|
| Open settings from SetupWindow | `setup open-settings` |
| Verify simulator vs ChronoView config | `setup verify-config --json` |
| Complete full setup workflow | `setup complete-full --verify-config --json` |
| Get camera button states | `setup camera-states --json` |
```

Place this section after "Application Lifecycle" and before "Essential Test Operations".

Note: `setup open-settings`, `setup camera-states` may not be implemented yet. Document as planned commands if not available in SetupCommands.cs.
</action>
  <verify>grep -n "Setup Commands" .claude/agents/test-executor.md</verify>
  <done>Setup commands documented in CLI reference table</done>
</task>

<task type="auto">
  <name>Task 2: Add setup workflow section to test-executor.md</name>
  <files>.claude/agents/test-executor.md</files>
  <action>
In test-executor.md, add a new execution pattern section after "Pattern 4: SetupWindow Handling" (around line 193).

Add "Pattern 5: Setup Workflow":

```markdown
### Pattern 5: Setup Workflow (NEW)
```bash
# Verify configuration before starting (optional but recommended)
ui_automation.exe setup verify-config --config-path path/to/simulator_config.json --json

# Complete full setup workflow (launch cameras, verify config, start monitoring)
ui_automation.exe setup complete-full --verify-config --json

# With verification step first, then full workflow:
ui_automation.exe setup verify-config --open-settings --json
ui_automation.exe setup complete-full --json
```

The `--verify-config` flag in `complete-full` runs config verification as a pre-step.
Use `--strict` to fail on config mismatches instead of continuing.
```
</action>
  <verify>grep -n "Pattern 5: Setup Workflow" .claude/agents/test-executor.md</verify>
  <done>Setup workflow pattern documented</done>
</task>

<task type="auto">
  <name>Task 3: Add setup commands to test-orchestrator.md CLI reference</name>
  <files>.claude/agents/test-orchestrator.md</files>
  <action>
In test-orchestrator.md, add setup commands to the UI Automation CLI Command Reference section (around line 248).

Add a new "Setup Commands" subsection:

```markdown
### Setup Commands
| Intent | Exact Command |
|--------|--------------|
| Verify simulator config matches ChronoView | `setup verify-config [--config-path PATH] [--open-settings] [--json]` |
| Complete setup with cameras and monitoring | `setup complete-full [--verify-config] [--strict] [--json]` |
| Get camera button states | `setup camera-states --json` |
```

Place after "Application Lifecycle" section.
</action>
  <verify>grep -n "Setup Commands" .claude/agents/test-orchestrator.md</verify>
  <done>Setup commands documented in orchestrator reference</done>
</task>

<task type="auto">
  <name>Task 4: Add config verification step to orchestrator workflow guidance</name>
  <files>.claude/agents/test-orchestrator.md</files>
  <action>
In test-orchestrator.md, update the workflow guidance to include config verification.

Find the "Orchestration Workflow" section (around line 110) and add to "Phase 1: Planning":

After "1.3 Create Focused Test Plan", add a new subsection:

```
**1.4. Config Verification (Setup Tests Only)**
For tests involving the setup workflow:
- Run `setup verify-config --json` before `setup complete-full`
- Check config matches between simulator and ChronoView
- Use `--strict` to fail on mismatches if config integrity is critical
```

Also add to "Phase 2: Delegation":
```
When delegating setup workflow tests:
- Include config verification step before full setup
- Specify simulator config path with `--config-path`
```
</action>
  <verify>grep -n "Config Verification" .claude/agents/test-orchestrator.md</verify>
  <done>Config verification step integrated into orchestrator workflow</done>
</task>

---

## Verification

1. test-executor.md has Setup Commands section in CLI reference
2. test-executor.md has Pattern 5: Setup Workflow section
3. test-orchestrator.md has Setup Commands section in CLI reference
4. test-orchestrator.md has config verification step in workflow guidance
5. All command examples use exact syntax from SetupCommands.cs

---

## Success Criteria

- Agents can reference setup commands in documentation
- Config verification step is clearly documented
- Workflow guidance includes when to use verify-config
- Documentation uses concise table format (not prose)

---

## Output

After completion, create `.planning/phases/23-performance-documentation/23-02-SUMMARY.md`
