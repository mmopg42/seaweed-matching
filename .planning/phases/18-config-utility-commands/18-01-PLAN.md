---
phase: 18-config-utility-commands
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [skills_scripts/ui_automation/Commands/UtilityCommands.cs, skills_scripts/ui_automation/Program.cs]
autonomous: true
---

<objective>
Extract all `inspect/*` and `config/*` commands from Program.cs to UtilityCommands handler module.

Purpose: Complete the command extraction refactoring by removing the final inline commands from Program.cs. The remaining commands are utility/diagnostic tools that inspect UI structure or read config files directly - they don't orchestrate controllers but provide debugging and configuration access. This reduces Program.cs from ~1125 to approximately 525 lines (~600 line reduction).

Output: UtilityCommands class with ~200 lines covering inspect commands (workflow, log) and config commands (path, read, get), registered via CommandRegistry.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior command extraction patterns established:
@.planning/phases/11-commands-architecture/11-01-SUMMARY.md
@.planning/phases/14-data-panel-commands/14-01-SUMMARY.md
@.planning/phases/15-workflow-settings-commands/15-01-SUMMARY.md
@.planning/phases/16-file-ops-commands/16-01-SUMMARY.md

# Existing infrastructure to use:
@skills_scripts/ui_automation/Commands/ICommandHandler.cs
@skills_scripts/ui_automation/Commands/CommandRegistry.cs
@skills_scripts/ui_automation/ChronoWindowFinder.cs
@skills_scripts/ui_automation/ChronoDataPanelReader.cs
@skills_scripts/ui_automation/UiAutomation.cs

# Source to extract from:
@skills_scripts/ui_automation/Program.cs (inspect commands ~lines 119-208, config commands ~lines 970-1102)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create UtilityCommands class with ICommandHandler</name>
  <files>skills_scripts/ui_automation/Commands/UtilityCommands.cs</files>
  <action>Create UtilityCommands.cs in Commands/ directory with:
  1. namespace UiAutomation.Commands
  2. using aliases:
     - using UiAuto = SkillsScripts.UiAutomation.UiAutomation
     - using Finder = SkillsScripts.UiAutomation.ChronoWindowFinder
     - using DataReader = SkillsScripts.UiAutomation.ChronoDataPanelReader
  3. using System.CommandLine and System.Text.Json
  4. class UtilityCommands : ICommandHandler
  5. RegisterCommands(RootCommand rootCommand) method
  6. Exit code constants (SUCCESS, ERROR, NOT_FOUND)
  7. XML summary comment describing inspect and config command groups

  DO NOT add command implementations yet - just the shell structure with method signature.</action>
  <verify>dotnet build skills_scripts/ui_automation/ui_automation.csproj succeeds</verify>
  <done>UtilityCommands.cs created with ICommandHandler interface, empty RegisterCommands method</done>
</task>

<task type="auto">
  <name>Task 2: Extract inspect commands to UtilityCommands</name>
  <files>skills_scripts/ui_automation/Commands/UtilityCommands.cs</files>
  <action>Copy the inspect command section from Program.cs to UtilityCommands.RegisterCommands method.

  Commands to extract:
  1. inspect workflow (lines ~122-150 in Program.cs):
     - Uses UiAutomation to find MainWindow and WorkflowPanel
     - Calls automation.ListElements(workflowPanel, maxDepth: 3)
     - Displays key elements and element tree
  2. inspect log (lines ~153-206 in Program.cs):
     - Uses ChronoDataPanelReader to find LogPanel
     - Gets LogDataGrid summary and row count
     - Calls automation.ListElements(logPanel, maxDepth: 2)

  Pattern to follow:
  - Create local UiAutomation and ChronoDataPanelReader instances
  - Keep all console output messages unchanged
  - Keep all command structures identical
  - Create jsonOption locally within RegisterCommands

  DO NOT refactor the command logic - copy verbatim from Program.cs.</action>
  <verify>dotnet build skills_scripts/ui_automation/ui_automation.csproj succeeds</verify>
  <done>inspect workflow and inspect log commands extracted with identical behavior</done>
</task>

<task type="auto">
  <name>Task 3: Extract config commands to UtilityCommands</name>
  <files>skills_scripts/ui_automation/Commands/UtilityCommands.cs</files>
  <action>Copy the config command section from Program.cs to UtilityCommands.RegisterCommands method.

  Commands to extract:
  1. config path (lines ~974-992 in Program.cs):
     - Gets LocalApplicationData folder path
     - Constructs path to config.json
     - Displays file location, existence, size, modified time
  2. config read (lines ~995-1041 in Program.cs):
     - Reads config.json file content
     - Outputs raw or pretty-printed JSON based on --json option
  3. config get (lines ~1044-1100 in Program.cs):
     - Reads specific config value by dot-notation key
     - Navigates JSON path (e.g., "folderPaths.line1SampleName")
     - Displays value or NOT_FOUND error

  Pattern to follow:
  - Use Environment.SpecialFolder.LocalApplicationData
  - Use Path.Combine to construct config path
  - Use JsonDocument for JSON parsing
  - Keep all console output and error handling unchanged

  DO NOT refactor the command logic - copy verbatim from Program.cs.</action>
  <verify>dotnet build skills_scripts/ui_automation/ui_automation.csproj succeeds</verify>
  <done>config path, config read, and config get commands extracted with identical behavior</done>
</task>

<task type="auto">
  <name>Task 4: Register UtilityCommands in Program.cs and remove extracted commands</name>
  <files>skills_scripts/ui_automation/Program.cs</files>
  <action>In Program.cs Main method:
  1. After the TestCommands registration (or after FileOpsCommands if TestCommands not yet registered), add:
     registry.RegisterHandler(new UtilityCommands());
  2. Delete the entire inspect command section (approximately lines 119-208)
  3. Delete the entire config command section (approximately lines 970-1102)
  4. Verify no other code references the removed commands
  5. Remove jsonOption local variable if it was only used by inspect/config commands

  Expected result: Program.cs reduces from ~1125 to approximately 525 lines (~600 line reduction).

  Note: The rowsOption and groupIdsOption definitions at the top of Main should remain as they are used by scenario and batch commands (or will be used by TestCommands once Phase 17 is complete).</action>
  <verify>
  1. dotnet build skills_scripts/ui_automation/ui_automation.csproj succeeds
  2. ./bin/Debug/net10.0-windows/ui_automation.exe inspect --help shows workflow, log subcommands
  3. ./bin/Debug/net10.0-windows/ui_automation.exe config --help shows path, read, get subcommands
  4. powershell -Command "(Get-Content 'skills_scripts\ui_automation\Program.cs').Count" shows ~525 lines
  </verify>
  <done>UtilityCommands registered, inspect/config commands removed from Program.cs, line count reduced by ~600</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] dotnet build succeeds with no errors
- [ ] `./bin/Debug/net10.0-windows/ui_automation.exe inspect --help` displays workflow, log
- [ ] `./bin/Debug/net10.0-windows/ui_automation.exe config --help` displays path, read, get
- [ ] `./bin/Debug/net10.0-windows/ui_automation.exe --help` includes inspect, config in command list
- [ ] Program.cs line count is approximately 525 lines
- [ ] All subcommands have their help text displayed correctly
- [ ] inspect workflow shows WorkflowPanel element tree
- [ ] config path shows config file location
</verification>

<success_criteria>

- UtilityCommands class created with ICommandHandler implementation
- All 5 command groups extracted (2 inspect + 3 config)
- Commands use file I/O and existing controller APIs
- Program.cs reduced from ~1125 to approximately 525 lines
- All CLI commands work identically to before extraction
- UtilityCommands registered via CommandRegistry pattern
- Program.cs now contains only: global options, CommandRegistry setup, rows/groupIds options, and args parsing
  </success_criteria>

<output>
After completion, create `.planning/phases/18-config-utility-commands/18-01-SUMMARY.md` following the template from prior phases
</output>
