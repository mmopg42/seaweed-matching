---
phase: 13-toolbar-commands
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills_scripts/ui_automation/Commands/ToolbarCommands.cs
  - skills_scripts/ui_automation/Program.cs
autonomous: true
---

<objective>
Extract all toolbar/* commands from Program.cs to ToolbarCommands handler module.

Purpose: Continue code quality refactoring toward 500-line target per file, following the proven extraction pattern from Phase 12.
Output: ToolbarCommands.cs handler class with 9 commands registered via CommandRegistry.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior phase summary for pattern reference:
@.planning/phases/12-windows-commands/12-01-SUMMARY.md

# Existing controller to reference:
@skills_scripts/ui_automation/ChronoToolbarController.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ToolbarCommands class shell</name>
  <files>skills_scripts/ui_automation/Commands/ToolbarCommands.cs</files>
  <action>
Create ToolbarCommands.cs in Commands/ folder following WindowsCommands pattern:

1. Add using statements: System.CommandLine, System.Text.Json, Toolbar alias
2. Implement ICommandHandler interface
3. Add private exit code constants (SUCCESS, ERROR, NOT_FOUND)
4. Create empty RegisterCommands(RootCommand) method that compiles
5. Add class XML doc summary

DO NOT extract any command handlers yet - just create the shell.
  </action>
  <verify>dotnet build skills_scripts/ui_automation/ui_automation.csproj succeeds</verify>
  <done>ToolbarCommands.cs compiles with empty RegisterCommands() method</done>
</task>

<task type="auto">
  <name>Task 2: Extract toolbar commands to ToolbarCommands</name>
  <files>skills_scripts/ui_automation/Commands/ToolbarCommands.cs</files>
  <action>
Extract all 9 toolbar commands from Program.cs (lines ~108-290) to ToolbarCommands.cs:

Commands to extract (verbatim migration - no refactoring):
- toolbar start (ClickStartButton)
- toolbar stop (ClickStopButton)
- toolbar settings (ClickSettingsButton)
- toolbar refresh (ClickRefreshButton)
- toolbar move (ClickMoveButton)
- toolbar delete (ClickDeleteButton)
- toolbar list (GetAvailableButtons)
- toolbar click (ClickToolbarButton with text argument)
- toolbar enabled (IsButtonEnabled with text argument)

Follow WindowsCommands pattern:
- Create local jsonOption in RegisterCommands()
- Copy PrintJsonOutput, PrintOutput, PrintVerbose helpers
- Use "Toolbar" alias for ChronoToolbarController
- Keep all Korean/English text unchanged
- Maintain --json output support

Remove the extracted command definitions from Program.cs after copying.
  </action>
  <verify>
grep -c "toolbarCommand" skills_scripts/ui_automation/Program.cs returns 0 (no toolbar commands in Program.cs)
  </verify>
  <done>All 9 toolbar commands in ToolbarCommands.cs, removed from Program.cs</done>
</task>

<task type="auto">
  <name>Task 3: Register ToolbarCommands in Program.cs</name>
  <files>skills_scripts/ui_automation/Program.cs</files>
  <action>
Add ToolbarCommands registration in Program.cs Main():

After the CommandRegistry initialization (after line ~100):
```csharp
// Phase 13-01: Register toolbar commands
var toolbarCommands = new ToolbarCommands();
toolbarCommands.RegisterAllCommands(rootCommand);
```

Note: Use RegisterAllCommands (not RegisterCommands) if WindowsCommands uses that naming, otherwise use RegisterCommands.
  </action>
  <verify>dotnet build skills_scripts/ui_automation/ui_automation.csproj succeeds</verify>
  <done>ToolbarCommands registered, all toolbar commands accessible via CLI</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] dotnet build succeeds without errors
- [ ] ./bin/Debug/net10.0-windows/ui_automation.exe --help shows toolbar command
- [ ] ./bin/Debug/net10.0-windows/ui_automation.exe toolbar --help shows all 9 subcommands
- [ ] Program.cs line count reduced by ~180 lines
</verification>

<success_criteria>

- ToolbarCommands.cs created with all 9 toolbar commands
- All commands use ChronoToolbarController API
- JSON output support maintained
- Program.cs reduced from 3054 to ~2874 lines (~180 line reduction)
- All CLI commands work identically to before extraction
  </success_criteria>

<output>
After completion, create `.planning/phases/13-toolbar-commands/13-01-SUMMARY.md`
</output>
