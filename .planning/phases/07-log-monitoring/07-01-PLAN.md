---
phase: 07-log-monitoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [skills_scripts/ui_automation/ChronoDataPanelReader.cs, skills_scripts/ui_automation/Program.cs]
autonomous: true
domain: [ui-automation, testing]
---

<objective>
Implement LogPanel discovery and basic log reading automation.

Purpose: Enable automated reading of LogPanel log messages for testing and verification.
Output: FindLogPanel method, log reading methods, inspect-log CLI command.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-data-panel/04-03-SUMMARY.md

# LogPanel Structure (from ChronoView/UI/Controls/LogPanel.xaml)
# - LogDataGrid (DataGrid with columns: Severity, Time, Source, Message)
# - SearchBox (TextBox for search filtering)
# - LevelFilter (ComboBox with: All, Debug, Info, Warning, Error)
# - AutoScrollCheckBox (CheckBox for auto-scroll toggle)
# - Clear Button ("로그 저장" / QuickSave, "📁" folder button, "✕" close)
# - Log rows appear as ControlType.DataItem with ControlType.Text children

@skills_scripts/ui_automation/ChronoDataPanelReader.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add FindLogPanel method to ChronoDataPanelReader</name>
  <files>skills_scripts/ui_automation/ChronoDataPanelReader.cs</files>
  <action>
    Add FindLogPanel method following the same pattern as FindStatisticsPanel:
    - Accept optional Window parameter (default: find MainWindow)
    - Search for ControlType.Custom with Name or ClassName containing "LogPanel"
    - Fallback: search all Custom controls and match by ClassName
    - Log success/failure with descriptive messages
    - Return AutomationElement? (null if not found)

    Add to ChronoDataPanelReader.cs in a new #region LogPanel section
  </action>
  <verify>dotnet build skills_scripts/ui_automation/ui_automation.csproj succeeds</verify>
  <done>FindLogPanel method finds LogPanel element in ChronoView MainWindow</done>
</task>

<task type="auto">
  <name>Task 2: Add log reading methods to ChronoDataPanelReader</name>
  <files>skills_scripts/ui_automation/ChronoDataPanelReader.cs</files>
  <action>
    Add methods to read log messages from LogDataGrid:
    - GetLogRowCount(LogPanel): Returns count of DataItem elements (log rows)
    - GetLogHeaders(LogPanel): Returns list of column headers (Severity, Time, Source, Message)
    - GetLogMessage(LogPanel, rowIndex): Returns Dictionary with Severity, Time, Source, Message
    - GetAllLogMessages(): Convenience method that finds LogPanel and returns all log messages

    Use the same DataItem + Text children pattern as FileGroupDataGrid (from 04-03)
    Each log row has 4 Text children: Severity, Timestamp, Source, Message
  </action>
  <verify>dotnet build succeeds, no compilation errors</verify>
  <done>Log reading methods extract Severity, Time, Source, Message from log rows</done>
</task>

<task type="auto">
  <name>Task 3: Add inspect-log CLI command</name>
  <files>skills_scripts/ui_automation/Program.cs</files>
  <action>
    Add "inspect log" subcommand following the pattern of "inspect workflow":
    - Uses ChronoDataPanelReader to find LogPanel
    - Lists element tree at depth=2 for LogDataGrid
    - Outputs ControlType, Name, AutomationId, ClassName for key elements
    - Shows current log row count and headers

    Add to the existing inspectCommand group
  </action>
  <verify>dotnet build succeeds, inspect-log command available in CLI</verify>
  <done>inspect-log command displays LogPanel structure and log summary</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `dotnet build skills_scripts/ui_automation/ui_automation.csproj` succeeds
- [ ] ChronoDataPanelReader has FindLogPanel method
- [ ] ChronoDataPanelReader has GetLogRowCount, GetLogHeaders, GetLogMessage, GetAllLogMessages methods
- [ ] CLI has "inspect log" command
</verification>

<success_criteria>

- FindLogPanel successfully locates LogPanel in MainWindow
- Log reading methods use DataItem pattern consistent with FileGroupDataGrid
- inspect-log command outputs LogPanel structure and log summary
- All methods follow established ChronoDataPanelReader patterns
  </success_criteria>

<output>
After completion, create `.planning/phases/07-log-monitoring/07-01-SUMMARY.md`
</output>
