---
phase: 07-log-monitoring
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified: [skills_scripts/ui_automation/ChronoDataPanelReader.cs, skills_scripts/ui_automation/Program.cs]
autonomous: true
domain: [ui-automation, testing]
---

<objective>
Implement log filtering and real-time log monitoring CLI commands.

Purpose: Enable automated log retrieval with filtering by level, search text, and tail-like monitoring.
Output: Log CLI commands (get, tail, filter, watch).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-log-monitoring/07-01-SUMMARY.md

# LogPanel filtering controls (from LogPanel.xaml):
# - LevelFilter ComboBox: All (null), Debug, Info, Warning, Error
# - SearchBox TextBox: Text-based search filter
# - LogDataGrid: Shows filtered view based on search + level
# - Log columns: Severity, Time, Source, Message

@skills_scripts/ui_automation/ChronoDataPanelReader.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add log filtering methods to ChronoDataPanelReader</name>
  <files>skills_scripts/ui_automation/ChronoDataPanelReader.cs</files>
  <action>
    Add methods to filter and retrieve log messages:
    - GetLogsByLevel(LogPanel, level): Returns only logs matching severity level (Debug, Info, Warning, Error)
    - SearchLogs(LogPanel, searchText): Returns logs containing search text in Source or Message
    - GetFilteredLogs(LogPanel, level, searchText): Combined filter by level and search
    - GetLatestLogs(LogPanel, count): Returns the most recent N log messages

    Level matching should be case-insensitive
    Search uses Contains() case-insensitive matching on Source and Message fields
  </action>
  <verify>dotnet build succeeds, no compilation errors</verify>
  <done>Log filtering methods return filtered log message dictionaries</done>
</task>

<task type="auto">
  <name>Task 2: Add log CLI commands</name>
  <files>skills_scripts/ui_automation/Program.cs</files>
  <action>
    Add "logs" command group with subcommands:
    - logs get: Get all log messages (with optional --json output)
    - logs tail [count]: Get latest N log messages (default: 10)
    - logs filter --level <level>: Filter by severity level (Debug, Info, Warning, Error)
    - logs search <text>: Search logs by text

    Use ChronoDataPanelReader methods from Task 1
    Follow existing CLI patterns (--json option for programmatic access)
    Add to rootCommand alongside stats, datagrid, workflow commands
  </action>
  <verify>dotnet build succeeds, logs commands available in CLI</verify>
  <done>CLI has logs get, logs tail, logs filter, logs search commands</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `dotnet build skills_scripts/ui_automation/ui_automation.csproj` succeeds
- [ ] ChronoDataPanelReader has GetLogsByLevel, SearchLogs, GetFilteredLogs, GetLatestLogs methods
- [ ] CLI has "logs" command group with get, tail, filter, search subcommands
- [ ] --json option works for log output
</verification>

<success_criteria>

- Log filtering methods work correctly for level and text search
- CLI commands provide human-readable and JSON output formats
- All log commands follow established CLI patterns (stats, datagrid, workflow)
- Commands gracefully handle empty log or LogPanel not found cases
  </success_criteria>

<output>
After completion, create `.planning/phases/07-log-monitoring/07-02-SUMMARY.md`
</output>
