---
phase: 25-simulator-status
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - task_helper/data_test/data_simulator.py
autonomous: true
user_setup: []

must_haves:
  truths:
    - "User can query simulation status via --status CLI flag"
    - "Status returns JSON with simulation state (running/completed/idle/error)"
    - "Status includes progress percentage (0-100)"
    - "Status includes items_created count"
    - "Status includes simulation_id (UUID)"
    - "Status includes last_activity timestamp (ISO 8601)"
    - "Status endpoint works without blocking if simulation is idle"
  artifacts:
    - path: "task_helper/data_test/data_simulator.py"
      provides: "Simulation status query via CLI"
      contains: "--status"
      exports: "get_status() method"
  key_links:
    - from: "argparse parser"
      to: "get_status() method"
      via: "--status argument triggers status query"
      pattern: "add_argument.*--status"
    - from: "get_status()"
      to: "is_running, current_simulation_id"
      via: "Direct attribute access for thread-safe reads"
      pattern: "self\\.is_running|self\\.current_simulation_id"
    - from: "get_status()"
      to: "stdout JSON output"
      via: "json.dumps() with sys.stdout.write"
      pattern: "json\\.dumps|print.*json"
---

<objective>
Add --status CLI endpoint to data_simulator.py for querying simulation state.

Purpose: Enable test-executor to poll simulation completion status programmatically.
Output: JSON status response with state, progress, items_created, simulation_id, last_activity.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@task_helper/data_test/data_simulator.py
</context>

<tasks>

<task type="auto">
  <name>Add --status CLI argument and get_status() method</name>
  <files>task_helper/data_test/data_simulator.py</files>
  <action>
    Add --status argument to argparse parser (around line 2239 in main section):
    - Add `parser.add_argument('--status', action='store_true', help='Show simulation status and exit')`
    - Handle --status BEFORE --cli check (status query shouldn't require --cli)
    - Return JSON status and exit with code 0

    Add get_status() method to DataSimulator class (around line 1207 after stop() method):
    - Method signature: `def get_status(self):`
    - Return dictionary with:
      - `status`: "running" if is_running else "completed" if simulation_id exists else "idle"
      - `progress`: 0-100 (calculate from items_created vs total items, or return 0 if no simulation)
      - `items_created`: len(moved_items) if simulation exists
      - `simulation_id`: current_simulation_id or None
      - `last_activity`: ISO 8601 timestamp (current time if running, or stored from last activity)

    Status logic:
    - "running": is_running == True
    - "completed": is_running == False AND current_simulation_id is not None
    - "idle": current_simulation_id is None (never started)
    - "error": never returned by default (reserved for future error state detection)

    For last_activity tracking, add:
    - `self.last_activity_time = None` attribute in __init__ (around line 83)
    - Update `self.last_activity_time = datetime.now()` each time an item is moved (in run_line1_simulation and run_line2_simulation)
  </action>
  <verify>
    Run: `python task_helper/data_test/data_simulator.py --status`
    Expected: JSON output with status field and all required fields
  </verify>
  <done>
    --status argument returns valid JSON with status, progress, items_created, simulation_id, last_activity fields
  </done>
</task>

<task type="auto">
  <name>Add main() entry point for --status handling</name>
  <files>task_helper/data_test/data_simulator.py</files>
  <action>
    Modify the main section (around line 2213) to handle --status:

    Current flow:
    1. Parse arguments
    2. Check --cli flag
    3. Either run GUI or CLI mode

    New flow (insert BEFORE --cli check):
    1. Parse arguments
    2. If args.status:
       - Create DataSimulator instance
       - Call get_status()
       - Print JSON to stdout with proper formatting
       - sys.exit(0)
    3. Continue with existing --cli/GUI logic

    JSON output format:
    ```json
    {
      "status": "running|completed|idle",
      "progress": 85.5,
      "items_created": 376,
      "simulation_id": "uuid-string",
      "last_activity": "2026-01-21T18:30:45"
    }
    ```

    Use json.dumps with indent=2 for human-readable output.
  </action>
  <verify>
    Run: `python task_helper/data_test/data_simulator.py --status` and parse JSON output
    Expected: Valid JSON with all 5 required fields
  </verify>
  <done>
    --status endpoint returns complete JSON status and exits cleanly
  </done>
</task>

</tasks>

<verification>
1. Run `python task_helper/data_test/data_simulator.py --status` when simulator is idle
   - Expected: status="idle", simulation_id=null, progress=0

2. Run simulator in background: `python task_helper/data_test/data_simulator.py --cli --read-config --mode dummy --line line1 &`
   - While running, query: `python task_helper/data_test/data_simulator.py --status`
   - Expected: status="running", simulation_id populated, progress increasing

3. After simulation completes, query: `python task_helper/data_test/data_simulator.py --status`
   - Expected: status="completed", items_created > 0, progress=100
</verification>

<success_criteria>
- --status CLI flag exists and is documented in --help output
- Status JSON contains all 5 required fields (status, progress, items_created, simulation_id, last_activity)
- Status query works without blocking
- Status values correctly reflect actual simulation state
- Exit code 0 returned for successful status query
</success_criteria>

<output>
After completion, create `.planning/phases/25-simulator-status/25-01-SUMMARY.md`
</output>
