---
phase: 04-data-panel
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [skills_scripts/ui_automation/UiAutomation.cs, skills_scripts/ui_automation/Program.cs]
autonomous: true
---

<objective>
Read statistics values from ChronoView's StatisticsPanel using FlaUI.

Purpose: Extract file count and matching statistics displayed in the StatisticsPanel for automation verification.

Output: StatisticsPanel values can be read programmatically.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-toolbar-control/03-04-SUMMARY.md
@ChronoView/UI/Controls/StatisticsPanel.xaml
@skills_scripts/ui_automation/ChronoWindowFinder.cs
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add FindStatisticsPanel method</name>
  <files>skills_scripts/ui_automation/UiAutomation.cs</files>
  <action>Add FindStatisticsPanel(Window? mainWindow) method that:
1. Takes the MainWindow as parameter
2. Searches for a UserControl with Name containing "StatisticsPanel" or Class ending with "StatisticsPanel"
3. Uses FindFirstDescendant with ConditionFactory
4. Returns the AutomationElement or null
5. Logs search progress and results

The StatisticsPanel is a UserControl (lines 1-171 of StatisticsPanel.xaml).</action>
  <verify>dotnet build succeeds. Method compiles correctly.</verify>
  <done>FindStatisticsPanel method added to UiAutomation.cs</done>
</task>

<task type="auto">
  <name>Task 2: Add GetStatisticsValue helper method</name>
  <files>skills_scripts/ui_automation/UiAutomation.cs</files>
  <action>Add GetStatisticsValue(AutomationElement? panel, string labelText) method that:
1. Takes a panel element and the label text to find (e.g., "NIR1:", "Total:")
2. Finds the parent Border element containing the label
3. Finds the sibling TextBlock with the actual count value (the bold one after the label)
4. Returns the text content as string or null if not found
5. Logs the extraction process

The statistics use a Border with StackPanel containing a label TextBlock and a value TextBlock (see lines 19-27 of StatisticsPanel.xaml for NIR1 example pattern).</action>
  <verify>dotnet build succeeds. Method can extract text from statistics chips.</verify>
  <done>GetStatisticsValue method added to UiAutomation.cs</done>
</task>

<task type="auto">
  <name>Task 3: Add GetAllStatistics method and CLI command</name>
  <files>skills_scripts/ui_automation/UiAutomation.cs, skills_scripts/ui_automation/Program.cs</files>
  <action>Add GetAllStatistics(Window? mainWindow) method to UiAutomation that:
1. Calls FindStatisticsPanel
2. Extracts all known statistics using GetStatisticsValue:
   - File Counts: NIR1, Normal1, Cam1, Cam2, Cam3, NIR2, Normal2, Cam4, Cam5, Cam6
   - Matching Status: Total, WithNIR, WithoutNIR, Failed, Abnormal
3. Returns a Dictionary<string, string> or similar structure
4. Logs all extracted values

Add "stats" CLI command that:
1. Creates UiAutomation and ChronoWindowFinder instances
2. Finds MainWindow
3. Calls GetAllStatistics
4. Prints results in readable format or JSON (--json option)
5. Properly disposes resources</action>
  <verify>dotnet build succeeds. CLI stats command works when ChronoView is running.</verify>
  <done>GetAllStatistics method and stats CLI command implemented</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] dotnet build succeeds without errors
- [ ] FindStatisticsPanel can locate the StatisticsPanel
- [ ] GetStatisticsValue can extract individual values
- [ ] GetAllStatistics returns all statistics
- [ ] stats CLI command works
</verification>

<success_criteria>
- StatisticsPanel can be found programmatically
- Individual statistic values can be extracted
- All file counts and matching stats are readable via CLI
- Foundation ready for DataGrid automation
</success_criteria>

<output>
After completion, create `.planning/phases/04-data-panel/04-01-SUMMARY.md`
</output>
