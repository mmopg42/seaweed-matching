---
phase: 04-data-panel
plan: 03
type: execute
wave: 2
depends_on: ["04-01", "04-02"]
files_modified: [skills_scripts/ui_automation/ChronoDataPanelReader.cs, skills_scripts/ui_automation/Program.cs]
autonomous: true
---

<objective>
Implement comprehensive DataGrid data extraction for ChronoView.

Purpose: Extract all data from FileGroupDataGrid including GroupId, Status, and camera-specific file information.

Output: Complete DataGrid data extraction API and CLI integration.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-data-panel/04-01-SUMMARY.md
@.planning/phases/04-data-panel/04-02-SUMMARY.md
@ChronoView/UI/Controls/FileGroupDataGrid.xaml
@skills_scripts/ui_automation/ChronoWindowFinder.cs
</context>

<tasks>
<task type="auto">
  <name>Task 1: Create ChronoDataPanelReader class</name>
  <files>skills_scripts/ui_automation/ChronoDataPanelReader.cs</files>
  <action>Create ChronoDataPanelReader.cs with comprehensive data panel reading API:

1. Take UIA3Automation instance in constructor (follow ChronoWindowFinder pattern)
2. Have a private ChronoWindowFinder field
3. Methods:
   - FindDataGrid() - From UiAutomation/find by ControlType.DataGrid
   - GetDataGridHeaders() - List column headers
   - GetDataRowCount() - Count rows
   - GetCellText(rowElement, int columnIndex) - Extract cell value
   - GetRowData(AutomationElement rowElement) - Extract all cells as Dictionary
   - GetAllData() - Extract entire DataGrid as structured data

4. Data structure for GetAllData:
   - Return list of objects with GroupId, Status, and camera values
   - Include row index and selection state if available

Use namespace SkillsScripts.UiAutomation</action>
  <verify>dotnet build succeeds. ChronoDataPanelReader class compiles.</verify>
  <done>ChronoDataPanelReader.cs created with data extraction API</done>
</task>

<task type="auto">
  <name>Task 2: Implement GetCellText and GetRowData methods</name>
  <files>skills_scripts/ui_automation/ChronoDataPanelReader.cs</files>
  <action>Implement cell extraction methods:

GetCellText(AutomationElement rowElement, int columnIndex):
1. Find DataGridCell elements within the row
2. Access cell by index
3. Use TextPattern or Name property to get cell content
4. Return string value or empty string if not found

GetRowData(AutomationElement rowElement):
1. Call GetCellText for each column index
2. Map to column names (from headers)
3. Return Dictionary<string, string> with column-name -> cell-value pairs
4. Include special handling for checkbox column (selection state)

Note: DataGrid cells in WPF can be accessed via ValuePattern or TextPattern depending on cell type.</action>
  <verify>dotnet build succeeds. Cell extraction methods work correctly.</verify>
  <done>GetCellText and GetRowData methods implemented</done>
</task>

<task type="auto">
  <name>Task 3: Add CLI commands and GetAllData</name>
  <files>skills_scripts/ui_automation/ChronoDataPanelReader.cs, skills_scripts/ui_automation/Program.cs</files>
  <action>Implement GetAllData method and CLI integration:

GetAllData():
1. Find MainWindow via ChronoWindowFinder
2. Find DataGrid element
3. Get all row elements
4. For each row, call GetRowData
5. Return structured list with all data

CLI "datagrid" command with subcommands:
- "datagrid info" - Show headers and row count
- "datagrid rows" - List all row data (GroupId, Status, etc.)
- "datagrid cell <row> <col>" - Get specific cell value
- "datagrid export" - Export all data as JSON

Include --json option for programmatic output on all subcommands.</action>
  <verify>dotnet build succeeds. All datagrid CLI commands work.</verify>
  <done>GetAllData method and datagrid CLI commands implemented</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] dotnet build succeeds without errors
- [ ] ChronoDataPanelReader class exists with all methods
- [ ] Cell extraction works for all column types
- [ ] GetAllData returns complete DataGrid data
- [ ] datagrid CLI commands work correctly
</verification>

<success_criteria>
- ChronoDataPanelReader provides complete data panel API
- All DataGrid data can be extracted programmatically
- CLI commands enable easy data access
- Phase 4 complete - ready for Phase 5 (WorkflowPanel)
</success_criteria>

<output>
After completion, create `.planning/phases/04-data-panel/04-03-SUMMARY.md`
</output>
