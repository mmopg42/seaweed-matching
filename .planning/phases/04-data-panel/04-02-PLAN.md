---
phase: 04-data-panel
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [skills_scripts/ui_automation/UiAutomation.cs, skills_scripts/ui_automation/Program.cs]
autonomous: true
---

<objective>
Analyze and understand FileGroupDataGrid structure for data extraction.

Purpose: Inspect the DataGrid structure to understand how rows, columns, and cells are organized for automation.

Output: FileGroupDataGrid structure is documented and cells can be accessed.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-data-panel/04-01-SUMMARY.md
@ChronoView/UI/Controls/FileGroupDataGrid.xaml
@skills_scripts/ui_automation/UiAutomation.cs
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add FindDataGrid method</name>
  <files>skills_scripts/ui_automation/UiAutomation.cs</files>
  <action>Add FindDataGrid(Window? mainWindow) method that:
1. Takes MainWindow as parameter
2. Searches for DataGrid control type (ControlType.DataGrid)
3. Optionally filters by Name ("MainDataGrid" from line 18 of FileGroupDataGrid.xaml)
4. Returns the DataGrid AutomationElement or null
5. Logs the search progress

Note: WPF DataGrid appears as ControlType.DataGrid in UI Automation.</action>
  <verify>dotnet build succeeds. Method can find the DataGrid element.</verify>
  <done>FindDataGrid method added to UiAutomation.cs</done>
</task>

<task type="auto">
  <name>Task 2: Add GetDataGridHeaders method</name>
  <files>skills_scripts/ui_automation/UiAutomation.cs</files>
  <action>Add GetDataGridHeaders(AutomationElement? dataGrid) method that:
1. Takes a DataGrid element
2. Finds all DataGridColumnHeader elements within the grid
3. Extracts the header text (Name property) from each
4. Returns List<string> of column headers or empty list if none found
5. Logs the discovered headers

The columns include: Checkbox (column 0), Index, Status, plus dynamic columns (lines 39-96 show static columns, line 95 indicates dynamic columns added by code-behind).</action>
  <verify>dotnet build succeeds. Method can list DataGrid column headers.</verify>
  <done>GetDataGridHeaders method added to UiAutomation.cs</done>
</task>

<task type="auto">
  <name>Task 3: Add GetDataRowCount method</name>
  <files>skills_scripts/ui_automation/UiAutomation.cs</files>
<action>Add GetDataRowCount(AutomationElement? dataGrid) method that:
1. Takes a DataGrid element
2. Finds all DataGridRow elements (DataGrid.Rows or descendant search)
3. Returns the count of rows
4. Logs the row count

Also add GetDataRow(AutomationElement dataGrid, int index) method that:
1. Gets row at specific index (0-based)
2. Returns the row AutomationElement or null

Add "datagrid info" CLI command that:
1. Finds the DataGrid
2. Calls GetDataGridHeaders to list columns
3. Calls GetDataRowCount to show row count
4. Outputs the structure information</action>
  <verify>dotnet build succeeds. Methods can inspect DataGrid structure.</verify>
  <done>GetDataRowCount, GetDataRow methods and datagrid info CLI command implemented</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] dotnet build succeeds without errors
- [ ] FindDataGrid locates the DataGrid element
- [ ] GetDataGridHeaders lists all columns
- [ ] GetDataRowCount returns correct row count
- [ ] datagrid info CLI command works
</verification>

<success_criteria>
- DataGrid element can be found programmatically
- Column headers can be listed
- Row count can be retrieved
- DataGrid structure is understood for data extraction
</success_criteria>

<output>
After completion, create `.planning/phases/04-data-panel/04-02-SUMMARY.md`
</output>
