---
phase: 28-skill-registry-definition
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/agents/test-executor-skills.md
autonomous: true

must_haves:
  truths:
    - "All 90+ CLI commands have corresponding skill definitions in test-executor-skills.md"
    - "Each skill has semantic name (intent-based, UPPER_SNAKE_CASE format)"
    - "Skill definition includes exact CLI command pattern"
    - "Skill definition includes parameter schema (TypeScript interface)"
    - "Skills organized by category (APP, BATCH, CONSOLE_LOGS, DATA_PANEL, FILE_OPS, LOGS, SETTINGS_DIALOG, SETUP, TEST, TOOLBAR, UTILITY, WINDOWS, WORKFLOW)"
    - "Registry document is discoverable and searchable"
  artifacts:
    - path: ".claude/agents/test-executor-skills.md"
      provides: "Complete skill registry documentation"
      min_lines: 600
  key_links:
    - from: "test-orchestrator.md"
      to: "test-executor-skills.md"
      via: "Delegation references skill names from registry"
      pattern: "test-executor-skills"
    - from: "test-executor.md"
      to: "test-executor-skills.md"
      via: "Executor uses skill definitions for CLI translation"
      pattern: "See test-executor-skills.md"
---

<objective>
Create semantic skill definitions for all 90+ CLI commands in the ChronoView automation CLI.

Purpose: Establish a skill registry that provides intent-based names for all CLI commands. This creates a clean abstraction layer where AI agents use semantic skill names (what to do) rather than constructing CLI commands directly (how to do it).

Output: A new `.claude/agents/test-executor-skills.md` file containing:
- Skill overview table with all skills organized by category
- Detailed skill definitions with TypeScript interfaces
- CLI command mappings for each skill
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/28-skill-registry-definition/28-CONTEXT.md
@.claude/agents/test-executor.md
</context>

<tasks>

<task type="auto">
  <name>Create skill registry document structure</name>
  <files>.claude/agents/test-executor-skills.md</files>
  <action>
    Create test-executor-skills.md with the following structure:

    1. Document header with purpose and usage notes
    2. Skill overview table (all skills in one table for quick reference)
    3. Category sections (13 total) with detailed skill definitions

    Document header should include:
    - Purpose: "Semantic skill registry for ChronoView CLI automation"
    - Usage: "test-orchestrator delegates by skill name, test-executor translates to CLI"
    - Skill format: UPPER_SNAKE_CASE (CATEGORY_ACTION)
    - Reference: Link back to test-executor.md for execution details

    Skill overview table columns:
    | Skill | Description | Category | CLI Command |

    Category sections (13 categories, alphabetical):
    - APP
    - BATCH
    - CONSOLE_LOGS
    - DATA_PANEL
    - FILE_OPS
    - LOGS
    - SETTINGS_DIALOG
    - SETUP
    - TEST
    - TOOLBAR
    - UTILITY
    - WINDOWS
    - WORKFLOW

    Do NOT include legacy/deprecated commands (detect, list, find, click).
  </action>
  <verify>File exists with header and section structure</verify>
  <done>test-executor-skills.md created with proper structure</done>
</task>

<task type="auto">
  <name>Populate APP, BATCH, CONSOLE_LOGS, DATA_PANEL skills</name>
  <files>.claude/agents/test-executor-skills.md</files>
  <action>
    Populate skills for first 4 categories:

    ## APP Category (4 skills)
    - APP_LAUNCH: Launch ChronoView application
    - APP_STOP: Stop all ChronoView processes
    - APP_RESTART: Restart ChronoView (stop + launch)
    - APP_STATUS: Check ChronoView running status

    ## BATCH Category (3 skills)
    - BATCH_SELECT_AND_MOVE: Select multiple rows and move them
    - BATCH_SELECT_AND_DELETE: Select multiple rows and delete them
    - BATCH_EXPORT_ALL: Export all available data

    ## CONSOLE_LOGS Category (3 skills)
    - CONSOLE_LOGS_LIST: List available console log files
    - CONSOLE_LOGS_TAIL: Read recent N lines from console log
    - CONSOLE_LOGS_SEARCH: Search console logs for text

    ## DATA_PANEL Category (7 skills)
    - DATA_PANEL_STATS: Read StatisticsPanel data
    - DATA_PANEL_HEADERS: Get DataGrid column headers
    - DATA_PANEL_ROWS: Get DataGrid row count
    - DATA_PANEL_DATA: Extract all DataGrid data
    - DATA_PANEL_INFO: Get DataGrid summary (headers + row count)
    - DATA_PANEL_CELL: Get specific cell value
    - DATA_PANEL_EXPORT: Export DataGrid as JSON

    Each skill definition includes:
    - skill: UPPER_SNAKE_CASE name
    - desc: Intent-based description
    - cli: Exact CLI command pattern
    - params: TypeScript interface for parameters
    - returns: TypeScript interface for response

    Example format:
    ```typescript
    {
      skill: "APP_LAUNCH",
      desc: "Launch ChronoView application using dotnet run",
      cli: "app launch [--json]",
      params: {
        json?: boolean;  // Output in JSON format
      },
      returns: {
        success: boolean;
        data?: {
          launched: boolean;
          processId: number;
        };
      }
    }
    ```
  </action>
  <verify>All 17 skills defined with complete schemas</verify>
  <done>APP, BATCH, CONSOLE_LOGS, DATA_PANEL skills documented</done>
</task>

<task type="auto">
  <name>Populate FILE_OPS, LOGS, SETTINGS_DIALOG skills</name>
  <files>.claude/agents/test-executor-skills.md</files>
  <action>
    Populate skills for next 3 categories:

    ## FILE_OPS Category (15 skills)
    - FILE_OPS_SELECT_ROW_INDEX: Select row by index
    - FILE_OPS_SELECT_GROUP_ID: Select row by GroupId
    - FILE_OPS_SELECT_PREFIX: Select rows by GroupId prefix
    - FILE_OPS_SELECT_ALL: Select all rows
    - FILE_OPS_CLEAR_SELECTION: Clear row selection
    - FILE_OPS_SELECTED: Get selected row indices
    - FILE_OPS_MOVE_ROWS: Select and move by row indices
    - FILE_OPS_MOVE_GROUP_IDS: Select and move by GroupIds
    - FILE_OPS_MOVE_PREFIX: Select and move by prefix
    - FILE_OPS_DELETE_ROWS: Select and delete by row indices
    - FILE_OPS_DELETE_GROUP_IDS: Select and delete by GroupIds
    - FILE_OPS_WAIT_MOVE: Wait for move operation completion
    - FILE_OPS_WAIT_DELETE: Wait for delete operation completion
    - FILE_OPS_CONFIRM: Handle delete confirmation dialog
    - FILE_OPS_VERIFY_DELETED: Verify group was deleted
    - FILE_OPS_VERIFY_ROW_COUNT: Verify row count changed

    ## LOGS Category (4 skills)
    - LOGS_GET: Get all LogPanel messages
    - LOGS_TAIL: Get recent N log messages
    - LOGS_FILTER: Filter logs by severity level
    - LOGS_SEARCH: Search log messages for text

    ## SETTINGS_DIALOG Category (17 skills)
    - SETTINGS_DIALOG_OPEN: Open SettingsDialog
    - SETTINGS_DIALOG_CLOSE: Close SettingsDialog
    - SETTINGS_DIALOG_INSPECT: Inspect dialog structure
    - SETTINGS_DIALOG_STATUS: Check if dialog is open
    - SETTINGS_DIALOG_PATH_GET_ALL: Get all configured paths
    - SETTINGS_DIALOG_PATH_GET_LINE1: Get Line 1 paths
    - SETTINGS_DIALOG_PATH_GET_LINE2: Get Line 2 paths
    - SETTINGS_DIALOG_PATH_GET_OUTPUT: Get output path
    - SETTINGS_DIALOG_PATH_GET_QUARANTINE: Get quarantine path
    - SETTINGS_DIALOG_PATH_SET: Set a path value
    - SETTINGS_DIALOG_CHECKBOX_GET: Get checkbox state
    - SETTINGS_DIALOG_CHECKBOX_SET: Set checkbox state
    - SETTINGS_DIALOG_CHECKBOX_LIST: List all checkboxes
    - SETTINGS_DIALOG_ACTION_SAVE: Click Save/OK button
    - SETTINGS_DIALOG_ACTION_APPLY: Click Apply button
    - SETTINGS_DIALOG_ACTION_CANCEL: Click Cancel button
    - SETTINGS_DIALOG_ACTION_RESET: Click Reset button

    Each skill follows the same TypeScript interface format as task 2.
  </action>
  <verify>All 36 skills defined with complete schemas</verify>
  <done>FILE_OPS, LOGS, SETTINGS_DIALOG skills documented</done>
</task>

<task type="auto">
  <name>Populate SETUP, TEST, TOOLBAR, UTILITY skills</name>
  <files>.claude/agents/test-executor-skills.md</files>
  <action>
    Populate skills for next 4 categories:

    ## SETUP Category (4 skills)
    - SETUP_VERIFY_CONFIG: Verify simulator vs ChronoView config
    - SETUP_COMPLETE_FULL: Complete full setup workflow
    - SETUP_OPEN_SETTINGS: Open SettingsDialog from SetupWindow
    - SETUP_CAMERA_STATES: Get camera button states

    ## TEST Category (3 skills)
    - TEST_CONNECTIVITY: Check ChronoView connection
    - TEST_CAPABILITIES: List available automation capabilities
    - TEST_DATAGRID: Check DataGrid accessibility

    ## TOOLBAR Category (9 skills)
    - TOOLBAR_START: Click Start button
    - TOOLBAR_STOP: Click Stop button
    - TOOLBAR_SETTINGS: Click Settings button
    - TOOLBAR_REFRESH: Click Refresh button
    - TOOLBAR_MOVE: Click Move button
    - TOOLBAR_DELETE: Click Delete button
    - TOOLBAR_LIST: List all toolbar buttons
    - TOOLBAR_CLICK: Click button by text
    - TOOLBAR_ENABLED: Check if button is enabled

    ## UTILITY Category (5 skills)
    - UTILITY_INSPECT_WORKFLOW: Inspect WorkflowPanel structure
    - UTILITY_INSPECT_LOG: Inspect LogPanel structure
    - UTILITY_CONFIG_PATH: Get config file location
    - UTILITY_CONFIG_READ: Read config file contents
    - UTILITY_CONFIG_GET: Get specific config value

    Each skill follows the same TypeScript interface format.
  </action>
  <verify>All 21 skills defined with complete schemas</verify>
  <done>SETUP, TEST, TOOLBAR, UTILITY skills documented</done>
</task>

<task type="auto">
  <name>Populate WINDOWS, WORKFLOW skills and finalize document</name>
  <files>.claude/agents/test-executor-skills.md</files>
  <action>
    Populate skills for final 2 categories and finalize the document:

    ## WINDOWS Category (6 skills)
    - WINDOWS_MAIN: Find MainWindow
    - WINDOWS_SETUP: Find SetupWindow
    - WINDOWS_SETUP_COMPLETE: Complete SetupWindow and go to MainWindow
    - WINDOWS_SETTINGS: Find SettingsDialog
    - WINDOWS_PREVIEW: Find ImagePreviewWindow
    - WINDOWS_ALL: List all ChronoView windows

    ## WORKFLOW Category (11 skills)
    - WORKFLOW_LAUNCH_GENERAL: Launch General Camera
    - WORKFLOW_LAUNCH_NIR: Launch NIR 1 Camera
    - WORKFLOW_LAUNCH_NIR2: Launch NIR 2 Camera
    - WORKFLOW_TOGGLE_FILTERING: Toggle NIR filtering
    - WORKFLOW_CAMERA_STATES: Get all camera states
    - WORKFLOW_PATH_GET_LINE1: Get Line 1 paths
    - WORKFLOW_PATH_GET_LINE2: Get Line 2 paths
    - WORKFLOW_PATH_GET_ALL: Get all workflow paths
    - WORKFLOW_PATH_SET_LINE1: Set Line 1 path
    - WORKFLOW_PATH_SET_LINE2: Set Line 2 path
    - WORKFLOW_SELECT_TAB: Select a tab in MainWindow

    After populating all skills, update the overview table to include all 90+ skills.

    Add footer with:
    - Total skill count
    - Category breakdown
    - Version note
    - Related documents (test-executor.md, test-orchestrator.md)
  </action>
  <verify>All 17 skills defined, overview table complete, footer added</verify>
  <done>WINDOWS, WORKFLOW skills documented, document finalized</done>
</task>

</tasks>

<verification>
1. Read test-executor-skills.md and verify:
   - Document has proper structure (header, overview table, category sections, footer)
   - All 13 categories are present
   - Overview table contains all 90+ skills
   - Each skill has: skill (UPPER_SNAKE_CASE), desc, cli, params, returns
   - TypeScript interfaces use correct syntax
   - CLI command patterns match actual commands from source code

2. Run grep to count skills:
   ```bash
   grep -c "skill:" .claude/agents/test-executor-skills.md
   ```
   Should be 90+

3. Verify no legacy commands included (detect, list, find, click as standalone)

4. Cross-reference with command source files to ensure completeness
</verification>

<success_criteria>
1. test-executor-skills.md created at .claude/agents/test-executor-skills.md
2. All 90+ CLI commands have corresponding skill definitions
3. All skill names follow UPPER_SNAKE_CASE format (CATEGORY_ACTION)
4. All skills include parameter schemas in TypeScript interface format
5. Skills organized by 13 categories (APP, BATCH, CONSOLE_LOGS, DATA_PANEL, FILE_OPS, LOGS, SETTINGS_DIALOG, SETUP, TEST, TOOLBAR, UTILITY, WINDOWS, WORKFLOW)
6. Overview table provides quick reference for all skills
7. Document is ready for reference by test-orchestrator (phase 29) and test-executor (phase 30)
</success_criteria>

<output>
After completion, create `.planning/phases/28-skill-registry-definition/28-01-SUMMARY.md`
</output>
