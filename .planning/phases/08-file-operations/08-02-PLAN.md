---
phase: 08-file-operations
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [skills_scripts/ui_automation/ChronoFileOperationsController.cs, skills_scripts/ui_automation/Program.cs]
autonomous: true
---

<objective>
Implement FileGroup delete operation automation with confirmation dialog handling.

Purpose: Enable automated testing of file delete operations through UI Automation.
Output: Delete methods added to ChronoFileOperationsController, plus CLI delete commands.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-toolbar-control/03-04-SUMMARY.md
@.planning/phases/04-data-panel/04-03-SUMMARY.md
@skills_scripts/ui_automation/ChronoDataPanelReader.cs
@skills_scripts/ui_automation/ChronoToolbarController.cs
@ChronoView/UI/ViewModels/FileOperationViewModel.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement delete operation automation</name>
  <files>skills_scripts/ui_automation/ChronoFileOperationsController.cs</files>
  <action>
Add delete operation methods to ChronoFileOperationsController:
- ClickDeleteButton() - Use ChronoToolbarController pattern to click "삭제" button
- SelectAndDeleteRows(int[] rowIndices) - Select multiple rows, click Delete
- SelectAndDeleteByGroupIds(string[] groupIds) - Select rows by GroupId, click Delete
- HandleDeleteConfirmationDialog() - Find and click confirmation dialog button
- WaitForDeleteComplete(int timeoutMs = 30000) - Wait for operation completion

Confirmation dialog handling:
- Delete operations may show a confirmation dialog (MessageBox)
- Find dialog by Name containing "삭제" (Delete) or "확인" (Confirm)
- Dialog has Yes/No or OK/Cancel buttons
- Click "예" (Yes) or "확인" (OK) to confirm deletion
- Use WaitForWindowClosed() pattern after confirmation if dialog can be detected

Reference FileOperationViewModel.cs:119-185 for delete operation flow.
  </action>
  <verify>Build succeeds, delete methods follow established button click patterns</verify>
  <done>Delete operation methods implemented with dialog handling</done>
</task>

<task type="auto">
  <name>Task 2: Implement verification methods for deletion</name>
  <files>skills_scripts/ui_automation/ChronoFileOperationsController.cs</files>
  <action>
Add verification methods to confirm deletion completed:
- GetDataRowCountAfterOperation() - Re-read DataGrid row count after operation
- VerifyGroupDeleted(string groupId) - Check if GroupId no longer exists in DataGrid
- WaitForRowCountChange(int originalCount, int timeoutMs = 5000) - Poll for row count change

Implementation:
- Reuse ChronoDataPanelReader.GetDataRowCount() pattern
- Search DataGrid for GroupId column value using existing cell access patterns
- Use polling with 200ms interval (DefaultPollIntervalMs)

These methods allow tests to verify delete operations actually removed rows.
  </action>
  <verify>Build succeeds, verification methods compile</verify>
  <done>Verification methods implemented for post-delete checks</done>
</task>

<task type="auto">
  <name>Task 3: Add CLI delete commands</name>
  <files>skills_scripts/ui_automation/Program.cs</files>
  <action>
Add CLI subcommands to existing "file-ops" group:
- file-ops delete --rows <indices> - Select rows and click Delete
- file-ops delete --group-ids <ids> - Select by GroupId and click Delete
- file-ops delete --force - Skip confirmation dialog (if supported)
- file-ops verify-deleted <groupId> - Verify group was removed from DataGrid

Follow existing CLI pattern from Program.cs.
Add to existing file-ops command group, do not create new top-level command.
All commands support --json option for JSON output.

Use ChronoFileOperationsController instance within command handlers.
Reuse select methods from 08-01 for row selection before delete.
  </action>
  <verify>Build succeeds, CLI delete commands invokable</verify>
  <done>CLI delete commands added to file-ops group</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] dotnet build succeeds without errors
- [ ] Delete methods handle confirmation dialogs
- [ ] CLI delete commands are invokable (use --help to verify)
- [ ] Verification methods can confirm row removal
</verification>

<success_criteria>

- Delete operation methods added to ChronoFileOperationsController
- Confirmation dialog handling implemented
- Verification methods confirm deletion results
- CLI delete commands complete the file-ops group
- Phase 8 (file-operations) complete
  </success_criteria>

<output>
After completion, create `.planning/phases/08-file-operations/08-02-SUMMARY.md`
</output>
