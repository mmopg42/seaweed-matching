---
phase: 08-file-operations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [skills_scripts/ui_automation/ChronoFileOperationsController.cs, skills_scripts/ui_automation/Program.cs]
autonomous: true
---

<objective>
Implement FileGroup selection and move operation automation for ChronoView.

Purpose: Enable automated testing of file move operations through UI Automation.
Output: ChronoFileOperationsController class with select and move methods, plus CLI commands.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-toolbar-control/03-04-SUMMARY.md
@.planning/phases/04-data-panel/04-03-SUMMARY.md
@skills_scripts/ui_automation/ChronoDataPanelReader.cs
@skills_scripts/ui_automation/ChronoToolbarController.cs
@ChronoView/UI/ViewModels/FileOperationViewModel.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ChronoFileOperationsController class skeleton</name>
  <files>skills_scripts/ui_automation/ChronoFileOperationsController.cs</files>
  <action>
Create ChronoFileOperationsController following established patterns:
- Namespace: SkillsScripts.UiAutomation
- Constructor: UIA3Automation injection + parameterless convenience constructor
- Field: private readonly UIA3Automation _automation
- Field: private readonly ChronoWindowFinder _windowFinder
- Implement IDisposable for _automation cleanup
- Add XML documentation comments

Follow ChronoDataPanelReader.cs:16-37 pattern for constructor structure.
Follow ChronoToolbarController.cs:33-55 pattern for class layout.
  </action>
  <verify>File created with proper using statements (FlaUI.Core, FlaUI.Core.AutomationElements, FlaUI.Core.Definitions, FlaUI.Core.Conditions, FlaUI.UIA3, System)</verify>
  <done>ChronoFileOperationsController.cs exists with proper structure</done>
</task>

<task type="auto">
  <name>Task 2: Implement DataGrid row selection methods</name>
  <files>skills_scripts/ui_automation/ChronoFileOperationsController.cs</files>
  <action>
Add methods for selecting FileGroups in the DataGrid:
- FindDataGrid() - Reuse ChronoDataPanelReader pattern to find MainDataGrid
- SelectRowByIndex(AutomationElement dataGrid, int rowIndex) - Select a single row
- SelectRowByGroupId(AutomationElement dataGrid, string groupId) - Find row by GroupId column value, then select
- SelectAllRows(AutomationElement dataGrid) - Click SelectAll checkbox in header
- ClearSelection(AutomationElement dataGrid) - Deselect all rows
- GetSelectedRows(AutomationElement dataGrid) - Return list of selected row indices

Selection implementation:
- DataGrid rows are ControlType.DataItem
- Each row has a CheckBox in first column (column index 0)
- Use SelectionItemPattern for programmatic selection if available
- Fallback: Click CheckBox element in row using InvokePattern

Follow FileGroupDataGrid.xaml:40-63 for checkbox structure.
  </action>
  <verify>Build succeeds, methods compile with proper FlaUI patterns</verify>
  <done>Selection methods implemented following DataGrid patterns from Phase 4</done>
</task>

<task type="auto">
  <name>Task 3: Implement move operation automation</name>
  <files>skills_scripts/ui_automation/ChronoFileOperationsController.cs</files>
  <action>
Add move operation methods:
- ClickMoveButton() - Use ChronoToolbarController pattern to click "이동" button
- SelectAndMoveRows(int[] rowIndices) - Select multiple rows, click Move button
- SelectAndMoveByGroupIds(string[] groupIds) - Select rows by GroupId, click Move
- WaitForMoveComplete(int timeoutMs = 30000) - Wait for IsOperationInProgress state

Implementation:
- ChronoToolbarController already has ClickMoveButton() - reuse or replicate
- After clicking Move button, MainWindow may show progress dialog
- Wait for operation completion by checking button enabled state or progress dialog closure
- Use Thread.Sleep with 200ms poll interval (DefaultPollIntervalMs pattern)

Reference FileOperationViewModel.cs:45-117 for move operation flow.
  </action>
  <verify>Build succeeds, move methods follow ChronoToolbarController button click pattern</verify>
  <done>Move operation methods implemented with proper wait handling</done>
</task>

<task type="auto">
  <name>Task 4: Add CLI commands for file operations</name>
  <files>skills_scripts/ui_automation/Program.cs</files>
  <action>
Add CLI command group "file-ops" with subcommands:
- file-ops select --row-index <index> - Select row by index
- file-ops select --group-id <id> - Select row by GroupId
- file-ops select-all - Select all rows
- file-ops clear-selection - Deselect all rows
- file-ops move --rows <indices> - Select rows and click Move
- file-ops move --group-ids <ids> - Select by GroupId and click Move
- file-ops selected - List selected row indices

Follow existing CLI pattern from Program.cs datagrid and logs commands.
Use System.CommandLine RootCommand with Command[] array.
All commands support --json option for JSON output.

Create ChronoFileOperationsController instance within command handlers.
  </action>
  <verify>Build succeeds, CLI commands registered and invokable</verify>
  <done>CLI commands added for file-ops operations</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] dotnet build succeeds without errors
- [ ] ChronoFileOperationsController follows established patterns
- [ ] CLI commands are invokable (use --help to verify)
</verification>

<success_criteria>

- ChronoFileOperationsController class created with selection and move methods
- DataGrid row selection works via checkbox clicking
- Move button click automation implemented
- CLI commands provide test interface for file operations
- All code follows ChronoDataPanelReader and ChronoToolbarController patterns
  </success_criteria>

<output>
After completion, create `.planning/phases/08-file-operations/08-01-SUMMARY.md`
</output>
