---
phase: 16-file-ops-commands
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [skills_scripts/ui_automation/Commands/FileOpsCommands.cs, skills_scripts/ui_automation/Program.cs]
autonomous: true
---

<objective>
Extract all `file-ops/*` commands from Program.cs to FileOpsCommands handler module.

Purpose: Continue Program.cs refactoring by extracting file operations commands (select, move, delete, wait, confirm, verify) to a focused handler class. This reduces Program.cs from 2045 to approximately 1650 lines (~400 line reduction).

Output: FileOpsCommands class with ~290 lines covering all file-ops operations, registered via CommandRegistry.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior command extraction patterns established:
@.planning/phases/11-commands-architecture/11-01-SUMMARY.md
@.planning/phases/14-data-panel-commands/14-01-SUMMARY.md
@.planning/phases/15-workflow-settings-commands/15-01-SUMMARY.md

# Existing infrastructure to use:
@skills_scripts/ui_automation/Commands/ICommandHandler.cs
@skills_scripts/ui_automation/Commands/CommandRegistry.cs
@skills_scripts/ui_automation/ChronoFileOperationsController.cs

# Source to extract from:
@skills_scripts/ui_automation/Program.cs (lines 841-1131 contain file-ops commands)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create FileOpsCommands class shell with ICommandHandler</name>
  <files>skills_scripts/ui_automation/Commands/FileOpsCommands.cs</files>
  <action>Create FileOpsCommands.cs in Commands/ directory with:
  1. namespace UiAutomation.Commands
  2. using FileOps = SkillsScripts.UiAutomation.ChronoFileOperationsController
  3. using System.CommandLine
  4. class FileOpsCommands : ICommandHandler
  5. RegisterCommands(RootCommand rootCommand) method with empty body
  6. XML summary comment describing file-ops command group

  DO NOT add any command implementations yet - just the shell structure.</action>
  <verify>dotnet build skills_scripts/ui_automation/ui_automation.csproj succeeds</verify>
  <done>FileOpsCommands.cs created with ICommandHandler interface, empty RegisterCommands method</done>
</task>

<task type="auto">
  <name>Task 2: Extract all file-ops commands to FileOpsCommands</name>
  <files>skills_scripts/ui_automation/Commands/FileOpsCommands.cs</files>
  <action>Copy the entire file-ops command section from Program.cs (lines 841-1131) to FileOpsCommands.RegisterCommands method.

  Commands to extract:
  1. file-ops select (with row-index and group-id subcommands)
  2. file-ops select-all
  3. file-ops clear-selection
  4. file-ops selected
  5. file-ops move (with rows and group-ids subcommands)
  6. file-ops delete (with rows and group-ids subcommands)
  7. file-ops wait (with move and delete subcommands)
  8. file-ops confirm
  9. file-ops verify (with deleted and row-count subcommands)

  Pattern to follow from WorkflowCommands.cs:
  - Use "using FileOps = SkillsScripts.UiAutomation.ChronoFileOperationsController;" alias
  - Create new FileOps() instances in each handler
  - Keep all console output messages unchanged
  - Keep all command structures and options identical

  DO NOT refactor the command logic - copy verbatim.</action>
  <verify>dotnet build skills_scripts/ui_automation/ui_automation.csproj succeeds</verify>
  <done>All 9 file-ops command groups extracted to FileOpsCommands with identical behavior</done>
</task>

<task type="auto">
  <name>Task 3: Register FileOpsCommands in Program.cs and remove extracted commands</name>
  <files>skills_scripts/ui_automation/Program.cs</files>
  <action>In Program.cs Main method:
  1. After the WorkflowCommands registration line, add:
     registry.RegisterHandler(new FileOpsCommands());
  2. Delete the entire file-ops command section (lines 841-1131)
  3. Verify no other code references the removed commands

  Expected result: Program.cs reduces from 2045 to approximately 1650 lines (~395 line reduction).</action>
  <verify>
  1. dotnet build skills_scripts/ui_automation/ui_automation.csproj succeeds
  2. ./bin/Debug/net10.0-windows/ui_automation.exe file-ops --help shows all subcommands
  3. powershell -Command "(Get-Content 'skills_scripts\ui_automation\Program.cs').Count" shows ~1650 lines
  </verify>
  <done>FileOpsCommands registered, file-ops commands removed from Program.cs, line count reduced by ~395</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] dotnet build succeeds with no errors
- [ ] `./bin/Debug/net10.0-windows/ui_automation.exe file-ops --help` displays all subcommands (select, select-all, clear-selection, selected, move, delete, wait, confirm, verify)
- [ ] `./bin/Debug/net10.0-windows/ui_automation.exe --help` includes file-ops in command list
- [ ] Program.cs line count is approximately 1650 lines
- [ ] All file-ops subcommands have their help text displayed
</verification>

<success_criteria>

- FileOpsCommands class created with ICommandHandler implementation
- All 9 file-ops command groups extracted (select, select-all, clear-selection, selected, move, delete, wait, confirm, verify)
- Commands use ChronoFileOperationsController API directly via FileOps alias
- Program.cs reduced from 2045 to approximately 1650 lines
- All CLI commands work identically to before extraction
- FileOpsCommands registered via CommandRegistry pattern
  </success_criteria>

<output>
After completion, create `.planning/phases/16-file-ops-commands/16-01-SUMMARY.md` following the template from .planning/phases/15-workflow-settings-commands/15-01-SUMMARY.md
</output>
