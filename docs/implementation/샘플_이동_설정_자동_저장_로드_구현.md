# 샘플 이동 설정 자동 저장/로드 구현 문서

## 개요

시료명(SampleName), NIR 이동(MoveNir), 전체 데이터 이동(MoveAllData) 필드의 값이 변경되면 자동으로 저장하고, 애플리케이션 시작 시 이전 값을 불러오도록 구현했습니다.

**구현 일자**: 2025-01-06  
**관련 계획서**: `c:\Users\redli\.cursor\plans\샘플_이동_설정_자동_저장_로드_계획_89a64a6f.plan.md`

## 구현 내용

### 1. ApplicationConfiguration에 SampleName 필드 추가

**파일**: `ChronoView/Models/ApplicationConfiguration.cs`

**변경 위치**: `MatchingSettings` 클래스 내부 (Line 152 근처)

**추가된 코드**:
```csharp
/// <summary>
/// Sample name/subject for file operations.
/// </summary>
public string? SampleName { get; set; } = null;
```

**설명**: 
- `MatchingSettings`에 `SampleName` 필드를 추가하여 시료명을 설정 파일에 저장할 수 있도록 했습니다.
- nullable string 타입으로, 값이 없을 때는 null로 저장됩니다.

### 2. MainWindowViewModel 필드 및 서비스 추가

**파일**: `ChronoView/UI/ViewModels/MainWindowViewModel.cs`

**추가된 필드** (Line 72-76):
```csharp
private readonly IConfigurationManager _configManager;
private System.Windows.Threading.DispatcherTimer? _saveDebounceTimer;
private readonly object _saveLock = new object();
private bool _isLoadingSettings = false;
```

**설명**:
- `_configManager`: 설정 저장/로드를 위한 ConfigurationManager 인스턴스
- `_saveDebounceTimer`: 디바운싱을 위한 타이머 (500ms 지연)
- `_saveLock`: 타이머 조작 시 스레드 안전성을 위한 lock 객체
- `_isLoadingSettings`: 설정 로드 중 자동 저장을 방지하기 위한 플래그

### 3. 생성자 수정

**파일**: `ChronoView/UI/ViewModels/MainWindowViewModel.cs`

**변경 내용** (Line 131):
```csharp
{
    _logger = logger;
    _configManager = config; // 추가
    Dashboard = new DashboardViewModel(...);
    // ...
}
```

**설명**: 생성자에서 `config` 파라미터를 `_configManager` 필드에 저장하여 이후 사용할 수 있도록 했습니다.

### 4. 속성 setter 수정 - 자동 저장 로직 추가

**파일**: `ChronoView/UI/ViewModels/MainWindowViewModel.cs`

**변경 내용** (Line 43-75):
```csharp
private string _sampleName = ""; 
public string SampleName 
{ 
    get => _sampleName; 
    set 
    { 
        if (SetProperty(ref _sampleName, value) && !_isLoadingSettings)
        {
            _ = SaveSampleMoveSettingsAsync();
        }
    }
}

private string _moveNir = ""; 
public string MoveNir 
{ 
    get => _moveNir; 
    set 
    { 
        if (SetProperty(ref _moveNir, value) && !_isLoadingSettings)
        {
            _ = SaveSampleMoveSettingsAsync();
        }
    }
}

private string _moveAllData = ""; 
public string MoveAllData 
{ 
    get => _moveAllData; 
    set 
    { 
        if (SetProperty(ref _moveAllData, value) && !_isLoadingSettings)
        {
            _ = SaveSampleMoveSettingsAsync();
        }
    }
}
```

**설명**:
- 각 속성의 setter에서 값이 변경되고 `_isLoadingSettings`가 false일 때만 `SaveSampleMoveSettingsAsync()`를 호출합니다.
- `SetProperty`가 true를 반환하면 값이 실제로 변경된 것이므로 저장을 트리거합니다.
- `_isLoadingSettings` 플래그로 설정 로드 중에는 자동 저장이 발생하지 않도록 방지합니다.

### 5. SaveSampleMoveSettingsAsync 메서드 추가

**파일**: `ChronoView/UI/ViewModels/MainWindowViewModel.cs`

**추가 위치**: `LoadSettings()` 메서드 이후 (Line 201-245)

**구현 내용**:
```csharp
private async Task SaveSampleMoveSettingsAsync()
{
    try
    {
        lock (_saveLock)
        {
            _saveDebounceTimer?.Stop();
            _saveDebounceTimer = new System.Windows.Threading.DispatcherTimer
            {
                Interval = TimeSpan.FromMilliseconds(500)
            };
            _saveDebounceTimer.Tick += async (s, e) =>
            {
                _saveDebounceTimer.Stop();
                try
                {
                    var config = await _configManager.LoadConfigurationAsync<ApplicationConfiguration>();
                    
                    // SampleName 저장
                    config.MatchingSettings.SampleName = string.IsNullOrWhiteSpace(SampleName) ? null : SampleName;
                    
                    // MoveNir 저장 (빈 값 또는 파싱 실패 시 null)
                    config.MatchingSettings.MoveNir = string.IsNullOrWhiteSpace(MoveNir) 
                        ? null 
                        : (int.TryParse(MoveNir, out int nirValue) ? nirValue : null);
                    
                    // MoveAllData 저장 (빈 값 또는 파싱 실패 시 null)
                    config.MatchingSettings.MoveAllData = string.IsNullOrWhiteSpace(MoveAllData) 
                        ? null 
                        : (int.TryParse(MoveAllData, out int allValue) ? allValue : null);
                    
                    await _configManager.SaveConfigurationAsync(config);
                    _logger.LogDebug("Sample move settings saved: SampleName={SampleName}, MoveNir={MoveNir}, MoveAllData={MoveAllData}", 
                        SampleName, MoveNir, MoveAllData);
                }
                catch (Exception ex)
                {
                    _logger.LogError(ex, "Failed to save sample move settings");
                }
            };
            _saveDebounceTimer.Start();
        }
    }
    catch (Exception ex)
    {
        _logger.LogError(ex, "Error setting up save debounce timer");
    }
}
```

**설명**:
- **디바운싱**: 500ms 지연을 두어 빠른 연속 입력 시 마지막 값만 저장되도록 최적화했습니다.
- **스레드 안전성**: `_saveLock`을 사용하여 타이머 조작 시 경합 조건을 방지했습니다.
- **값 변환**:
  - `SampleName`: 빈 문자열이면 null로 저장
  - `MoveNir`: 빈 문자열이거나 파싱 실패 시 null로 저장, 성공 시 int 값 저장
  - `MoveAllData`: 빈 문자열이거나 파싱 실패 시 null로 저장, 성공 시 int 값 저장
- **에러 처리**: 저장 실패 시 로그만 기록하고 예외를 전파하지 않습니다.

### 6. LoadSettings() 메서드 수정

**파일**: `ChronoView/UI/ViewModels/MainWindowViewModel.cs`

**변경 내용** (Line 164-201):
```csharp
public void LoadSettings()
{
    _isLoadingSettings = true;
    try
    {
        var appConfig = (WpfApplication.Current as App)?.Services.GetService(typeof(IConfigurationManager)) as IConfigurationManager;
        var config = appConfig?.LoadConfiguration<ApplicationConfiguration>();
        if (config != null)
        {
            // ... 기존 UI 설정 로드 코드 ...

            // 샘플 이동 설정 로드 (setter 트리거 방지)
            _sampleName = config.MatchingSettings.SampleName ?? "";
            _moveNir = config.MatchingSettings.MoveNir?.ToString() ?? "";
            _moveAllData = config.MatchingSettings.MoveAllData?.ToString() ?? "";
            OnPropertyChanged(nameof(SampleName));
            OnPropertyChanged(nameof(MoveNir));
            OnPropertyChanged(nameof(MoveAllData));
        }
    }
    finally
    {
        _isLoadingSettings = false;
    }
}
```

**설명**:
- `_isLoadingSettings` 플래그를 true로 설정하여 로드 중에는 자동 저장이 발생하지 않도록 했습니다.
- 필드에 직접 값을 할당한 후 `OnPropertyChanged`를 호출하여 UI에 알립니다.
- setter를 거치지 않으므로 자동 저장이 트리거되지 않습니다.
- `finally` 블록에서 플래그를 false로 설정하여 이후 변경사항은 정상적으로 저장되도록 했습니다.

### 7. Dispose 메서드 수정

**파일**: `ChronoView/UI/ViewModels/MainWindowViewModel.cs`

**변경 내용** (Line 530-537):
```csharp
public void Dispose() 
{ 
    _saveDebounceTimer?.Stop();
    _saveDebounceTimer = null;
    _windowCts.Cancel(); 
    Dashboard.Dispose(); 
    Control.Dispose(); 
}
```

**설명**: 
- `_saveDebounceTimer`를 정지하고 null로 설정하여 리소스를 정리합니다.
- 애플리케이션 종료 시 타이머가 남아있지 않도록 보장합니다.

## 동작 흐름

### 1. 애플리케이션 시작 시

1. `MainWindowViewModel` 생성자에서 `LoadSettings()` 호출
2. `_isLoadingSettings = true` 설정
3. 설정 파일에서 `SampleName`, `MoveNir`, `MoveAllData` 값 로드
4. 필드에 직접 할당 후 `OnPropertyChanged` 호출
5. `_isLoadingSettings = false` 설정
6. UI에 이전 값이 표시됨

### 2. 값 변경 시

1. 사용자가 TextBox에서 값 입력/수정
2. 속성 setter 호출 (`SampleName`, `MoveNir`, `MoveAllData`)
3. `SetProperty`가 true를 반환하고 `_isLoadingSettings`가 false이면
4. `SaveSampleMoveSettingsAsync()` 호출
5. 기존 타이머가 있으면 정지
6. 새로운 타이머 생성 (500ms 지연)
7. 500ms 후:
   - 설정 파일 로드
   - 현재 값으로 업데이트
   - 설정 파일 저장
   - 로그 기록

### 3. 애플리케이션 재시작 시

1. 저장된 값이 자동으로 로드되어 UI에 표시됨
2. 사용자가 이전에 입력한 값이 그대로 유지됨

## 디바운싱 동작

- **목적**: 빠른 연속 입력 시 불필요한 저장 작업을 방지하고 성능을 최적화
- **동작**: 
  - 값이 변경될 때마다 타이머를 재시작
  - 500ms 동안 추가 변경이 없으면 저장 실행
  - 연속 입력 시 마지막 값만 저장됨

**예시**:
- 사용자가 "1", "2", "3"을 빠르게 입력
- 각 입력마다 타이머가 재시작됨
- 마지막 입력 후 500ms가 지나면 "3"만 저장됨

## 값 변환 규칙

### SampleName
- 빈 문자열 또는 공백만 있는 경우 → `null`로 저장
- 값이 있는 경우 → 그대로 저장

### MoveNir
- 빈 문자열 또는 공백만 있는 경우 → `null`로 저장
- 숫자로 파싱 가능한 경우 → `int` 값으로 저장
- 숫자로 파싱 불가능한 경우 → `null`로 저장

### MoveAllData
- 빈 문자열 또는 공백만 있는 경우 → `null`로 저장
- 숫자로 파싱 가능한 경우 → `int` 값으로 저장
- 숫자로 파싱 불가능한 경우 → `null`로 저장

## 영향 범위

### 수정된 파일

1. **ChronoView/Models/ApplicationConfiguration.cs**
   - `MatchingSettings.SampleName` 필드 추가

2. **ChronoView/UI/ViewModels/MainWindowViewModel.cs**
   - 필드 및 서비스 추가
   - 생성자 수정
   - 속성 setter 수정
   - `SaveSampleMoveSettingsAsync()` 메서드 추가
   - `LoadSettings()` 메서드 수정
   - `Dispose()` 메서드 수정

### 영향 없는 부분

- 기존 이동/삭제 로직
- 다른 설정 로드/저장 로직
- UI 바인딩 (기존과 동일)

## 테스트 시나리오

### 1. 초기 로드 테스트
- **시나리오**: 애플리케이션 시작
- **검증**: 이전에 저장된 값이 UI에 표시되는지 확인
- **예상 결과**: 설정 파일에 저장된 값이 TextBox에 표시됨

### 2. 값 변경 및 저장 테스트
- **시나리오**: 각 필드 값을 변경
- **검증**: 500ms 후 설정 파일에 저장되는지 확인
- **예상 결과**: 
  - 값 변경 후 500ms 지연 후 저장
  - 설정 파일에 올바른 값이 저장됨

### 3. 디바운싱 테스트
- **시나리오**: 빠르게 연속 입력 (예: "1", "2", "3")
- **검증**: 마지막 값만 저장되는지 확인
- **예상 결과**: "3"만 저장되고 이전 값들은 저장되지 않음

### 4. 빈 값 처리 테스트
- **시나리오**: 필드를 비우고 저장
- **검증**: null로 저장되는지 확인
- **예상 결과**: 설정 파일에서 해당 필드가 null로 저장됨

### 5. 파싱 실패 처리 테스트
- **시나리오**: MoveNir 또는 MoveAllData에 숫자가 아닌 값 입력 (예: "abc")
- **검증**: null로 저장되는지 확인
- **예상 결과**: 설정 파일에서 해당 필드가 null로 저장됨

## 주의사항

1. **비동기 저장**: `SaveSampleMoveSettingsAsync()`는 fire-and-forget 패턴으로 호출되므로 저장 실패 시 사용자에게 알림이 표시되지 않습니다. 로그를 확인해야 합니다.

2. **디바운싱 지연**: 값 변경 후 500ms 지연이 있으므로, 애플리케이션을 즉시 종료하면 마지막 변경사항이 저장되지 않을 수 있습니다.

3. **스레드 안전성**: `_saveLock`을 사용하여 타이머 조작 시 경합 조건을 방지했지만, UI 스레드에서만 호출되므로 실제로는 문제가 없을 것으로 예상됩니다.

## 향후 개선 사항

1. **저장 상태 표시**: 저장 중/완료 상태를 UI에 표시
2. **저장 실패 알림**: 저장 실패 시 사용자에게 알림 표시
3. **디바운싱 시간 조정**: 필요에 따라 디바운싱 시간을 설정에서 조정 가능하도록
4. **저장 전 검증**: 값이 유효한지 검증 후 저장

## 참고

- **계획서**: `c:\Users\redli\.cursor\plans\샘플_이동_설정_자동_저장_로드_계획_89a64a6f.plan.md`
- **관련 파일**:
  - `ChronoView/Models/ApplicationConfiguration.cs`
  - `ChronoView/UI/ViewModels/MainWindowViewModel.cs`
  - `ChronoView/Core/Configuration/IConfigurationManager.cs`

